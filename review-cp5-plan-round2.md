# CP5 Theorem 73 Scoping Plan — Independent Adversarial Review, Round 2

Target: `cp5-thm73-scoping` at `bde35d804b724971efa962b99526f535e07cee55`.
Round-1 basis: `review-cp5-plan-round1.md` (12 exact required changes).

## Probe log and findings

1. **[note] Review setup / repository state.** The checkout is on the requested branch and exact target commit `bde35d8`. It is ahead of its remote, with no tracked or staged worktree change; the only pre-existing item is untracked `paper/`. This requested round-2 report was absent/untracked. Idris 2 is version 0.8.0. The report is the only repository artifact this review will create; all elaboration probes are placed under `/tmp/thm73-review2-probes/`.

2. **[note] Immutable external probe tree.** Copied the exact target `src/` tree and all five revised research modules to `/tmp/thm73-review2-probes/src/` (212 Idris modules). No Idris process was active. All subsequent checks use this external tree and cannot alter tracked repository files.

3. **[note] Revised local-diamond spike elaborates.** `idris2 --source-dir src --check src/DGamma/CP5ConfluenceLocalDiamondSpike.idr` passed under Idris 2 v0.8.0 in the external tree (31-module closure). This establishes API/index compatibility only; the named holes remain unproved.

4. **[minor] Revised deletion-chain check timed out on first run.** The sequential external command exceeded 600 seconds before returning diagnostics. This is a validation-time failure, not yet an elaboration failure; no concurrent Idris process had been running. I will verify process cleanup and rerun once with a longer timeout before classifying the interface.

5. **[note] Revised deletion-chain spike elaborates on rerun.** The longer sequential rerun of `CP5ConfluenceDeletionChainSpike.idr` passed (165-module closure); the first timeout was cold-build latency. Diagnostics were only existing lowercase-shadowing warnings. The strengthened fields are API-valid but still named-hole obligations.

6. **[note] Revised canonical-sort spike elaborates.** `CP5ConfluenceCanonicalSortSpike.idr` passed sequentially against the exact release API (dependency cache reused). In particular, Idris accepts the exact `CanonicalSupportTransport` and `oneTraceCanonicalScheduleSpike` indices.

7. **[note] Revised renaming-composition spike elaborates.** `CP5ConfluenceRenamingCompositionSpike.idr` passed sequentially (8-module closure). The new equality fields coupling the composed current endpoint to the composed bijection are accepted by the release types.

8. **[note] Revised cross-trace spike elaborates.** `CP5ConfluenceCrossTraceSpike.idr` passed sequentially (33-module closure), including the complete outer `confluenceResultFromCanonicalCapital` implementation. Elaboration does not establish that the hole-bearing convergence/endpoint bridge obligations are derivable.

9. **[note] Exact one-trace assembly probe, invocation 1 needs direct imports.** The external constructor implementation failed before index checking because `DecEq` and `MkCanonicalSchedule` are not re-exported through the spike import. This is a probe-module scope error, not evidence against the revised pipeline. I will import `DGamma.CP3` and `Decidable.Equality` directly and rerun.

10. **[note] Exact one-trace assembly probe, invocation 2 needs two more direct imports.** After adding CP3/`DecEq`, elaboration again stopped only on non-re-exported names (`SystemState` and `replayDiscipline`). I will add direct `DGamma.Calculus` and local-diamond imports; no dependent index has failed yet.

11. **[minor] Exact one-trace assembly probe exposes an export/accessibility concern.** Invocation 3 still had a missing direct deletion-chain import, but it also resolved `ReplayInvariantBundle.replayDiscipline` and reported the projection “not accessible in this context.” Because `CanonicalSchedule` requires both registration-discipline proofs and the proposed exact constructor is in a different module, I will isolate whether this is only import/qualification or whether the record field is not externally usable. If the latter, the pipeline is type-shaped but not consumable.

12. **[note] Exact one-trace assembly probe, invocation 4 identifies record namespace.** With all direct imports present, the only failure was the qualifier: Idris suggests the generated projection `DGamma.CP5ConfluenceLocalDiamondSpike.ReplayInvariantBundle.replayDiscipline`. I will use that exact name. This reduces the prior accessibility concern to namespace resolution unless the next run proves otherwise.

13. **[note] Exact one-trace producer→consumer assembly passes.** The external `assembleScheduleProbe` now constructs `MkCanonicalSchedule` completely, with no hole, from exactly `CanonicalizationPremises`, `ClosingFreeReduction`, reduced-endpoint `SupportOrderingCapital`, `SortedClosingFreeTrace`, `CanonicalSupportTransport`, and `OneTraceOrchestrationAccounting`. It passed. This positively confirms the revised deletion/canonicalization/support-transport/accounting outputs can feed every immutable `CanonicalSchedule original` field, including both discipline proofs and definitionally coupled endpoint/registration metadata.

14. **[note] Generic replay-correspondence composition is derivable; wrapper probe needs CP3 import.** The external `composeReplayCorrespondenceProbe` elaborated completely from the two one-way generator/stage origin maps and map/outcome equations. The same module then stopped on a non-re-exported `CanonicalSchedule` name before checking wrapper assembly. Thus the strengthened generic correspondence is plausibly sufficient and composable, but the revised research set does not itself state this transitivity helper despite needing it between deletion and sorting.

15. **[major] One-trace schedule output is not propositionally connected to the replay correspondence needed by cross-trace matching.** The wrapper assembly probe cannot even state `canonicalTrace schedule = sortedTrace sorted` for an arbitrary result of type `CanonicalSchedule original`: the hidden endpoints `canonicalFinal schedule` and `sortedFinal sorted` need not coincide. `oneTraceCanonicalScheduleSpike` returns only the public schedule, while `IndependentCanonicalSchedule` additionally needs `RelationalReplayCorrespondence original (canonicalTrace schedule)` and canonical independence. The five revised interfaces contain no producer for that wrapper and no schedule/result equality. A transparent implementation might reduce when constructed and wrapped in the same definition, but that is not an interface guarantee and the advertised producer→consumer chain is incomplete. Add an exact `independentCanonicalScheduleSpike` (or return an enriched one-trace package) that constructs the schedule and replay correspondence together.

16. **[note] Simultaneous enriched-package probe, invocation 1 needs direct deletion import/qualification.** The attempt to construct `IndependentCanonicalSchedule` in the same definition as the exact schedule stopped on a non-re-exported `CanonicalizationPremises` name and unqualified external helper access. No canonical-trace/replay index mismatch was reached; I will add the direct import and exact module qualification.

17. **[note] Simultaneous enriched-package probe, invocation 2 reaches only a missing direct local-diamond import.** The schedule helper and composed correspondence now align definitionally. Idris stopped on the non-re-exported generic independence theorem name, so I will import its defining module directly. This is scope, not an index failure.

18. **[major] Positive simultaneous-package attempt confirms the opaque endpoint mismatch.** After all direct imports, Idris rejects the fully assembled schedule plus composed deletion/sorting replay correspondence at `MkIndependentCanonicalSchedule`: “Can't solve constraint between: `sorted.sortedFinal` and `schedule.canonicalFinal`.” Calling a complete external one-trace constructor does not make its hidden `canonicalFinal` definitionally equal to the sorting result across the module boundary. This validates Finding 15 as a real producer/consumer type gap, not merely an inability to state heterogeneous trace equality.

19. **[blocker] Fresh gap: local transpositions omit the orchestration→activation orientation required to swap whole blocks.** `CP5ConfluenceLocalDiamondSpike.idr` states A/A, activation-then-orchestration (A/O), and O/O only. A canonical actor body may end with yielded `OInsert`; swapping the following actor block leftward makes that O-Insert cross the following block's L-Begin as an O/A adjacent pair. The negative probe applying `activationOrchestrationDiamondSpike` to a checked O/A pair fails exactly with `PaperActivationStep right` versus required `PaperActivationStep left`. Relational diamonds are source-sensitive and cannot be inverted for free. Add a source-sensitive `orchestrationActivationDiamondSpike` (or prove and encode a sorting orientation that never requires O/A crossings; ordinary whole-block bubble swaps do require them).

20. **[note] Support-path overstrength probe, invocation 1 needs explicit lookup parameters.** The generic target-present lemma failed because Idris could not infer the dependent `lookupFiber` key/value parameters; subsequent errors were cascading. I will annotate them explicitly and rerun. The intended contradiction has not yet been checked.

21. **[note] Support-path overstrength probe, invocation 2 needs explicit implicit binders.** Even with lookup arguments, Idris's auto-implicit generalization did not couple the two occurrences of the dependent `key`/`value`. I will replace the helper signature with explicit `{name,key,...}` and `{value : key -> Type}` binders. This remains probe elaboration plumbing.

22. **[note] Support-path overstrength probe, invocation 3 reaches only dependent-pair let syntax.** The target-present lemma now elaborates; the remaining failure is Idris rejecting a dependent-pair pattern in a `let`. I will use an explicit `case` without changing the argument.

23. **[note] Support-path overstrength probe, invocation 4 needs direct coeffects import.** The proof reached construction of the original `ParentSupportEdge`; unification requested the non-re-exported lookup implementation `DGamma.Coeffects.lookupBinding`. I will import `DGamma.Coeffects` directly. No semantic premise failed.

24. **[note] Support-path overstrength probe, invocation 5 reveals quantity mismatch in the probe.** `CanonicalSupportTransport.supportPathToReduced` is an unrestricted (not quantity-0) field, while the contradiction helper was declared erased, causing the locally constructed path to be inaccessible as a runtime argument. I will make the probe helpers unrestricted to match the revised interface. This also notes that several proof-looking path fields in the research record are runtime data, but that is not itself a soundness defect.

25. **[note] Support-path overstrength probe, invocation 6 hit proof/runtime quantity friction rather than refuting the argument.** Making the helper unrestricted fails because `SupportPath` hides intermediate indices and `endpointNamesWithdrawn` is erased; the original erased helper had elaborated. I will restore erased proof helpers and inline the constructed original path into the unrestricted projection, avoiding an erased local passed to a runtime-valued record field.

26. **[blocker] `CanonicalSupportTransport` is over-strong and excludes an endpoint case the accepted relation explicitly permits.** The completed external proof `transportForbidsPermittedVestigialChild` shows: if `endpointWithdrawnNames` contains a child still present in the original endpoint as a retired `ChildOf parent` fiber (the `VestigialNameWithdrawn` case), then `CanonicalSupportTransport.supportPathToReduced` maps the immediate original `SupportParent` path to a reduced path ending at that absent child; every `SupportPath ... _ child` entails a reduced lookup for `child`, contradicting `endpointNamesWithdrawn`. This is not a hypothetical shape: `RawNamesWithdrawn` deliberately permits exactly such retired, clean, original-present vestigials. Therefore the strengthened support spike is generally uninhabitable. Restrict path transport to supported endpoints/nodes, or remove unrestricted path fields and prove only the supported-order facts actually needed by `LinearizesSupport`/input placement. Re-spike the exact corrected transport and assembly.

27. **[blocker] Cross-trace wrapper carries independence but drops the full premise bundle required by its own swap primitive.** `canonicalSchedulesConvergeSpike` is supposed to execute certified adjacent transpositions using `adjacentSwapSuffixSpike`, whose mandatory input is `ReplayInvariantBundle ... original`. But `IndependentCanonicalSchedule` contains only `schedule`, original/canonical `TraceIndependent`, and replay correspondence; a public `CanonicalSchedule` supplies discipline but not alignment, empty/well-formed start, endpoint well-formedness, totality, quiet/no-failure, provenance/ranks, `PrecedenceAcyclic`, `SupportWellFounded`, or `SupportMatchesActive`. Thus O20 cannot feed O6. Strengthen the internal canonical wrapper with `canonicalReplayPremises : ReplayInvariantBundle ... (canonicalTrace schedule)` (and make the one-trace producer return it), or change the convergence interface to consume the exact bundle explicitly.

28. **[blocker] Final endpoint composition still lacks deleted-generation classification capital.** A targeted interface scan finds no research reference to accepted `DeletedClosingRegistration`, `RegistrationTraceCorrespondence`, `leftDeletedGenerations`, or `rightDeletedGenerations`. `ClosingFreeReduction.deletionGenerationHistory` is only an unconstrained list-of-lists plus `concat = endpointWithdrawnGenerations`; it does not retain which closing episode/parent activation made each generation deleted. `CanonicalRegistrationCorrespondence` proves merely “actual original generated birth and absent canonically.” Yet `SystemEquivalentByRenamingModuloVestigial` classifies an original-present unmatched fiber with `VestigialEndpointGeneration`, which requires its current generation to belong to the **accepted same-input scanner's deleted-generation list**. `canonicalSchedulesToOriginalEndpointSpike` receives only the two public schedules, `sameInputs`, and canonical bridge, so the needed link has been erased. Retain a typed per-generation deletion-classification witness through `ClosingFreeReduction`/one-trace capital and make O21 consume enriched schedules tying each withdrawn generation to `leftDeletedGenerations`/`rightDeletedGenerations` of `generatedRegistrationTree sameInputs`.

29. **[blocker] `MappedCanonicalSupportOrders` repeats the same unrestricted-path overstrength across traces.** The external `mappedPathsForbidAbsentRenamedTarget` proof passes: `mappedSupportPathForward` implies that every left path's renamed target has a right endpoint fiber. But accepted `CurrentEndpointRenaming` explicitly admits vestigial current generations, and vestigial raw names may be present on one endpoint and absent on the other; `SupportPath` itself is not restricted to supported actors and can end at a retired vestigial child via `SupportParent`. The mapped-order theorem only needs comparability among names in the support orders. Restrict forward/backward path fields to supported/order-member endpoints (and, if needed, supported intermediate paths), then certify the permutation against that restricted partial order.

30. **[note] Hygiene probe, invocation 1 passes immutability/reachability but needs a comment-aware escape scan.** `git diff --exit-code 34b21c9..bde35d8 -- src dgamma.ipkg` is empty, and no `src`/ipkg reference reaches `research`/CP5 spikes. The combined source scan stopped on the English comment “no postulate or escape hatch is used” in `CP3.idr:745`, a false positive rather than a declaration. I will rerun with named holes/actual escape-call patterns and inspect any hits.

31. **[note] Hygiene checks pass.** The comment-aware rerun confirms: (a) `git diff 34b21c9..bde35d8 -- src dgamma.ipkg` is empty; (b) `research/` and `CP5Confluence*` are unreachable from `src`/`dgamma.ipkg`; (c) branch `bde35d8` and local `main` `src/**/*.idr` contain no named holes, `believe_me`, `assert_total`, or actual `postulate` declarations under the scan; and (d) no files are staged. Worktree items are only pre-existing untracked `paper/` and this mandated untracked review report.

32. **[note] Exact release package build passes externally.** `idris2 --build dgamma.ipkg` completed all 207 package modules in `/tmp/thm73-review2-probes`. Research modules were not built. Diagnostics were only established lowercase-shadowing warnings, confirming revision 2 did not disturb release buildability.

33. **[note] Bijection-coupling projection probe, invocation 1 needs direct calculus import.** The exact equality helper stopped because `SystemState` hidden indices in the composed record were not in scope through transitive imports; the body error cascaded. I will import `DGamma.Calculus` directly and rerun.

34. **[note] Bijection-coupling projection probe, invocation 2 needs direct `DecEq` import.** With `SystemState` in scope, only the record's hidden `DecEq` parameter name remains unavailable. I will add `Decidable.Equality`; no equality mismatch has appeared.

35. **[note] Round-1 major #23 (composition bijection coupling) is concretely fixed.** The completed external projection proves `currentNameBijection (composedCurrentEndpoint result) = composeNameBijection leftCurrentRenaming rightCurrentRenaming` by composing `composedCurrentUsesBijection` with `composedNameBijectionExact`. This is a real proposition exposed by the revised record, not a prose assertion.

36. **[note] Round-1 major #46 (Lemma-70 capital) is fixed at the shared bundle boundary.** The complete external `lemma70CapitalProjectionProbe` extracts exact `ReachedFromEmpty`, `PrecedenceAcyclic`, `SupportWellFounded`, and `SupportMatchesActive` values from `ReplayInvariantBundle`. The record also directly contains provenance and protocol/parent ranks. Deletion and sorting outputs carry this bundle; Finding 27 is specifically that cross-trace `IndependentCanonicalSchedule` later drops it.

37. **[major] The 50–90 range is directionally honest but not currently estimable against impossible/missing interfaces; several grades are outliers.** Revision 2 now has 23 named research holes across 1,290 spike LOC. After correcting Findings 18–29, roughly **60–100 shifts** is more credible until the new O/A, supported-path, enriched canonical-wrapper, and deleted-generation-classification gates are spiked. Grade outliers: O2 is likely **L rather than XL** once `RelationalReplayCorrespondence` exists (the external correspondence composition was short; transformation lifting is structural); O13 is **S–M rather than L** because the exact capital is already projected from `ReplayInvariantBundle`; and O18's final constructor assembly is **S–M rather than L–XL**, as the complete external assembly probe demonstrates—the hard accounting belongs in O11/O16/O17. Conversely O20/O21 are under-scoped, not under-graded: keep XL but add the missing bundle/history work. A 50–90 band can be reconsidered only after corrected interfaces elaborate and countermodels are closed.

38. **[note] Interface citation inventory.** Exact revised definition locations used below: deletion step/reduction at `CP5ConfluenceDeletionChainSpike.idr:91,150`; replay/bundle/diamond/safety/adjacent result at `CP5ConfluenceLocalDiamondSpike.idr:55,98,179,215,255`; support/sorted/accounting/schedule at `CP5ConfluenceCanonicalSortSpike.idr:78,126,222,258`; independent/mapped/convergence at `CP5ConfluenceCrossTraceSpike.idr:19,78,140`; and coupled/bridge/O21 at `CP5ConfluenceRenamingCompositionSpike.idr:52,120,170`.

## Verification of the twelve round-1 required changes

| # | Status | Actual revised interface evidence and adversarial result |
|---:|---|---|
| 1 | **addressed** | `THM73-PLAN.md` now states 23 total obligations (O1–O22 proof, O23 validation) and withdraws 24–50 in favor of provisional 50–90. Estimate caveats/outliers are discussed below. |
| 2 | **partially addressed** | `ClosingFreeReduction` (`research/DGamma/CP5ConfluenceDeletionChainSpike.idr:150`) now has `reductionSameExternalInputs`, `reductionReplayCorrespondence`, `cumulativeEndpoint`, exact-list `deletionHistoryAligned`, and `cumulativeRegistrationAccounting`. These are enough for the accepted schedule, as Probe 13 proves. However `deletionGenerationHistory` is only `List (List RegistrationGeneration)` and is not tied to per-step candidates/closed parent activations or accepted `DeletedClosingRegistration`; the requested history needed by final composition is still lost (Finding 28). |
| 3 | **partially addressed** | `RelationalReplayCorrespondence` and `traceIndependentAfterRelationalReplaySpike` (`...LocalDiamondSpike.idr:55,84`) are generic; `DeletionChainStep`, `AdjacentSwapResult`, and `SortedClosingFreeTrace` carry instances. The universal-public-`DeletionResult` defect is fixed. But no declared correspondence-transitivity/one-trace enriched producer connects the final schedule's hidden canonical trace to the composed correspondence; Probes 14–18 expose the exact endpoint mismatch. |
| 4 | **partially addressed** | `ReplayInvariantBundle` (`...LocalDiamondSpike.idr:98`) contains alignment, discipline, empty/well-formed start, endpoint WF, totality, quiet/no-failure, independence, provenance, ranks, `PrecedenceAcyclic`, `SupportWellFounded`, and `SupportMatchesActive`. `DeletionChainStep.nextPremises`, `AdjacentSwapResult.swappedPremises`, and `SortedClosingFreeTrace.sortedPremises` retain it. `IndependentCanonicalSchedule` then drops it before cross-trace swapping, so “every recursive deletion/swap state” is not achieved. |
| 5 | **addressed** | `LocalRelationalDiamond` (`...LocalDiamondSpike.idr:179`) has `movedRightTag`, `movedLeftTag`, all four activation/orchestration branch reconstructions, effects/controls/WF; replay iterator outcomes are preserved by `RelationalReplayCorrespondence.replayIteratorOutcomePreserved`; `AdjacentSwapResult` (`:255`) carries the replayed suffix, same external inputs, endpoint, correspondence, and next bundle. |
| 6 | **addressed (narrow round-1 requirement)** | `OrchestrationSwapSafety` and `orchestrationOrchestrationDiamondSpike` (`...LocalDiamondSpike.idr:215,328`) add a checked early-right firing, pair discipline, generation scan, distinct inserted children, and anti-license-cross conditions. `AdjacentSwapResult.swappedSameExternalInputs` prevents unsafe external-root reordering. G2 is a stated fail gate. A separate newly discovered O/A orientation blocker remains (Finding 19). |
| 7 | **partially addressed, but unusable** | `CanonicalSupportTransport` (`...CanonicalSortSpike.idr:78`) names support truth, both path maps, parent lookup, linearization, and input placement; `oneTraceCanonicalScheduleSpike` (`:258`) has the exact accepted output. Probe 13 confirms assembly. Probe 26 proves the unrestricted original→reduced path field contradicts the accepted vestigial-child endpoint case, so the central transport obligation is not inhabitable as stated. |
| 8 | **partially addressed, but unusable** | `MappedCanonicalSupportOrders` (`...CrossTraceSpike.idr:78`) has forward/backward paths and `CertifiedIncomparablePermutation`. Probe 29 proves the unrestricted path fields force endpoint presence for mapped vestigials, contrary to accepted current-renaming cases. The certified sequence is useful only after the path relation is corrected. |
| 9 | **partially addressed** | `IndependentCanonicalSchedule` (`...CrossTraceSpike.idr:19`) explicitly carries original and canonical `TraceIndependent`, and both matching/convergence spikes consume left/right wrappers. But no revised producer can construct the wrapper across the hidden schedule endpoint (Finding 18), and it lacks the `ReplayInvariantBundle` required by every operational cross-trace adjacent swap (Finding 27). |
| 10 | **partially addressed** | The generic `CoupledComposedModuloVestigialEndpoint` (`...RenamingCompositionSpike.idr:52`) genuinely couples current endpoint and composed bijection (Probe 35). `canonicalSchedulesToOriginalEndpointSpike` (`:170`) now consumes schedules + fixed bridge + accepted `sameInputs` and returns the exact original-final relation. However its inputs omit typed links from schedule-withdrawn generations to the accepted scanner's deleted-generation sets, so the four vestigial cases cannot in general be classified (Finding 28). |
| 11 | **partially addressed** | External reflexive/transitive spikes, per-deletion/per-swap same-input fields, `OneTraceOrchestrationAccounting`, and exact endpoint-indexed `CanonicalRegistrationCorrespondence` are present. What is still absent is semantic retained/deleted classification against `RegistrationTraceCorrespondence`; no research interface mentions `DeletedClosingRegistration` or `leftDeletedGenerations`/`rightDeletedGenerations`. |
| 12 | **addressed** | `THM73-PLAN.md` section 5 now separates mathematical G1–G5 pass/fail gates from four actual architecture decisions and states concrete failure consequences. |

### Special blocker disposition

- **Closing-free evidence payload:** improved enough for public schedule assembly, but semantically incomplete for final vestigial composition.
- **Local tag/outcome preservation:** addressed by explicit tag/branch equations plus iterator outcome correspondence.
- **O/O obligation:** present and source-sensitive; **new O/A orientation is absent**.
- **Original→reduced support/input transport:** exact indices are present, but the path transport is inconsistent with accepted vestigials.
- **Cross-trace independence:** fields are present, but the wrapper has neither a producer nor the full swap bundle.
- **Renaming composition input families:** exact schedule/bridge families are now used, but deleted-generation classification is absent.
- **Round-1 major #23:** addressed; Probe 35 derives the exact composed bijection equality.
- **Round-1 major #46:** addressed at `ReplayInvariantBundle`; Probe 36 projects exact `ReachedFromEmpty` and acyclicity capital. Cross-trace later drops the bundle.

## Revised pipeline attack

1. **Deletion chain → closing-free reduction:** trace indices, next premises, external inputs, replay correspondence, endpoint, and public canonical registration correspondence line up. The cumulative history is not semantically typed.
2. **Reduction → shape/order/sort:** reduced premises feed shape/order/sort. The proposed bidirectional support graph bridge is false for vestigials.
3. **Sort/accounting → accepted one-trace schedule:** **passes exactly**; Probe 13 implements the whole `MkCanonicalSchedule` constructor without a hole.
4. **Accepted schedule → independent canonical capital:** **fails**; Probes 15 and 18 show the schedule's hidden final is not definitionally/propositionally connected to the sorted trace correspondence across the module interface.
5. **Independent capital → cross-trace operational sorting:** **fails**; the wrapper lacks `ReplayInvariantBundle`, and mapped path capital is over-strong. Whole-block sorting also needs O/A local commutation.
6. **Canonical bridge → original endpoint relation:** the result type is exact and fixed to the accepted bijection, but inputs lack evidence that every unmatched original-present generation is in the accepted scanner's deleted set.
7. **Original endpoint relation → `ConfluenceResult`:** exact and complete; `confluenceResultFromCanonicalCapital` remains a correct small constructor.

## New attacks introduced by revision 2

- **Uninhabitable strengthened support transport:** Probe 26 gives a checked contradiction for permitted original-present/reduced-absent vestigial children.
- **Uninhabitable strengthened mapped paths:** Probe 29 shows unrestricted path mapping excludes permitted mapped endpoint absence.
- **Missing O/A orientation:** Probe 19 demonstrates the existing A/O theorem cannot consume orchestration-then-activation pairs encountered while swapping yielded-registration-bearing blocks.
- **Opaque enriched-schedule boundary:** a public schedule alone hides the final/trace needed to reuse the actual replay correspondence.
- **Cross-trace invariant loss:** canonical independence was added, but every other recursive swap invariant was omitted.
- **Withdrawal history erasure:** the stronger final O21 conclusion needs scanner deletion membership that no revised record retains.

## Estimate review

The provisional range is substantially more honest than round 1, but no finite band is credible until the impossible support/path interfaces and missing enriched boundaries are corrected. After correction, use approximately **60–100 shifts** provisionally. Outliers: O2 should likely be **L**, not XL, once correspondence exists; O13 should be **S–M**, not L, because it is bundle projection/accepted theorem invocation; O18 final assembly should be **S–M**, not L–XL, as Probe 13 demonstrates. O20/O21 should remain XL but must absorb the missing bundle and typed deleted-generation work rather than treating it as already scoped.

## Hygiene

- All five revised spike modules elaborate sequentially under Idris 2 v0.8.0.
- Exact external release build passes all 207 package modules.
- `research/` is unreachable from `dgamma.ipkg` and `src/`.
- `git diff 34b21c9..bde35d8 -- src dgamma.ipkg` is empty.
- Branch and `main` source trees pass named-hole/escape-hatch scans.
- No staged files or tracked worktree edits exist; only untracked `paper/` and this mandated review report are present.

## Exact changes required for round 3

1. **Add the missing O/A local case.** State and elaborate a source-sensitive `orchestrationActivationDiamondSpike` with early activation applicability, parent/freshness restrictions, tag/branch preservation, and integrate it into adjacent whole-block sorting/cross-trace transposition. Alternatively provide a certified sorting algorithm/interface proving O/A is never needed; the current block shape makes that unlikely.
2. **Replace `CanonicalSupportTransport`'s false unrestricted path maps.** Do not map paths ending in withdrawn vestigials. Prefer constructing `LinearizesSupport` at `originalFinal` before deletion and transporting only the facts needed to sort the reduced trace/input placement, or define a precisely support-restricted relation and prove it is adequate. Re-elaborate the exact schedule constructor against the corrected capital.
3. **Replace `MappedCanonicalSupportOrders`' false unrestricted path maps.** Certify comparability only in the actual common partial order used for supported canonical blocks, with explicit treatment of paths through unsupported/vestigial fibers; retain the adjacent permutation only after proving both orders linearize that same relation.
4. **Introduce one exact enriched one-trace output/producer.** It must jointly return the accepted `CanonicalSchedule`, `RelationalReplayCorrespondence original (canonicalTrace schedule)`, `ReplayInvariantBundle ... (canonicalTrace schedule)`, original/canonical independence, and the definitional/equality coupling needed by consumers. Add and prove/spike replay-correspondence transitivity rather than leaving it implicit.
5. **Retain typed deletion classification, not a plain list history.** For every cumulative withdrawn generation, carry occurrence/closed-parent evidence sufficient to derive accepted `DeletedClosingRegistration` and membership in the corresponding `leftDeletedGenerations`/`rightDeletedGenerations` scanner output. Preserve it through deletion + sorting accounting.
6. **Make cross-trace convergence consume the enriched capitals.** Its operational adjacent swaps must receive the full canonical `ReplayInvariantBundle`, not only independence, and must return the next bundle through every certified permutation step.
7. **Strengthen O21 inputs with the typed withdrawal/scanner links.** `canonicalSchedulesToOriginalEndpointSpike` must consume both enriched one-trace capitals (or equivalent exact fields) so all original-present unmatched fibers can inhabit `VestigialEndpointGeneration` under `generatedRegistrationTree sameInputs`. Keep the now-correct fixed-bijection coupling.
8. **Revise the closure matrix and estimates after the corrected probes.** Mark the currently partial changes honestly, add negative regressions for vestigial support paths and O/A crossings, sequentially recheck all spikes, and re-estimate from the corrected gates (provisionally 60–100).

## Verdict

**REJECT.** Revision 2 fixes the visible tag payload, exact one-trace schedule assembly, O/O surface, composition-bijection coupling, and Lemma-70 bundle capital, but the strengthened support and mapped-path records are provably incompatible with accepted vestigial endpoint cases; whole-block sorting still lacks O/A commutation; the accepted schedule cannot be connected modularly to the replay correspondence/full bundle needed by cross-trace swaps; and final endpoint composition lacks typed membership in the accepted deleted-generation scanner. These are inhabitation and producer/consumer defects in the revised research interfaces, not proof-implementation details, so proof shifts should not start before round 3 addresses all eight changes above.
