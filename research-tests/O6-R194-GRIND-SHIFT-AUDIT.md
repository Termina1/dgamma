# R194 main-lane grind-shift audit

## Baseline and guards

Start 2026-09-08T23:53:53Z, branch cp5-thm73-scoping, exact HEAD
b81362d88d8b52298ddd8a1bff41e741fde65d97. Only untracked paper/ and
review-o6-body-adversarial.md. Idris2 0.8.0. Timeout03:53:53Z;
new-attempt cutoff03:13:53Z; frozen-validation start cutoff03:28:53Z;
final gate03:38:53Z. R193 audit/verification/lane2 integration read FIRST,
then R192 audit/memo/verification/gate/manifest and supporting guard material.
No new full-paper-rereading attestation is made; R193's corrected manifest is
historical evidence, not a claim about this shift.

R193 check/commit/artifact/frozen guards adapted as run-r194-*; S0-1 fresh
CrossTrace PASS6.257s,3,808,608KiB, own Building. Bootstrap artifact60baab2f.
The R194 runner now retains every sampled RSS and compares the target bytes
with the launch snapshot while monitoring, interrupting on mutation. The
R193 runner inspected at baseline lacked that mutation comparison despite
its intended protocol; this shift does not retroactively re-label R193 runs.
No target mutation occurred in R194. Samples of0 mean no live sample captured,
not zero resource usage. No seed deletion/cold build or orphan kill.

One native compiler at a time in THIS worktree. Lane2 compilers encountered
at guards are classified "lane-2 compiler (separate worktree)", never killed
or counted against main concurrency; their command lines are recorded only.
No cd/edit/build/commit in lane2 and no lane2-created result target in main.
Inherited main-baseline variant modules are included only as clarified by the
explicit preflight ruling below; that is not certification of lane2 results.
Shared heavy lock policy: atomic mkdir /tmp/dgamma-heavy.lock with JSON
owner(lane R194-main,pid,timestampUTC,unit,path), maximum20min wait, only
age>25min plus dead-owner cleanup logged. No heavy check launched yet.
Default48GiB; only unchanged-source pinned LocalDiamond validations may use
standing52GiB, with its actual sampled peak retained.

Production src/ + dgamma.ipkg frozen against34b21c9; CP3 blob
2c697e532e83989de8591fa6a4378747c6a501c0. All five protected spike bodies,
four hole statements, O19, LocalDiamond and A11 record frozen. No protected
body attempt until all convergence inputs are producer-owned. No O17/O21
work, G31, source surface revision or unsafe escape.

## Unit A cap:30/30 retained; no convergence-body attempt

A1–A7: executable canonical-ordinal conjugation of the accepted ORIGINAL
registration map by the two actual replay-generation bijections. Both
bijection laws proved. Actual generated occurrence maps discharge both
replay-stamp equations. A single typed birth attachment carries the SAME
opposite occurrence, its original-origin equation and its actual physical
canonical/replayed ordinal equation simultaneously. Accepted capital produces
this attachment on the original SUPPORTED-child scope, including subsequent
operational replay of the left canonical trace. This is not an unsupported
birth producer or an assertion that the original map acts on reordered stamps.

A8–A17: native Iter/last-Finish packets transported to the exact effect-source
resolver/table. Primitive Advance equations retain literal native targets;
evaluator determinism attaches these targets to the ACTUAL checked endpoints.
The existing E46 history successor is applied to those endpoints. Iter and
last-Finish extract both authentic callbacks and resolver values, retaining
source lookups and target guards explicitly at that intermediate boundary.

A18–A22: a new opposite-source observation INDEXED by the already chosen left
component/program. The earlier unindexed shared-source result did not expose
those equalities to this consumer. Native lifecycle/fiber relations and the
actual primitive lookup produce the indexed packet without a right lookup or
shared-program premise. This is not an exhausted Either-role callback adapter.

A23–A30: target guard indexed by ONE native fiber, transported by equality of
explicit fiber values from one primitive lookup. Native single-role Iter
source inversion then produces source program, opposite source, both targets,
both resolver/callback observations and both native endpoint attachments.
`o20HistoryCheckedIterCut` needs only an input history cut and TWO ACTUAL
checked Iter edges, plus the explicit local ordinal/live-environment indices.
It does NOT pair arbitrary canonical traces or create a target edge. Latest
source commit9231d651, fresh PASS4.184s/3,810,544KiB.

All30 declarations immediately guarded committed.33 proof invocations:
31 PASS and2 rejected. A8-1 PASS was followed by A8-2 PASS to correct a
misleading pre-commit docstring (success equation IS stored in the observation).
A15-1 had a missing DIRECT defining import, rejected despite exit0; A15-2 PASS.
A29-1 rejected a named state pattern unified by Refl; A29-2 uses an anonymous
forced index, not a nonlinear binder, and clarifies its docstring. All earlier
source snapshots/logs remain append-only. No3/3 exhaustion, interruption,
unchecked retained declaration or extra hole. A cap respected.

### Exact remaining Unit A obligations

(i) Whole accepted canonical/replayed trace pairing is NOT produced by the
unilateral role word. The supported birth ordinal triangle is now attached,
but arbitrary unsupported retained/closing birth disposition must still own
an opposite retained canonical occurrence (or a justified absent/stuttering
case) and an internal raw bijection compatible with all live births. Root
insertion ordinal attachment also requires a proof for the actual replay:
`ActionRegistrationReplayCorrespondence.replayGeneratedOrdinalPreserved`
quantifies generated births, not root births. No new root map law is assumed.
Actual Iter successor extraction is closed locally; the generic last-Finish
successor still exposes its source/target observations. No whole stage sequence
or alignment-to-each-schedule-ordinal theorem has been constructed.

(ii) Therefore no arbitrary initial-to-endpoint history/all-name cut fold is
producer-owned. Local cuts and empty origin are not a whole-cut producer.

(iii) Needed endpoint lemma: for the ACTUAL endpoint live environments and
internal history bijection, construct ALL-name `O20AllNameCut` at the supplied
current-name bijection. Supported/non-vestigial agreement alone does not prove
this. The present vestigial branch still needs canonical endpoint absence or
exact current-map control agreement; the unchanged O21 vestigial package is
an original-endpoint alternative, not automatically D5's exact Maybe-fiber
relation. E8 generation-only history classification cannot manufacture this
all-name rebasing. R193 present-vestigial mismatch retains its stated scope;
no protected-theorem counterexample is claimed.

(iv) D5 still consumes that missing all-name cut. No bridge producer and no
`canonicalSchedulesConvergeSpike` body attempt. Fresh final census pending;
last audited4=1/2/0/0/1. Proceeding to pre-authorized Unit B, not extending A.

## Unit B cap:16/16 retained; requested conditional theorem proved

B1–B3: original/canonical/operational occurrence origins plus raw uniqueness
produce the two former E60 component attachments at the ACTUAL first block end
and ACTUAL second opening. `o20BlockEndReferenceComponent` and
`o20BlockOpeningReferenceComponent` need no component-equality hypothesis.
B4–B8: the installed body owns endpoint installedness; an explicit primitive
lookup observer yields present evidence; accepted original supportedness owns
both reference lookups. These are erased packets, not assumed semantic frames.

B9–B10: the whole installed opening/body frame now derives the right Begin at
the selected inversion's OWN native left cut. Child exclusion, original support,
source well-formedness, all three lookups and both component attachments are
produced. Only literal `ZeroGapPending` remains as a physical safety input.
B11: `o20ReachedInversionOwnCutSafe` proves the native candidate check succeeds.
B12–B13: authentic enumeration membership, primitive actor-name equalities and
produced own-cut clauses imply whole finite safe-selector presence. No equation
of independent dependent records, native memberships or swap proofs is used.
B14: the existing operational progress producer exposes its own exact defining
Maybe-map equation, without changing its existing body or choosing a payload.
B15–B16: the actual stopped search's own rejection contradicts an actual finite
inversion under quantified native-slot gap0. Thus
`o20StoppedOrderEqualsGoalZeroGap` proves stoppedOrder=goalOrder, CONDITIONALLY
on literal ZeroGapPending for each reached goal inversion. The total search and
equality-or-inversion observation are authentic producers, not caller guesses.

All16 declarations immediately guarded committed;18 invocations=16 PASS/2
rejected. B10-1 and B12-1 lacked direct defining imports; their second attempts
passed. No exhausted unit. Last source408bd21e, B16-1 fresh PASS13.525s,
3,818,528KiB. Across A/B:46 declarations/46 receipts,51 proof invocations,
47 PASS/4 rejected; plus one baseline gives52 total pre-final invocations.

**Exact scope:** the final theorem consumes base accepted original capital and
an actual reached reference-search record. Its only UNDISCHARGED additional
safety condition is quantified ZeroGapPending at native inversion slots,
whose existing definition is exactly `transitionCount gap = 0`. It is not
proved for arbitrary gaps. A12Pending is unnecessary in this frozen theorem:
zero actual transitions excludes every intervening root step. No extended-
grammar/A12-variant theorem, availability-root relocation, scoped-to-raw cast,
or sibling selector body is claimed. The A and B caps are both binding/exhausted
budgets, NOT proof-unit exhaustion; no additional source unit is launched.

## Unit C: findings and evidence preparation

C1 synchronizes README/NOTES and the findings register, retaining the corrected
R192 memo P2 citation: CP3 CanonicalSchedule begins3240, canonicalBlock3256–3257,
inputPlacement3265. Source citation re-inspected; no new paper erratum promoted.
See O6-R194-FINDINGS.md for the exact resolved/open boundaries.

Source freeze408bd21e. Interim independent audit PASS authenticates all46
one-declaration source commits, all52 transcripts/snapshots/RSS samples, the
A8 comment-only successful recheck, strict A30/B16 caps and main serialization.
Ten noncompiler evidence-contract regressions PASS. The immutable final plan
has56 checks (49 positives/7 expected negatives), including all7 changed targets,
all5 protected spikes, seeded package, main fixtures and R8/R16 boundaries.
It deliberately omits lane-owned ActorLifecycleOnlyExtended/AvailabilityAware
source targets and the extension-dependent R192 probe; no lane2 certification
is inferred. Final results and evidence archive remain pending at this entry.

### C2 preflight owner clarification; C1 plan withdrawn UNRUN

The interpretation behind C1's exclusions was rejected by the supervisor.
Main-tree inherited baseline variant modules and their regression fixtures MUST
be included; only files absent from main may be excluded. The old56-slot plan
and SHA remain versioned, withdrawn BEFORE any final compiler, and all52
inherited targets are restored with their bytes authenticated to b81362d8.
Restored immutable plan:59 checks=52 positives/7 expected negatives, no exclusions.
See O6-R194-PREFLIGHT-RULING.md for the exact request/ruling and original plan.
The guard permits the two inherited variant modules only in planned validation
and only at their frozen baseline bytes. No ongoing lane2-created result is
certified. Source freeze408bd21e and binding A30/B16 caps are unchanged.
The corrected ten-test suite distinguishes inherited main baseline targets from
sibling-worktree paths; original preflight test logs remain historical evidence.
