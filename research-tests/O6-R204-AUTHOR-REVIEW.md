# R204 author review — checked PARTIAL, not independent human approval

Reviewed source freeze `3e6d8ff0`, D1 artifacts `56d1269b`. This is the author's
read-only source/claim review, separate from the required owner/reviewer gate.
All29 retained declarations and all3 new fixtures were read. Final validation
is running at this review checkpoint; final results belong in the audit.

## Result

No source-correctness blocker found within the precisely scoped support
statements. Two README precision issues were found and the parent approved
both documentation-only repairs for D2:

1. A8's history labels the actual **canonical/permuted physical words**, while
   returning original insertion attachments too. It is not merely an original
   chronology history and not a whole canonical synchronization.
2. The actual precise goal declaration is `o20CanonicalSynchronizationGoalModulo`.
   `o20SynchronizeCanonicalHistoriesModulo` is the still-absent desired producer,
   not an existing proved or hole-bearing function. The table now names the
   existing goal and describes the missing producer honestly.

## B review

- `CP5O20PresentVestigialSelectionSpike:everyPresentVestigialSelected` consumes
  the FULL accepted packet, obtains its native discarded-birth classification,
  and applies the real chain induction. No selected-list equality/coverage
  callback or desired membership is supplied by the caller.
- `CP5O20ChainCurrentDisappearanceSpike:o20UniqueGeneratedCurrent` compares
  actual insertion counts using the macro's existing unique-raw-insertion
  hypothesis. It does not equate dependent occurrences or weaken the action.
- The selected branch derives native withdrawal absence. The whole-chain
  theorem has genuine absent, selected-present, retained and closing-free
  cases, so non-head selections are covered. Its retained branch transports
  the birth and close through the real node's generation bijection; uniqueness
  is carried by the exported singleton actual derivation theorem.
- The only new `where 0 atObservation` is part of that structurally decreasing
  chain recursion, not a let alias: its retained branch recursively processes
  the smaller derivation. No caller-supplied induction hypothesis is added.
- Left/right canonical vestigial disappearance each uses its real native
  chronology and actual deletion/sorting. The right side does not assume a
  fabricated swapped correspondence.
- `CP5O20CurrentNameRebaseSpike` authenticates original controls and accepted
  current-name images for ANY canonical-present fiber, including unsupported
  present. Root/generated insertion-count laws are tied to the SAME actual
  replay. Current stamps are compared via unique source insertion positions,
  not by equating whole dependent birth packets.
- B14 is **present-class stamp/count/name support**, not full O20AllNameCut.
  Parent/provider control re-renaming and absent-domain transport under the
  conjugated internal/current name maps remain missing. D5 is unattempted;
  neither the exhausted generic consumer nor frozen deletionTheoremProof is
  called or edited.

## A review

- `CP5O20InsertOccurrenceHistorySpike` progresses from observed insertion
  plans to producer-owned native stage/action/tag equations, then binds them
  to the two supplied words' actual occurrences. Stage tag equations do not
  silently substitute for input occurrence tag equations: A2/A3 authenticate
  the latter explicitly.
- A6 is generic over the actual supplied words, A7 adds their computed native
  prefix scans, and A8 uses accepted canonical/permuted physical words plus
  original birth attachments. Support is explicit where needed. No prior
  ALL-name cut or whole-word coverage is inferred from these one-edge results.
  The action-derived prefix scan is not by itself a proof of ALL-name runtime
  coherence at that cut; the empty-origin/cut invariant is a separate obligation.
- `CP5O20ChronologicalOccurrenceHistorySpike` follows ordinary/deleted/retained
  native scan constructors. Prefix extension retains the exact original suffix
  openness and changes only its physical count coordinate. Both accepted
  original chronologies have actual retained-event birth producers.
- A12/A13 require an event match and two LOCAL child/parent raw-name equations.
  These are explicit conditional premises, not silently derived for removed
  or vestigial births from endpoint name matching. No global chronological
  zip/order/coverage is proved.
- A14 only joins already-produced histories at exact matching intermediate
  states and generation environments. It invents neither cuts nor skips.
- A15 is STOP3/3, fully reverted. No literal path-append equality is retained.
  The parent-approved probable opacity diagnosis and gated R205 candidate
  (two `export` → `public export` changes, +14 bytes, type/body unchanged)
  are recorded in the audit and `O6-R204-R205-VISIBILITY-CANDIDATE.json`.
  This is source-level diagnosis only, no new successful compiler proof.
  The current source stays unchanged; R204 exhaustion remains binding.
- Canonical-prefix activation-position transport, unconditional predecessor
  ALL-name cut, genuine unsupported/closing skip integration, whole R198
  folds and conditional/discharged synchronization all remain OPEN.

## Fixture review

- B8 traverses both actual eight-edge discarded-birth chronologies backwards;
  it does not supply the desired classification or fabricate a canonical chain.
- A9 builds a real paired Insert history within two eight-edge original traces;
  the stage and its labels are produced. Its literal environments are the
  concrete original prefixes, not a claimed universal initial-cut theorem.
- A16 includes six native edges with child Retire and Remove. It obtains the
  retained event's actual birth/open parent suffix and derives ordinal2 from
  the produced stamp. Current endpoint absence is not turned into epsilon.
- These fixtures use identity matching, not a nontrivial alpha-renaming case.
  They do not by themselves establish full-cut/D5/convergence constructibility.

## Evidence and scope checks

P1 preserves the complete statement and removes exactly the two body aliases,
using the owner-approved inline `the` annotations. P2 changes only the exact
measurement prose; all numeric and other JSON fields are identical. Both are
covered by adversarial compiler-free tests and guarded before/after receipts.
R203 A13's structural `rightHistory` is untouched.

Every retained source has a fresh own single Building and immediate guarded
receipt. A3-2 remains a genuine PASS superseded only by an authenticated
rstrip-only whitespace repair; it is not relabelled a failure. A15's three
failed snapshots are preserved, followed by exact full revert and separately
authorized V0-CACHE validation. No fourth proof attempt or cap extension.

No new holes/postulates, unsafe assertions, partial functions, let aliases,
with, stored frozen fields or scoped-to-raw casts were found. Existing four
macro holes remain byte-identical: **4 = 1/2/0/0/1**. Production and frozen
sources are unchanged. **C INELIGIBLE**; Thm73 and the full O20 macro are NOT
closed. Independent machine authentication is not independent human review.

No new paper inconsistency is established by this shift. The paper's Thm73
argument requires coherent whole canonical histories, not merely insertion
positions or lifecycle role-word equality; the explicit remaining obligations
are therefore substantive, not documentation TODOs that can be ignored.

## D2 validation confirmation

The detached final plan has now completed:190/190 expected outcomes, including
all9 changed Idris sources and the7 inherited expected-negative fixtures.
Final source hashes remain the reviewed source-freeze hashes.37 compiler-free
checks and final independent machine/frozen/resource verification PASS. This
does not change any partial/open proof boundary or supply human approval.
