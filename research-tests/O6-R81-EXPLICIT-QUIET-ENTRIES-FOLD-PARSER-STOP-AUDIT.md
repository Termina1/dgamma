# O6 revision 81: explicit quiet entries fold parser stop

## Scope

Grind shift #89 (overall #143) resumed from reviewer-accepted revision 80 at
`743a558`. The first authorized unit was a top-level private
`pointwiseQuietEntriesTrueExplicit`, with worlds, entry lists, uniqueness
proofs, endpoint relations, target well-formedness, source quietness, tracked
suffix membership, and its result all written against literal
`MkSystemState`/`MkCoeffectContext` shapes. It used no as-pattern aliases and no
`where` block.

The unit exhausted its independent fresh three-attempt budget and was removed.
No later unit was opened.

## Attempts

Each attempt cleared CP5 TTC/TTM and visibly rebuilt
`DGamma.CP5ConfluenceLocalDiamondSpike` directly from source.

1. The complete recursive producer was stated with explicit outer
   `name/key/world/error/value` binders and literal constructors throughout.
   Elaboration stopped while binding a dependent implicit `value` generated by
   an otherwise unannotated `MkCoeffectContext`:

   ```text
   Can't bind implicit ... {value:...} of type (... : key) -> Type
   ```

2. Every `MkCoeffectContext` received explicit `{key = name}` and
   `{value = FiberAt name key value world error}` arguments. Idris rejected the
   second named constructor argument at parse time:

   ```text
   Error: Expected ')'.
   ... {value = FiberAt name key value world error}
       ^
   ```

3. Each context instead received a full expected type via
   `the (Registry name key value world error) (MkCoeffectContext ...)`.
   The declaration parsed past the previous location, but the recursive clause
   again hit `Expected ')'` at a nested typed context used as the concrete
   `lookupFiber` registry argument.

Parser failures count under the permanent budget. The entire unit was removed
on exhaustion, and the accepted revision-80 source was freshly rebuilt to
confirm a safe boundary.

## Diagnosis

This stop is earlier than revision 80's semantic fold body. The tracked
membership algorithm was already validated there through target lookup, source
recovery, and `pointwiseQuietFiberTrue`; revision 81 is a parser/elaboration
surface failure caused by repeatedly typing the dependent context constructor
in isolation.

The next cure should type the **whole state**, using the project-established
shape:

```idris
(the (SystemState name key value world error)
  (MkSystemState concreteWorld
    (MkCoeffectContext concreteEntries concreteUnique)))
```

This exact whole-state `the` form is already checked throughout CP4. Premises
should use those fully typed whole-state expressions. Where a registry is
required, project `registry` from the typed whole state rather than applying
`the` directly to `MkCoeffectContext`. This preserves the authorized literal
constructor surface while avoiding a standalone dependent-context parser
boundary.

Give the statement and recursive producer a new independent unit budget only
if explicitly authorized; do not reuse revision 81's exhausted unit.

## Status

- fields 1–5: **closed and frozen**;
- revision-80 lifecycle/fiber quietness chain: **closed and frozen**;
- field 6 target-entry fold: **not retained**;
- field 6 wrapper: **not opened**;
- fields 7–15: **not opened**;
- result/body assembly: **not opened**;
- holes: **20**, split **6/4/8/1/1**;
- accepted remaining band: **held at 1–12 shifts**.

No frozen public surface, spike interface, production source, package, CP3,
hole, postulate, escape hatch, or detached premise changed.
