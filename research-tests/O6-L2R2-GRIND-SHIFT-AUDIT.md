# LANE 2 SHIFT L2R2 — checked research audit

## Scope, boundary and status

- Worktree only: `/Users/vyacheslavshebanov/Work/dgamma-lane2`.
- Branch: `cp5-thm73-lane-a8a10`; starting commit `986342303cce59c27c157867cfeb124d3147a9f0`.
- Last proof commit: **`141a1d802cea0a9ad3e492719d7fa0e050ad7311`** (C20).
- Runner/immutable-plan/design publication: **`8a1bf8cda58f6858940f74be7aea7b04f2a7daa9`**.
- Option A research continuation only. **B′ cap22 and C′ cap20 both reached.** No self-extension, new variant attachment grammar, production unfreeze, or whole-package rebuild.
- Main worktree was not entered, read, edited, built or committed. No main-lane compiler was signalled. No subagents were launched by this child worker.
- No changes to `src/`, `research/`, `dgamma.ipkg`, `README.md`, `NOTES.md`, `THM73-PLAN.md`, O20, the frozen canonical/cross-trace/renaming/local-diamond spikes, or frozen O19 surface. Two separately authorized predecessor **comment-only** repairs are listed below.

### Outcome matrix — intentionally not “all B′/C′ complete”

| Requested seam | Checked result | Remaining obligation |
|---|---|---|
| B′(i), generic own-child Retire squares | **Root OInsert / own-child ORetire** square from actual source/old checked edges, both alternate checked edges, world + ordered-binding equality. | Generic Begin, Iter, Finish and other-parent child-insert Retire square producers not supplied. |
| B′(ii), new-structure Remove squares | **LBegin / own-child ORemove** and **LAdvance / own-child ORemove**, including Iter and Finish tags. Explicit removed-binding absence; real checked alternates and exact snapshot. | Root-insert and other-parent child-insert Remove cases remain open. |
| B′(iii), whole-foreign-run relocation replay | Structural **native replay fold** plus early retirement and exact count/endpoint theorem. | Explicit LOCAL checked per-edge dispatcher is still an input; no complete dispatcher or unconditional whole-run normalization. |
| B′(iv), quantified extended zero gap | Generic conditional theorem from residual-head coverage + **NoRootOrchestration** in the actual gap. | Coverage/normalization not produced. No-root is stronger than no-forced-root. Candidate A12 demonstrates an actual gap1 countershape. |
| C′, old rejects / new admits | Old canonical placement refuted for every support state/order; actual root's **EarliestAvailableRootBirth** inhabited on explicit native trace. | Entire AvailabilityAwareCanonicalInputPlacement package, full registration history/reachability and canonical schedule not claimed. |
| C′, physically contextualized root move + exact-one measure | Approved snapshot square + real four-edge freeing prefix + two six-edge runs; full annotated inversion counts **3 → 2**. | Old literal-endpoint root square remains parked. This is not normalization to zero. |
| C′, availability-aware root phase preserving support | Scoped generic step consumes authenticated square, keeps arbitrary earlier context, creates native swapped trace, proves exact runtime and whole executable support-set equality and local exact-one decrease. | No arbitrary suffix, generic square producer, full support-linearization transport, invariant bundle or complete O17 normalizer. |
| A12 design gate | ATTACH/PREMISE/PLACEMENT audit with actual gap1 and exact O19 field comparison. Provisional ATTACH/PLACEMENT recommendation. | **Owner gate before any new attachment grammar.** No such grammar was defined. |

## 1. Generic retained proof capital

All new Idris files are in `research-tests/O6-L2R2-Sources/DGamma/`.

### Checked observation bridge (B1–B5, B17)

`L2R2RetireInsert.rootInsertAtAbsence` proves the raw native insertion observation from absence and declaration availability. It concludes an actual `observeActionResult (applyAction ...)` result, not equality of independently reconstructed uniqueness certificates.

`L2R2CheckedSnapshot.CheckedSnapshotStep` retains a runtime successor and erased checked-action/snapshot equations. `checkedSnapshotObserved` turns an observed raw evaluator result into a **real checked successor** using source well-formedness and `checkedFromRaw`; `checkedRootInsert` combines it with the native root insertion observation. `checkedAcrossSnapshot` replays an existing checked action at a well-formed snapshot-equal source through native observation coherence.

These are real auxiliary lemmas, not themselves commutation squares. Explicit quantities are used: specifications/equations and proof functions at quantity0, state/snapshot/trace data retained in record interfaces.

### Root-insert / own-child Retire square (B6–B10)

`ChildRetireSnapshotExchange` includes actual source lookup and `ChildOf parent` evidence, actor distinction, the actual retirement action, both alternate checked edges, and exact endpoint runtime equality.

`commuteRootInsertChildRetire` (`3fabc497`) takes the **original** root-insert/Retire edges, source well-formedness, actual child fiber/parent and actor distinction. It constructs retirement first and root insertion second. Alternate applicability and endpoint equality are **proved**, not inputs. The world and ordered binding list agree; equality of erased `UniqueKeys` proofs is not claimed.

Other requested Retire action kinds remain open. Actor-name inequality alone must not be presented as a completed all-action commutation theorem. The future dispatcher must discharge actual action/source conditions; this audit makes no semantic claim that its fully general callback is automatically inhabitable.

### New Remove package and native lifecycle commuters (B11–B16)

`ChildRemoveSnapshotExchange` adds a producer-owned Boolean absence observation at the early deletion target, actual source-child evidence, both real checked successors, and exact snapshot equality. This is **not** the exhausted L2R1 `childRemoveAtFound` statement.

`deletedBindingAbsent` derives the observed absence from native lookup plus `deletedKeyNotElem`; a failed attempt to use private CP3 `lookupDeleteSelf` was rejected and replaced, not bypassed.

`commuteBeginChildRemove` and `commuteAdvanceChildRemove` (`1ede1b79` for the latter) execute the existing CP4 Begin/Advance deletion commuters. Their source premises require an actually present **retired Inactive own-child leaf**, childlessness, actor distinction, well-formedness, and original checked lifecycle/Remove edges. The LAdvance theorem covers its native source-domain cases, including Iter and empty/effectful Finish. Intermediate helpers consume CP4 replay packages, but the two public generic producers **construct** those packages by calling the checked native commuters; they do not accept an arbitrary alternate square as an oracle.

### Whole-run replay, honestly conditional (B18–B20)

`L2R2ForeignReplay.RetirementReplay` holds a native relocated trace, transition count `S (transitionCount original)`, and the exact runtime endpoint of the original run followed by retirement.

`foreignRetireReplayCPS` is structural induction on `ForeignChildRun`. Each recursive step obtains a real checked action from the displayed **single-edge** callback, frames the original child lookup, propagates source/target well-formedness and snapshot equality, and constructs the native transition spine. `replayRetirementBeforeForeignRun` (`6acd8069`) adds the actually checked early retirement from L2R1 B23.

This is more than early applicability, but **not a complete normalization theorem**. The local callback (all foreign action kinds, at an arbitrary well-formed snapshot-equal retired source) remains an explicit premise. It is not silently replaced by a supplied whole-suffix trace, yet its generic implementation remains substantive work.

### Conditional physical zero gap (B21–B22)

`RemainingGapHeadIsRoot` expresses a residual coverage obligation. `extendedZeroGapWithoutRoot` (`33ccc4e3`) is quantified over actual ordered extended blocks and a supplied gap linked by count transport. When the gap is nonempty its root-head witness contradicts `NoRootOrchestration`; the empty branch transports zero count.

Use the actual `extendedBetweenBlocks ordered` and `Refl` for the link. No equality-to-zero or coincident-cut premise is assumed, but **the coverage and no-root premises are not produced**. This is not a universal zero-gap claim and not yet the weaker no-forced-root formulation requested for eventual normalization.

## 2. Explicit native A12 and root-phase evidence

The detailed witness and three-design comparison are in `O6-L2R2-A12-DESIGN-GATE.md`.

- `smallTrace`: seven actual checked edges, literal endpoint `smallState 7`.
- `smallRootEarliest`: actual ordinal4 birth, current compatibility, **every** earlier Nat/LT cut excluded, including retirement-only cut3.
- `smallStrictPlacementRejected`: old canonical placement uninhabited for **every support state/order**; actual ordinal4 cannot precede actual ordinal0.
- `smallExtendedBlocks`: parent0 range **[0,4)** includes Finish0/own-child Retire1/Remove1; following actor2 range **[5,7)**; actual located Root Insert3 in the physical gap, **count1**. The child actions are already relocated into their parent's body.
- `smallRootSnapshotSquare`: both real Begin2/Insert3 routes after the freeing Remove; source/destination cut compatibility; state9 versus state6 endpoint **snapshot**, not literal-state, equality.
- `supportSetAcrossSnapshot`: equality of the complete executable support list from exact ordered runtime bindings.
- `rootPhaseFromSnapshot`: genuine prefix-context splice from an explicit authenticated square, with checked output, runtime/support preservation and exact-one local measure.
- `smallRootPhaseEvidence`: real four-edge freeing context, complete six-edge old/new runs and actual-state annotations; full inversion counts **3→2**, exact-one equality.

### What these fixtures do NOT establish

They do not supply the entire availability canonical-placement package, a full support-covering block decomposition, quiescent final schedule, initial empty-registry reachability, registration-protocol history, or the full canonical invariant bundle. Root3 has no lifecycle block in the seven-edge trace. Thus **CANDIDATE A12** is a checked native structural countershape, not a claimed formal refutation of the paper under every metatheorem premise.

The paper text is absent in this lane worktree. The design comparison identifies paper intent through the existing mechanization and explicitly leaves exact-paper-language re-audit to the owner/reviewer; no main-worktree file was read to fill that gap.

## 3. C4 ruling and representation-proof identity

C4-1 failed literal equality of independently reconstructed native registries because erased uniqueness witnesses differed. The owner authorized a **C4-only two-file bundle**:

1. body/data-expression changes to existing `smallState`, with no new state declaration or recursive checked-evaluator builder;
2. exactly one new top-level C4 producer;
3. both target files snapshotted, hash-authenticated, mutation-monitored, freshly rebuilt and committed together.

C4-2 aligned physical states using insert/replace/delete operations from one origin. The seven physical edges then matched, but alternate root insertion still had different erased uniqueness evidence depending on whether the head actor2 was **Inactive** or **Reloading**.

The owner authorized C4-3's separate alternate endpoint and snapshot equality, including the corresponding execution-package adjustment, and explicitly parked the exact old C13 fixture. C4-3 passed on its **third and last allowed attempt**; receipt `4e6311ed` owns both source hashes and both Building lines. No extra attempt was granted or taken.

The owner separately authorized the snapshot-level research root phase. The exact signature difference is recorded in the A12 memo. This is not a conversion to the frozen exact square and not an excuse to assume proof irrelevance.

## 4. Micro-unit and stop accounting

The complete 42-entry name/commit table and all copied-statement origins are in `O6-L2R2-DECLARATION-ORIGINS.md`. Every micro-unit has a retained fresh PASS and an immediate guarded **source** commit with no intervening compiler invocation.

- **B1–B22:** 22 retained declarations, no B23 attempt.
- **C1–C20:** 20 retained declarations, no C21 attempt.
- **55 development compiler invocations:** 42 PASS, 13 rejected attempts.
- No unit failed all three attempts. B1 and C4 passed on attempt3; no exhausted seam was reopened.
- Forbidden inherited seams stayed parked: R192 eleven-edge nested producer, L2R1 B24 exact `childRemoveAtFound`, nested/transparent R174 exact occurrence and old-strict rejection, original exact root fixture.
- Caps were reached well before the wall-clock cutoff; remaining work was compiler-free audit/evidence, final checks and the required design/review gate. No idle budget was converted into unauthorized new declarations.

### All rejected compiler attempts (raw transcripts retained)

| Attempt | Diagnostic / resolution |
|---|---|
| B1-1 | Missing `Data.List.Elem` import; imported the native type. |
| B1-2 | Supplied absence witness versus evaluator `Refl` uniqueness identity; changed within unit to actual runtime observation. |
| B4-1 | Implicit dependent value family unbound; fully instantiated source well-formedness arguments. |
| B7-1 | Dependent lookup family implicits unbound; explicit full instantiation. |
| B8-1 | Source state implicit unbound in square type; explicit native indices. |
| B9-1 | `replaceEntries` versus projected `replaceBinding` view mismatch; explicit single-constructor registry elimination. |
| B12-1 | `CP3.lookupDeleteSelf` is private; proved observed absence with public lookup/uniqueness lemmas. |
| B13-1 | Action-owner versus transition-owner projection mismatch; explicit action/source typing. |
| B19-1 | Structural replay LHS dependent index mismatch; corrected the single-constructor induction view. |
| B22-1 | Projected dependent block-gap endpoints could not unify; explicit supplied actual gap and count transport. |
| C4-1 | Literal native-state erased uniqueness witnesses differed; owner-gated data bundle. |
| C4-2 | Alternate insertion's Inactive2 versus Reloading2 uniqueness proof identity; owner-gated separate endpoint + snapshot. |
| C19-1 | Wrong occurrence accessor (`actionBefore`); corrected to `beforeActionOccurrence`, and instantiated AvailabilityTrace's explicit type parameters. |

These are **compiler failures**, not silently retained theorem claims. There were no interrupted attempts, source-mutation incidents, preflight compiler launches after rejection, or failed-run commits.

## 5. Resource / process / fresh-build discipline

- Idris 2 **0.8.0**; `%default total` in every new Idris module.
- Seeded build retained. No deletion, mass touch, cold rebuild or package check.
- Detached Python `-I` wrappers; one owned Idris compiler at a time. Source/companion hashes are taken before launch; target-only mtime touches are recorded with old/new nanosecond values. A target's **own Building line** is mandatory for PASS.
- Source-mutation monitoring inherited from the repaired L2R1 monitor; no mutation was observed. Exact source hash is rechecked by the commit guard. Polling is not claimed to be continuous filesystem surveillance.
- RSS sampled every **250 ms**, not a continuous peak. Maximum recorded own RSS: **821,456 KiB (~0.783 GiB)**, C19-2. No heavy launch/lock acquisition was needed. Unlocked interruption threshold is 18 GiB; heavy mode reserves the shared lock before launch and has a 48 GiB interruption threshold.
- **11 separate main-lane compiler PIDs** appeared in snapshots, always classified `main-lane compiler (separate worktree)`. None was owned or signalled.
- Across development + final checks: **67 compiler invocations**, **54 PASS / 13 FAIL**, **0 interrupted**, **0 source mutations**, **0 heavy locks**. Recorded compiler wall time totals ~91.65 seconds; the rest of shift work was proof construction, diagnosis, gated representation design and evidence/review preparation, not unrecorded compilers.
- No new `believe_me`, `assert_total`, postulates, holes, `assert_smaller`, partial definitions, `with`, or let aliases. Existing imported baseline code is reused, not silently represented as newly re-proved.

## 6. Authorized predecessor documentation repairs

Committed through the exact-byte artifact guard in `8a1bf8cd`, not an unguarded source commit:

1. `research-tests/run-l2r1-check.py` module docstring now truthfully says it never changes source **contents** and does the approved target-only mtime touch. Python executable AST is unchanged.
2. `research-tests/DGamma/L2R1RootHoist.idr` says **candidate admissible-root states (trace/square NOT proved in L2R1)**. Idris non-comment code is byte-identical after comment stripping.

No predecessor Idris declaration was changed. The guard authenticates both exact replacements against the shift base and records before/after hashes. The independent verifier checks the semantic-code boundary separately.

## 7. Final validation and reproducible evidence

The immutable plan `O6-L2R2-FINAL-VALIDATION-PLAN.json` was committed **before** launching V1, with exact hashes and leaf-before-dependent source dependencies. It covers **all 12 current new modules**. It was not rewritten after checking.

**V1–V12 all freshly PASS**, completed `2026-09-08T23:50:00.665357Z`; each has its own Building line, exit0, unchanged source hash and no mutation. This is deliberately not a whole-package build; that was forbidden in this lane.

Compiler-free runner regression tests: **7/7 PASS**, using the actual helper ASTs and fake process output to check declaration counting, body-only companion classification, genuine target freshness, dependency/wrong-worktree rejection, and never owning the main compiler or Python wrapper. No compiler/commit guard is invoked by those tests.

Evidence artifacts:
- `O6-L2R2-COMPILER-EVIDENCE.tar.gz`: raw per-invocation JSON, source bytes, compiler and wrapper logs, C4 companion snapshots, plans, receipts and metadata.
- `O6-L2R2-COMPILER-LEDGER.json`: normalized invocation index, hashes, matching source commits, outcomes, monitors, bundle metadata and archive hash.
- `O6-L2R2-DECLARATION-ORIGINS.md`: every retained declaration and exact inherited/full-statement-copy origins; no quietly shrunken canonical copy.
- `O6-L2R2-A12-DESIGN-GATE.md`: candidate A12 evidence, design comparison, approved snapshot signature delta and precise open premises.
- `O6-L2R2-INDEPENDENT-VERIFICATION.json`: published compiler-free acceptance evidence when verification succeeds. This mechanical verifier is **not** a substitute for the required independent human proof review.

Reproduction commands (lane2 cwd):

```text
python3 -I research-tests/run-l2r2-evidence-tests.py
python3 -I research-tests/run-l2r2-independent-verify.py
git diff --check
git status --short
```

The final compiler plan is append-only historical evidence; do not rerun its invocation IDs. An independently requested recheck needs new IDs/authority and the seeded-build/lock rules, not a cold build.

### Evidence publication boundary

All source-commit receipts are inside the archive. The archive's own publication receipt and later report-only receipt cannot recursively be inside it; the separate verifier authenticates **every shift commit**, including those artifact commits, against the live append-only receipt ledger. A published verification report names the HEAD it actually checked; publishing that report is a subsequent artifact-only commit, not a retroactive claim about its own hash.

## Status

**Fully checked within their stated scope:** native checked-observation bridges; generic root-insert/own-child Retire square; generic Begin and Advance/own-child Remove squares; executable support-set coherence; actual small native trace and old-strict rejection; actual earliest-root clause; actual extended blocks with gap1; approved native snapshot root square; actual six-edge physical inversion decrease 3→2.

**Proved conditionally / partial:** whole-foreign-run replay (local dispatcher input); trace-quantified zero gap (residual coverage + no-root inputs); generic prefix-context snapshot root phase (authenticated square input, no suffix, local measure).

**Still only inherited statements / open:** full availability canonical placement, complete canonical/closing-free producers, complete generic Retire/Remove dispatchers, full root-phase/suffix normalization, support-linearization/invariant transport, unconditional adjacency, attached-root grammar and O19 integration. No full confluence theorem is claimed by this shift.

**Next:** independent proof review, owner A12 grammar/availability-dependency gate, then complete the missing native action-square dispatcher and real coverage/normalization producers under the chosen interface. Preserve the honest conditional statements until those premises are discharged.
