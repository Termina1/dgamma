# R182 grind-shift audit

## Authenticated start / guard

2026-09-07 **17:07 UTC**, HEAD **159ef07**, branch `cp5-thm73-scoping`.
Tracked tree clean; only permitted `paper/` and the frozen untracked adversarial
review. Idris **0.8.0**, CLI `--source-dir` and `--check` authenticated; no orphan.
No new attempt after **20:27**, safe final gate by **20:52**, timeout **21:07**.
Research only; seeded serialized Python `-I` checks, unchanged 48GiB guard.
One new declaration OR one signature surface per check; fresh3/3 per micro-unit;
commit ONLY after machine-reading the fresh PASS bit. No build/seed deletion,
legacy R11, production edit, new LocalDiamond declaration, `with`, local alias,
unsafe proof, postulate, partiality, scoped/raw cast, frozen deletion call or
O17 root/body work. All other protected hole declarations remain byte-frozen.

### R181 independent review / chronology erratum (parent steering)

Independent read-only review returned **ACCEPT-WITH-NOTES**: all50 retained
declarations reconcile (PASS before commit, exact target files), stop hygiene
and frozen boundaries clean, no mathematical overclaim. The dfc933e violation
was handled and remains unexcused. P2 chronology erratum: R181's heading
“Unit F —8/8 complete at52ea260,16:02UTC” conflates F8 compiler completion
**16:01:29UTC** with commit52ea260 at **16:06:38UTC**. The ratified R181 audit
is not rewritten.

## Unit A design BEFORE code — owner-authorized O19 exception

Owner16:50UTC accepts the R181 count7 failure mechanism without the missing
TraceIndependent input. This is a correction of OUR weaker block predicate,
not a paper or Cordis defect. O19 BODY remains a hole pending the post-A gate.

**Chosen R146(iii) formulation: both-direction first-step applicability at the
same pre-left cut.** The existing left located block already owns an exact
`BeginStep nameEq keyEq left (blockPreStart leftBlock) (blockStart leftBlock)`.
Add ONE erased field `safetyRightOpeningEarly`, an existing
`CheckedEarlyApplication ... (blockPreStart leftBlock) (LBegin right) LBeginTag`.
Its actual target is stored in the existing certificate and its checked equation
is erased. Thus either actor can open FIRST at the common cut. This does NOT
assert whole swapped-block execution, post-swap installation, a diamond or
all later crossing applicability. Those remain O19 producer obligations.
Do not add a duplicate left certificate: derive it from `blockOpening`.

Why this, rather than an unproduced endpoint support-closure oracle: R179 G
already executes `checkedApplyAction` at a selected actual cut and authenticates
its exact action/tag via `checkedEarlyApplicationObserved`. The future O20
selector can use that positive guard for this precise field. R177 rank/orientation
capital locates/orients a candidate, not unconditional applicability. R181 D
provides actual successful resolved-view transport, not a theorem that arbitrary
rank-descent candidates pass this guard. Completeness of finite independent
pair selection remains open and is NOT inferred from `Nothing`. No O20 premise
or body is revised. The new field rules out the certified provider/consumer
shape directly using the existing actual raw `LBegin1=Nothing` at that cut,
without assembling or assuming TraceIndependent to obtain the contradiction.

### Exact old-to-new clause map / consumers

| Old clause | Revised clause | Producer/consumer obligation |
|---|---|---|
| `safetyLeftInOrder` | unchanged | actual selected left block |
| `safetyRightInOrder` | unchanged | actual selected right block |
| `safetyLeftBeforeRight` | unchanged | current source-order occurrence |
| `safetyBlocksOrdered` | unchanged | both exact whole-trace located blocks |
| `safetyLeftDoesNotGenerateRight` | unchanged | licensing exclusion |
| `safetyRightDoesNotGenerateLeft` | unchanged | reverse licensing exclusion |
| missing right-first check | NEW erased `safetyRightOpeningEarly` | actual checked LBegin right at exact pre-left cut, exact LBeginTag |
| left-first check | already `blockOpening` | no additional caller oracle |
| O19 `safety` binder | rename `applicableSafety`, same strengthened record | signature-only, RHS hole unchanged |
| O19 `0 sourceUnique` | unchanged | original raw insertion uniqueness remains |

There are NO retained concrete `MkAdjacentActorSwapSafety` clients. The future
O20 selector is its sole missing producer. R6SafetyDetachmentNegative must still
reject another trace; R8ZeroDerivationOperationalStepNegative must still reject
zero derivation at the whole-block boundary. R7OperationalThreadingPositive and
R9 range fixtures merely quantify over exact safety, so need no constructor
migration. Other four hole declarations/adjacentSwapSuffix remain byte-frozen.

Negative fixture plan: reuse R181 authoritative blocks/decomposition and exact
root-source rejection; universally refute revised safety for ANY bundle at the
same source, so the missing independence field is irrelevant, not postulated.
Positive fixture plan: two actual empty-dependency/empty-provision components,
authenticated empty-origin six-edge contiguous trace, actual bundle/decomposition
using R172/R174/R181 constructor methodology, and a full revised safety inhabitant.
Empty provisions are genuinely disjoint; this fixture must not be presented as
proof of a nonempty-provision generic theorem.

## Micro-unit ledger

Pending. Exact source snapshots, diagnostics, memory/interval metadata and
commit correlations are persisted by the R182 harness under `/tmp/dgamma-r182`
and archived in this directory before each substantive gate.

## Status

A design only. No body permission inferred. Unit B waits for the explicit
post-A reply; C is pre-authorized capital while that reply is late; D no O20 body.
Census starts **5=1/3/0/0/1**, unchanged.
