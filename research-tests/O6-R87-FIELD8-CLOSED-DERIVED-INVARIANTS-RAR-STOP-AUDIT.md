# O6 revision 87: field 8 closed; derived endpoint invariants; whole-RAR stop

## Scope

Grind shift #95 (overall #149) resumed at reviewer-accepted revision 86
`5e43dd4`. It followed the authorized per-action backward decomposition, closed
private package field 8, and proved standalone foundations for fields 10–15.
The shift stopped before attempting field 9 because strict record order exposes
a missing producer-owned whole-trace relational replay correspondence. No
assembly or body hole was opened.

## Retained backward chain

Each declaration had its own fresh three-attempt budget and every retained
commit passed a visible fresh rebuild of
`DGamma.CP5ConfluenceLocalDiamondSpike` plus the R16 theorem-assembly positive.

- `paperActivationActiveFiberProvidesBackward` (`31a8ae3`) passed attempt 2.
  Attempt 1 only corrected the exact indexed `Fired {before} {afterState}` type
  and explicit `decEq @{nameEq}` use. The retained owner branch contradicts
  `activationSourceOwnerNotActive`; the foreign branch delegates to frozen
  `foreignActiveFiberProvidesBackward`.
- `paperInsertActiveFiberProvidesBackward` (`5aba38e`) passed attempt 1. The
  source lookup-absence equality is an explicit ordinary argument in the
  signature; no generated dependent implicit is used.
- `paperRetireActiveFiberProvidesBackward` (`06d50c8`) passed attempt 1. The
  owner branch delegates to frozen `retireOwnerActiveProvidesBackward`; the
  foreign branch uses the checked frame.
- `paperRemoveActiveFiberProvidesBackward` (`9fb6391`) passed attempt 2. Its
  signature explicitly contains both the checked equation and raw
  `applyAction ... = Just ...` equation. Attempt 1 exposed only a dependent
  `oldFiber` correlation layout; moving `sameFiber` inside the exact `MkFiber`
  elimination closed it. The owner branch uses `removeSuccessView` and
  `inactiveLifecycleFromRemovalGuard` to contradict activity.
- `paperStepActiveFibersBackward` (`764990a`) passed attempt 1. It is now a
  dispatcher only. Insert absence and remove raw equations are explicitly typed
  erased locals before calling their per-action consumers.
- `alignedPaperStepActiveFibersBackward` (`32cef27`) passed attempt 1. One
  `AlignedStep` elimination reindexes the abstract transition to the caller's
  exact dictionaries/action/tag/checked equation before delegating.

The revision-86 combined-dispatcher annotation wall is therefore fully closed.

## Field 8 closure

- `adjacentSwappedFinalActiveFibersProvideAll` (`40b4d29`) passed attempt 1. It
  factors the authenticated empty-to-source-pair active-provision proof and the
  exact diamond endpoint transport out of moved-left.
- `adjacentMovedLeftComponentTotal` was reduced to that projection in `a91f9cf`
  (attempt 1).
- `adjacentMovedRightComponentTotal` (`dc89c0d`) passed attempt 1. It classifies
  moved-left from frozen revision-21 `registrationSwapSafety`, runs the aligned
  dispatcher backward from `swappedFinal` to `swappedMiddle`, and applies
  `transitionComponentTotalFromActiveFibers` to moved-right. No detached
  intermediate endpoint is accepted.
- `adjacentTargetTraceComponentsTotal` (`bfc2f69`) passed attempt 1. It combines
  the unchanged prefix, moved-right, moved-left, and exact sealed-suffix replay
  totality with `appendTraceComponentsTotal`.
- `AdjacentAlignedPointwiseReplay.alignedReplayTotal` and its producer
  population (`6bb2782`) passed attempt 1. The producer splits the original
  `replayTotal` at the exact frozen adjacent decomposition and consumes the
  same prefix/pair alignment and sealed suffix already owned by the envelope.

Private package fields 1–8 are now closed.

## Fields 10–15 foundations

Strict package order is field 9 independence, then fields 10–15. The following
standalone constructive foundations were retained without changing package
field order:

- `replayProvenanceFromDiscipline` (`fef506c`), attempt 2; attempt 1 discovered
  the required direct research-only import of `DGamma.CP4SupportSolution`.
- `replayProtocolRankedFromEmpty` (`a7160e9`), attempt 1.
- `replayParentRanksIncreaseFromEmpty` (`85c120a`), attempt 1.
- `replayPrecedencePathIsSupportPath` and
  `replayPrecedenceAcyclicFromRanks` (`fb057bc`), attempt 2; attempt 1 discovered
  that the analogous deletion-local path converter is private, so the retained
  proof structurally converts `PrecedencePath` to `SupportPath` locally.
- `replaySupportWellFoundedFromRanks` (`c8193d5`), attempt 1.
- `supportMatchesActiveFromReplayFields` (`c5d5d44`), attempt 3; attempts 1–2
  were parser failures caused by naming an argument `total`, a reserved word.
  Renaming it `totality` closed the exact Lemma-70 application. The
  research-only module now directly imports `DGamma.CP4Lemma70`.

These lemmas derive fields 10–15 entirely from already owned target alignment,
discipline, initial empty/well-formed evidence, quietness, failure freedom, and
totality. No source endpoint invariant is transported opaquely.

## Field 9 stop: missing whole-trace RAR

`replayIndependent` is constructively available from checked
`traceIndependentAfterRelationalReplaySpike`, but that theorem requires:

```text
RelationalReplayCorrespondence original targetTrace
```

The current producer owns:

- exact unchanged prefix alignment;
- the frozen diamond-owned moved pair;
- `SealedSuffixReplaySpine suffix replayedSuffix` and therefore
  `sealedSuffixRelationalReplayCorrespondence suffix replayedSuffix`;
- the original global `replayIndependent`;
- exact target endpoint relations and occurrence/ordinal foundations.

It does **not yet own a whole original-to-target RAR**. In particular:

- suffix RAR cannot be attached to the bare prefix or moved pair;
- `LocalRelationalDiamond` intentionally retains endpoint effects/controls and
  moved action/tag classifications, but no detached moved-pair generator map or
  iterator-stage correspondence field;
- `ActionRegistrationReplayCorrespondence` alone is insufficient for field 9,
  because Definition 60 also needs relational generator maps and exact iterator
  outcomes;
- accepting a caller-supplied RAR or target independence would violate the
  frozen scoping rules.

This is the next semantic implementation unit, not evidence of a false theorem.
The checked local-diamond construction already contains the actor-local map and
iterator comparison machinery, but O6 must package it producer-correlatively
across the unchanged prefix, reversed pair, and sealed suffix. The safe next
campaign is:

1. construct identity prefix RAR;
2. derive a moved-pair RAR from the exact aligned source pair, diamond-owned
   moved pair, revision-21 classification, source `TraceIndependent`, and the
   existing activation/orchestration map + iterator comparison producers;
3. compose prefix, pair, and sealed-suffix RAR structurally;
4. populate field 9 with `traceIndependentAfterRelationalReplaySpike`;
5. populate fields 10–15 using the retained foundations;
6. only with a clear budget assemble the operational occurrence fold,
   `AdjacentSwapResult`, and `adjacentSwapSuffixSpike_rhs`.

The moved-pair RAR must not be replaced by endpoint-only reasoning, independently
stored `DecEq` equality, a bare action occurrence mapping, or output-shaped
caller capital.

## Status

- fields 1–8: **closed and producer-populated**;
- field 9 `replayIndependent`: **open at producer-owned whole RAR**;
- fields 10–15: **constructive foundations closed; package population waits for
  strict field-9 order**;
- `AdjacentSwapOperationalOccurrenceFold`: **unassembled**;
- `AdjacentSwapResult`: **unassembled**;
- `adjacentSwapSuffixSpike_rhs`: **unopened**;
- holes: **20**, split **6/4/8/1/1**.

The accepted remainder band remains **1–10 shifts**. The mandated holes
`20 -> 19` adversarial-review boundary was not reached.

## Isolation

The 1183-byte frozen adjacent interface, revisions 19–21, all frozen field 1–7
and revision-86 capital, production `src/`, `dgamma.ipkg`, and CP3 are unchanged.
No hole, postulate, escape hatch, detached premise, public frozen-surface
revision, or target-shaped caller evidence was introduced.
