# R198 owner-approved unprotected research-goal restatement

The old lockstep goal/fold remain byte-unchanged. They are now a
**superseded candidate**: `o20SynchronizationForwardOrdinalFixed` (A8,
3e4f2f9b) proves that every final-left live stamp must have its birth ordinal
fixed by generationForward. A11 attaches this necessity to any authentic scan.
This is a proved necessary condition; it is NOT a compiled accepted-pair
counterexample or a proof that the repeated-tag fixture inhabits all capital.
The owner's decision to repair the research specification is recorded below.

## A3 stop and alternative-construction ruling (verbatim)

> RATIFIED: A3 3/3 stop, full revert to 48019e93; the failure is the known representation seam (independently reconstructed states carry different erased uniqueness witnesses — Inactive vs Reloading parent entries), not a rejected input or a gap; the modulo-goal ruling stays CONDITIONAL and inactive. Continue the non-exhausted A units: structural necessities of the stamped family (equal counters / final birth-ordinal compatibility) — and ONE more shot at the repeated-tag fixture is permitted ONLY as a NEW construction that derives BOTH words from ONE native origin by applying the checked squares/exchanges (the L2R1 relocation pattern: never author the second word's states), since the question matters for the goal's shape; if you cannot build it that way in ≤3 attempts, keep it parked. Then B: exact endpoint-controls analysis per name class (supported / present-unsupported / removed) with the missing-lemma list. No restatement/cure without a confirmed fixture.

That optional shot was NOT launched. The public full O19 insertion/activation
square requires the lifecycle actor to differ from the insertion parent,
which fails for this own-parent Iter/Insert. The early-Iter producer does not
have that limitation, but does not alone produce the full exchange. No private
frozen helper was exposed, copied, or called; no fourth A3 attempt occurred.

## Activation ruling (supersedes the earlier fixture condition)

> SUPERVISOR RULING on A4–A8: the necessity theorem is decisive enough on its own — the OLD lockstep goal forces `generationForward mapping` to be BIRTH-ORDINAL-PRESERVING on every final-left live stamp, while the accepted GeneratedOrchestrationMatched (E9) relates generated births by generation content and does NOT require ordinal preservation (the repeated-tag shuffle is exactly a legal ordinal shift 2↔3). So, independent of the fixture (which fails only on representation witnesses), the lockstep goal demands a property the accepted inputs do not supply. ACTIVATE the conditional ruling as a principled RESEARCH-SURFACE restatement, not a fiat: (1) define `o20CanonicalSynchronizationGoalModulo` (new type): synchronization MODULO the conjugated generation map — corresponding births related by the map with physical ordinals allowed to differ within the same actor block, stutters justified by the map; keep the old goal as "superseded candidate (forces ordinal preservation — necessity theorem A8)"; (2) show the modulo goal is SUFFICIENT downstream: either re-type the stamped fold's cut modulo the map (new fold, old kept) or prove a bridge lemma that the modulo endpoint cut composed with the endpoint current bijection yields the O20HistoryCut/O20AllNameCut shape D5 needs (the endpoint current bijection already composes with the generation map — R194 non-vestigial agreement); (3) only then attempt the producer against the modulo goal in the remaining A slots. Docstrings must state the necessity theorem as the reason. Do not spend attempts on the own-parent Iter/child square (no public producer; correctly recorded).

The quoted legal-shuffle assessment is the owner's mathematical ruling, NOT a
native fixture PASS. E9 formally governs generated Retire/Remove; E8 governs
surviving births. Neither directly requires equality of physical birth ordinals.
Their logical interaction with FULL capital has not been refuted by a compiled
countermodel here. No claim of protected convergence/paper failure is inferred.

## Exact new design gate (verbatim approval)

> APPROVED — the occurrence-labelled actual-path specification is the right non-fiat repair: O20OccurrenceStampedHistory keeps the actual O20StampedStage native transitions but labels each stage by its TWO actual LocatedActionOccurrence positions in the supplied left/right words (same action/tag), with no lockstep physical-counter successor restriction; the NEW structural fold reuses o20StampedStageCut and takes ONLY the initial cut (no successor cut/callback oracle); O20HistorySynchronizationModulo pins both real final scans plus this occurrence-attached path; o20CanonicalSynchronizationGoalModulo specializes the exact conjugated actual maps. Conditions: (1) the docstrings state the honest limit verbatim — "whole-word / per-actor ordering and coverage producer remains to prove; arbitrary skips are NOT asserted to be zero native edges"; (2) the old lockstep goal/fold stay untouched, labelled superseded-candidate with the necessity theorem as the reason; (3) fixtures: the R191 11-edge pair under the new fold, and the R193/R195 pair. B debt stands exactly as you state: the ALL-name rebase cannot follow R194 non-vestigial agreement alone — present-vestigial / current-image absence is the explicit remaining lemma; no D5 controls by fiat.

No production/frozen statement/body is authorized to change. In particular,
the new modulo history still requires an actual path, occurrence attachment
and genuine scans; the downstream fold does not supply the missing path or
ALL-name endpoint rebase under expectedBridgeBijection. This ruling does not
relax A26/B16/C3/D4 caps or three-attempt/exhausted-seam rules.
