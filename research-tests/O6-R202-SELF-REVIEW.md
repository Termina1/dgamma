# R202 self-review — checked PARTIAL, not independent acceptance

Proof source freeze: `2a28e44e058ecf30a587c9151fc5de0bf52254c6` (30 guarded declarations, five new sources). This document is a lead-engineer source review. Separate machine authentication verifies source/log/receipt/plan bytes; neither is an independent human proof review. The required reviewer/owner gate remains parent-owned.

## B: genuinely joined locally, NOT globally

1. `o20DeletedBirthClosingIndex` obtains the **original classification's own** closing occurrence in its own birth suffix, rebases by actual preceding trace count, and returns strict original order. It does not invent a closing ordinal.
2. B7/B8 return both a retained closing action and its exact `generationSubsequenceSourceOrdinal` equation. Registered filters exclude registered Unloads; foreign-center filtering explicitly excludes the selected raw parent.
3. `o20SubsequenceReflectsSourceOrder` is structural over Keep/Delete, including impossible empty/zero cases. Its exact source-index equations are essential when actions repeat. No equal-action-to-equal-occurrence cast occurs.
4. B11/B12 use **one shared segment/subsequence** for birth and closing witnesses, reflect their source order, and remove the retained birth's actual preceding trace and edge. The output close is in **that same retained birth's own suffix**. A retained birth and its exact source-index equation are premises; target closing action/order are outputs. These lemmas do not supply the whole three-segment deletion-step glue.
5. B13 produces the retained birth from `deletionRegistrationAccounting` with the original parent/component in its dependent type and the actual producer's forward stamp. It does not also produce the B11/B12 native subsequence-origin equation or segment assignment.
6. B2 uses `selectedRegisteredDuring` completeness to contradict nonselection **if** the classification's exact birth is located in the selected center with the exact stamp equation. It does not derive those inputs from a removed-center closing occurrence.
7. B14 uses the actual R193 eight-edge historical trace: original birth2, close7 (birth suffix index4). The target trace/subsequence, Unload exclusion, retained birth and birth-origin equation remain conditional inputs. It is not a closed instance of a full accepted deletion step or global coverage.

**OPEN B:** cross-segment retained birth/close join; selected-parent removed-center resolution; `o20DeletionRetainedClosingBirth`; actual-chain global discarded-generation selection; all-name current-coordinate rebase and D5. No capital-insufficiency counterexample or new-field manifest was established. No frozen deletion theorem was called, and no scoped-to-raw weakening was introduced.

## A: computed environments and local positions, NOT synchronization

1. A1 proves the native final scan ordinal from actual trace count. A2 computes the prefix live table through existing `scanGenerations`/`o20ScannedFinalLive` and produces its genuine scan certificate.
2. A3's new unfrozen record has no `leftLive`/`rightLive` parameters. Both environments in its stage type are deterministic native scans of the **same attached births' actual preceding traces**. Its positions remain authenticated **ORIGINAL** activation positions, not claimed canonical positions.
3. A4 constructs the new packet from physical origins. A5 removes the arbitrary environment arguments from the accepted A18-style supported occurrence telescope. Existing execution, uniqueness, generated matching and original support premises remain explicit. No new predecessor relation or arbitrary target observation is accepted.
4. A6 only derives the successor all-name cut **conditional on the actual scanned predecessor cut**. The predecessor's accepted-input producer remains open. Native paired Inserts at two real states do not imply those predecessor states are related.
5. A7 computes exact prefix scan outputs on R191's real child source cut and R193's real historical birth cut. No complete accepted canonical permutation or paired history is constructed by this fixture.
6. A8–A15 prove local executable registration-index laws: deleted classifications preserve the activation/counter inputs and observed positions; surviving classifications increment the same activation's counter and next observed position exactly once. A13 handles arbitrary association-list tails without a hidden uniqueness assumption. Full generation and L-Begin ordinals are retained in activation keys. Explicit typed decision/equation pairs, not inferred case views, discharge Boolean observations.
7. A16 compares the two **index-classifier branches** at a native root/begin index. It does not claim the historical birth has both accepted classifications or that this fixture proves a canonical prefix transport theorem.

**OPEN A:** whole canonical per-activation Insert position/order preservation; all-name predecessor production; genuine root/unsupported/closing skips; complete ordered paired occurrence histories; `o20SynchronizeCanonicalHistoriesModulo` at its unchanged goal. No arbitrary native edge is treated as epsilon. The existing global synchronization specification and stamped-history family are byte-unchanged.

## Failed attempts and evidence controls

- B2-1: missing direct `Data.List.Elem` import. Attempt2 passes; no proof premise added.
- A8-1: direct event-position equality met an opaque case-motive conversion. Attempt2 proves the honest activation/counter field-input law instead; A9/A10 subsequently derive the exact observed-position equality. Both signatures remain in raw source snapshots; no failed statement is called proved.
- A13-1: the first rewrite exposed a second test of the same Boolean key after update reduction. A second explicit rewrite fixes it; theorem unchanged.
- A16-1: `LBegin` supplied too little information to infer unused action family parameters. Attempt2 explicitly annotates `Action Nat R45Key R45Value Unit String`; expected values unchanged.

Each unit has at most two attempts, below the cap of three. B14/A16 caps are reached, not extended. Every successful new declaration has an immediate guarded exact-source commit. No proof-source edit after freeze; no new hole, postulate, partial function, `believe_me`, `assert_total`, `with`, let alias or scoped-to-raw cast. All new proof modules use `%default total` and quantity-zero proof declarations. One new record contains only erased specification fields; executable scanning/index operations are reused from native definitions.

C is ineligible and unattempted. Frozen census remains 1/2/0/0/1; production/package and all protected proof surfaces are unchanged. Any claim of full A, full B, global D5, or convergence closure would be a review blocker. The intended result is a checked partial milestone only.
