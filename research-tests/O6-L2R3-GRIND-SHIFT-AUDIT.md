# LANE 2 SHIFT L2R3 — checked research audit

## Boundary and result: bounded PARTIAL stage-1 capital

- Correct worktree only: `/Users/vyacheslavshebanov/Work/dgamma-lane2`, branch `cp5-thm73-lane-a8a10`.
- Start **1893851e**, verified exact HEAD, branch, clean tracked/untracked tree before any edit. Read both required L2R2 audit/design documents first.
- Unit0 commit **7469e29a**; last proof commit **784cb92d4d2e1fb1d3626d7286a0e7734dc2cdb7**; immutable final-plan/tools publication **ffb3106d**.
- **A19/24, B12/12, C14/14** retained; 45 one-declaration source commits. No attempt beyond B12/C14, no source work after C14, no exhausted statement reopened. The unused A slots were not repurposed as a C-cap extension.
- All new Idris files are under `research-tests/O6-L2R3-Sources/DGamma/`. No edit/build/commit/cwd entry in the main worktree. No src/, research/, package, root docs, frozen O20/O19/canonical/cross-trace/renaming/LocalDiamond changes. No subagents launched by this child worker.
- This is **not** a completed full dispatcher, whole R191 relocation, global attached normalizer, CP3/O19 integration or Theorem73 proof. Human independent proof review remains distinct from the mechanical verifier.

| Scope | Checked result | Explicit limit |
|---|---|---|
| Unit0 P2 docs | Exact two L2R2 runner module-docstring repairs, executable Python AST unchanged; corrected wording copied to L2R3 guards. | No predecessor Idris source edited. |
| A(i) attached grammar | Extended core + actual ordered checked root-insert bundle. Each root has a real own-child removal/shared declared-key witness or a previously consumed bundle root barrier. Empty initial history. | Grammar does not construct the global last-release selector, prove all forced inputs are assigned, or encode complete canonical placement. |
| A(ii) inclusions | `extendedIntoAttached`, `oldIntoAttached`; inherited old→extended reused. | Forward inclusions only; no coercion back to frozen blocks. |
| A(iii) blocks/order | Full located-block and physical-order research copies, all installed/no-other-episode/decomposition fields retained. Gap starts after bundle. | No zero gap by definition. |
| A(iv) SAME C12 trace | Parent0 [0,5), actor2 [5,7), real Root3 in parent body, actual gap NoTransitions/count0. | Same limitations of inherited fixture: not a full support-covering canonical schedule or empty-registry reachability history. |
| A(v) conditional gap | Actual root-gap membership in authenticated bundles replaces NoRootOrchestration; residual-head coverage and explicit bundle/gap separation imply zero. | Membership, separation and residual coverage remain normalization/decomposition inputs. Separation cannot be inferred merely from a pair of non-maximal possibly overlapping blocks. |
| B barrier closure | One-origin native R-then-S trace; empty-provision S always provision-free but follows key-forced R; both in parent bundle in order, gap0. Generic leastness theorem, concrete ordinal closure instance, frozen strict rejection. | Least closure is parameterized by root/seed predicates; no executable global classifier or universal link from every trace to those predicates. |
| B external order | Invert frozen actual RootOrchestrationStep/SameExternalOrchestration; any S-headed checked candidate fails against actual R/S suffix. | Arbitrary-internal-prefix transport of this suffix theorem is not claimed. |
| C(i) local dispatcher | **ROOT OInsert** and **FOREIGN ORemove** replay branches at arbitrary well-formed snapshot-equal retired sources; original checked edge is the only operational input. | Begin/Iter/Finish/other-parent child insert/Retire branches remain missing. No all-role dispatcher or whole R191 ForeignReplay instance. Existing L2R2 fold remains conditional. |
| C(ii) bundle root phase | Native post-release Begin2,R,S → R,Begin2,S → R,S,Begin2. Two authenticated snapshot phase steps, original/middle real suffixes, step-owned moved trail, exact runtime/support equality. Local inversions **2→1→0**, each exactly one. Single-R moved trail count0 too. | Zero is **post-LAST-release-local**, starting at physical cut4. The inherited full trace's raw blocked inversions are not eliminated or redefined. No global availability-aware measure or arbitrary-length bundle normalizer. |

## 1. Unit0 and compiler guards

The two exact predecessor replacements are authorized by this task:

- `research-tests/run-l2r2-commit.py:2`: now mentions the authorized C4 two-file bundle exception.
- `research-tests/run-l2r2-check.py:4`: likewise, retaining the target-only touch statement.

Unit0 was committed by the artifact guard after **V00 fresh PASS** of unchanged L2R2SmallStates. Initial **V0** was a real rejected check: Idris treats the last `--source-dir` as the target source directory; the copied runner initially supplied the new-source directory for an old Unit0 target. It produced no target Building line, no PASS and no commit. The repaired runner selects `target.parent.parent`; V00 then had its own Building line. Both raw records remain immutable.

L2R3 scripts were copied from L2R2. Changes: exact worktree/output paths, bounded A/B/C IDs and caps, exact authorized Unit0 target exception, dynamic target source root, final plan/evidence schemas, and **no reusable L2R3 C4 bundle authority**. No two-file source bundle was used. The final commit guard rejects any bundle without a separately gated runner change. Every source commit is immediate after its source PASS with exact SHA256 and a GUARDED COMMIT receipt; artifact commits use the latest unchanged-source PASS anchor and exact artifact hashes.

The final guard tests execute isolated ACTUAL helper/assertion ASTs, not copied test approximations: declaration count, target existence/nonempty/worktree boundary, cap/attempt limits, PASS retry rejection, target Building line, wrong dependency/main-worktree freshness rejection, and ownership classification. **11/11 PASS**. One compiler-free test-harness first run failed because macOS `/tmp` resolves to `/private/tmp`; resolving the temporary fixture root fixed the test, not the runner. That failed report is retained separately. No compiler was invoked by these tests.

## 2. Ratified A12 ruling, unchanged

> A12 DESIGN RULING (supervisor under owner delegation, record verbatim in the design gate memo): direction = ATTACH with immediate-after-freeing PLACEMENT. Rules: (1) a forced root input (one whose declared provision keys were declaration-occupied until an own-child ORemove) is PLACED immediately after the LAST release that makes it native-applicable and ATTACHED to that releasing actor's extended block as a trailing element — multi-key releases across different parents attach to the last releasing block; (2) several roots released by the same removal form an ORDERED BUNDLE at the end of that block, keeping their original orchestration order (external orchestration order among roots is retained, only interleaving with lifecycle changes); (3) non-forced roots stay under "orchestration inputs first" (RootInputsBeforeLifecycle unchanged for them). Next lane shift (L2R3) executes this fixture-first: define the research grammar variant `ActorLifecycleOnlyAttached` (extended grammar + trailing forced-root bundle with the release witness), prove old→extended→attached inclusions, show the C12 small countershape gets gap 0 under it with the root inside parent0's attached block, then the conditional gap theorem with the no-root premise replaced by the attached-normal-form premise. Also keep the PREMISE theorem as interim capital. No production edit; this goes into the CP3 diff plan for the owner's signature (ActorLifecycleOnly + CanonicalInputPlacement + the O19 safetyBlocksAdjacent reading over attached blocks).

> SUPERVISOR AMENDMENT to the A12 ruling (record verbatim next to it): correct obligation. Since the frozen external orchestration (RootOrchestrationStep / SameExternalOrchestration) preserves the relative order of root inputs, "forced" must be the LEAST set of root inputs that (a) contains every root whose declared provision keys are declaration-occupied until an own-child ORemove (key-forced), and (b) is closed under "later in orchestration order than a forced root" (barrier-forced). Rule (3) becomes: only roots BEFORE the first forced root in orchestration order stay under "orchestration inputs first"; the ordered trailing bundle after the last release contains all forced roots (key- or barrier-forced) in their orchestration order; a barrier-forced root with always-native-available provisions attaches to the same releasing block as the forced root it follows (or a later releasing block if it is itself key-forced by a later release). This is the transitive-closure qualification you identified; it is an unproved generalization obligation for L2R3 (define, then prove the R-then-S fixture), not a guess. Artifact-only now.

### Supervisor paper check (reported authority; no lane paper text)

Record of the task's supervisor paper audit:

> Theorem 73(1) clauses — (a) "the same orchestration steps in their original order", (b) "those at a fiber the orchestrator inserted preceding every lifecycle step", (c) "each of the rest following the step that registered the fiber it acts on"; for a key-forced root, (a)+(c) place it after the child's O-Remove, which follows the parent's registering L-Iter — contradicting (b); the proof moves root inputs to the front only "past a lifecycle step of a different fiber by Lemma 71(2)" and never exchanges two orchestration steps; moving a root insert back past an O-Remove ENLARGES the registry, while Lemma 71(2)'s O-Insert-premise argument covers only "a smaller registry only relaxes" (single-source premise ∀m. p_n ∩ p_m = ∅). So A8/A12 are paper-level gaps and ATTACH is clause (c) generalized to forced roots.

This paper-level ruling is attributed to the supervisor, not represented as an independent PDF read by this worker. `paper/` is absent in this lane, and the main worktree was never entered/read to obtain it. The native fixtures remain scoped structural countershapes, not a formal refutation under every paper metatheorem hypothesis.

## 3. Exact attached grammar and inclusions

`L2R3Attached.AttachedRelease` stores:
- actual removed child and removed fiber;
- a native `LocatedActionOccurrence (ORemove child) core`;
- exact fully-instantiated source lookup and `ChildOf selected` metadata;
- one actual shared key and membership in both declared provision lists.

Thus release means declaration occupancy at the actual pre-removal state, not active-table occupancy or retirement. `AttachedReason` is `KeyReleased release` or `EarlierForcedRoot (Elem earlier priorRoots)`. `OrderedForcedRootBundle` admits only checked OInsert roots; each step prepends its root to the already-consumed history. That history is a reverse accumulator, **not a reordered output trail**: physical transitions retain original order. The wrapper seeds it with `[]`, so a barrier cannot bootstrap itself from an arbitrary initial name. Core witnesses precede every bundled root by the physical append.

Exact definition (A4 **4974a6f9**):

```idris
public export
data ActorLifecycleOnlyAttached :
  {name, key, world, error : Type} -> {value : key -> Type} ->
  (nameEq : DecEq name) -> (selected : name) ->
  {first, finalState : SystemState name key value world error} ->
  Transitions first finalState -> Type where
  AttachedWithoutRoots :
    {name, key, world, error : Type} -> {value : key -> Type} ->
    {nameEq : DecEq name} -> {selected : name} ->
    {first, finalState : SystemState name key value world error} ->
    (core : Transitions first finalState) ->
    (0 extended : ActorLifecycleOnlyExtended nameEq selected core) ->
    ActorLifecycleOnlyAttached nameEq selected core
  AttachedWithRoots :
    {name, key, world, error : Type} -> {value : key -> Type} ->
    {nameEq : DecEq name} -> {selected : name} ->
    {first, coreEnd, finalState : SystemState name key value world error} ->
    (core : Transitions first coreEnd) ->
    (0 extended : ActorLifecycleOnlyExtended nameEq selected core) ->
    (bundle : Transitions coreEnd finalState) ->
    (0 orderedForced : OrderedForcedRootBundle nameEq selected core [] bundle) ->
    ActorLifecycleOnlyAttached nameEq selected (appendTransitions core bundle)
```

Forward inclusion types (omitting unchanged universe/state implicits):

```idris
extendedIntoAttached : ActorLifecycleOnlyExtended nameEq selected trace ->
  ActorLifecycleOnlyAttached nameEq selected trace
oldIntoAttached : (nameEq : DecEq name) -> ActorLifecycleOnly selected trace ->
  ActorLifecycleOnlyAttached nameEq selected trace
```

A5 **d2b26a9a**, A6 **0f19676d**. The existing `actorLifecycleOnlyIntoExtended` is reused, not copied or changed. Quantities: runtime states/traces/witness data retained where stored; grammar/decomposition/equation specifications and proof producers are erased. Every new proof module uses `%default total`, `%unbound_implicits off`. No new with, let alias, holes, partiality, postulate, believe_me, assert_total or assert_smaller.

### Full block copies, not smaller substitutes

A7 **a7799047** copies the full `LocatedOpenEpisodeBlockExtended` counterpart of CP3:1824; only the actor grammar/accessor namespace changes. Installedness includes bundled roots, not just the core. A8 **ce0c13bd** copies the exact physical `BlockBeforeExtended` equation (CP3:1873), replacing block types/accessors. Neither copy assumes adjacency.

### C12 SAME-trace literal zero gap

A9 **ffb8a023** authenticates Remove1 inside the actual Finish0/Retire1/Remove1 core; shared declared key True. A11 **1b7e1eeb** simultaneously constructs `smallAttachedBlocks : SmallAttachedBlocks` from unchanged `smallTrace` and native `smallState`:

```text
parent0: [0,5) = Begin0 ; Finish0 ; Retire1 ; Remove1 ; Insert3 Root
actor2:  [5,7) = Begin2 ; Finish2
physicalOrder = MkBlockBeforeAttached NoTransitions Refl
forcedRootGapCount : transitionCount (attachedBetweenBlocks physicalOrder) = 0
```

The root has its real body occurrence, not a tag-list membership. A19 **a30ec23d** separately constructs a nonvacuous generic `AttachedBundleOccurrence ... smallTrace ... 4` with actual bundle split, local occurrence, global offset and bounds. The original L2R2 extended gap1 witness is untouched; attached endpoints—not an arithmetic assertion—explain the changed gap.

### Conditional theorem, additional decomposition obligation visible

A18 **2f57340c**, `attachedZeroGapInNormalForm`, retains L2R2 residual-root-head coverage/count transport, ties the physical gap offset to the earlier attached body, and consumes:

1. `AttachedNormalForm`: every actual root-orchestration occurrence in the supplied gap is in an actual attached block's forced bundle at the same global ordinal;
2. `separated`: each authenticated bundle interval lies before or after that gap.

The separation premise is explicit because the grammar and a pair of located blocks do not establish a maximal nonoverlapping schedule. It is **not** a count-zero, coincident-cut or NoRootOrchestration premise. Actual member bounds and disjoint intervals exclude a nonempty root head, so residual coverage yields zero. This is a conditional theorem only: coverage, membership assignment, last-release choice and separation are not generated here. The inherited `extendedZeroGapWithoutRoot` remains interim capital.

## 4. Barrier closure fixture and least set

The exact definition is:

```text
ForcedRootInput rootInput keyForced n is the LEAST finite inductive family:
  KeyForces   : rootInput n -> keyForced n -> ForcedRootInput ... n
  OrderForces : ForcedRootInput ... earlier -> rootInput later ->
                LT earlier later -> ForcedRootInput ... later
```

RootInput is intended to classify native root OInsert occurrences by their original physical ordinal; its restriction inherits original external order. `forcedRootLeast` (**7737a019**) proves containment in EVERY candidate family containing those seeds and closed under strictly later actual roots. It is not merely an asserted least-set comment. The classifier predicates are parameters: global executable classification/linkage is still open.

Native B6 **c6e3324f** `barrierTrace` shares smallState0..5 and adds S4 (empty provisions), Begin2/Finish2. All new states are native operations from smallState5. B5 **37917417** authenticates the new edges via checkedFromRaw/Preservation and S's free provisions at every actual cut. No alternate state was reconstructed to pretend proof identity.

B8 **224988a1** simultaneously builds attached parent0 **[0,6)**, actor2 **[6,8)**, actual gap0 and both roots in order. R3 uses the actual own-child Remove1 release. S4 uses `EarlierForcedRoot {earlier=3} Here` in already-consumed `[3]`; empty S cannot be key-forced by that release. S is attached to R's releasing block.

B11 **9c7c0a7c** exposes actual root births at ordinals4/5, native RootOrchestrationStep witnesses, `4<5`, S in the concrete forced closure over displayed root ordinals `[4,5]`/seed4, and rejects frozen strict placement for EVERY support state/order. B9/B12 (**110fd718**, **0249d5ef**) invert frozen SameExternalOrchestration and refute ANY checked S-headed candidate against the real R/S suffix. Moving S before R cannot preserve that external order. The suffix theorem is not a claimed generic arbitrary-internal-prefix transport proof.

## 5. A10/A8 stage-1 remainder, precise coverage

### Native dispatcher branches, not a complete dispatcher

- C1/C2 (**30b7d7a9**, **73207255**): `replayRootAfterRetirement`. Actual source/old checked root edge, actual own-child lookup/parent and distinction, well-formed source/current, and exact retired-source runtime equality. The producer constructs late retirement, calls B10's real square, derives early-state identity by native determinism, and replays at current. It does not accept alternate applicability/snapshot as an oracle.
- C9–C14 (final **784cb92d**): `replayRemoveAfterRetirement`. Original checked foreign Remove produces its actual single-constructor native view. Retirement preserves parent metadata, hence childlessness, while distinct lookup framing preserves the removed entry/guard. Native raw replay plus Preservation gives the checked alternate. Exact ordered-bindings retirement/deletion algebra and CP4 snapshot coherence supply arbitrary-current replay. No own-child-removal exact-state producer was restated; this is foreign removal AFTER a different child's early retirement.

**Still missing:** Begin, LIter/LFinish (and the additional lifecycle forms allowed by the generic Action domain), other-parent child OInsert, ORetire dispatchers, all-role assembly, and the whole-run R191-style `L2R2ForeignReplay` instance. Existing Begin/Advance-vs-child-Remove squares are not mislabeled as foreign-Begin/Advance-after-Retire branches; the retired/deleted actor roles differ. No supplied whole-suffix oracle or weakened copied fold hides that distinction. C cap14 was reached without self-extension.

### Native ordered bundle phase, local zero

C3–C8 (final **99837ef0**) build three actual post-release paths:

```text
smallState4 -> Begin2 -> R3 -> S4       [local inversions 2]
smallState4 -> R3 -> Begin2 -> S4       [local inversions 1]
smallState4 -> R3 -> S4 -> Begin2       [local inversions 0]
```

The first R square is inherited; the second S square (**300f18e0**) is new and native after R. Each is availability-aware. Actual S edges run on both sides of the first move; exact final world and ordered bindings agree. The second phase's OWN native trace is annotated as the final moved trail. `BundlePhaseEvidence` stores both phases, actual original/middle transitions and annotations, exact-one decreases, local single-R zero, R/S zero, and exact runtime/complete executable support-set preservation.

The inherited `rootPhaseFromSnapshot` body is export-opaque when imported. C8-1 correctly failed a literal endpoint conversion, not a theorem. C8-2 uses the **same type and exact constructor recipe** with existing squares, support theorem and beginSnapshotRootDecreases. It does not change the phase signature or invoke proof irrelevance.

The zero is deliberately LOCAL from freeing cut4, with prior count0. The old six-edge full-prefix single-R counts remain 3→2, and pre-release lifecycle inversions for forced roots remain blocked. No global raw-rootBirthInversions=0 theorem, new global measure, suffix-independent general normalizer, full support-linearization transport, canonical invariant bundle or frozen O17/O19 integration is claimed.

## 6. Attempt, resource and scope evidence

- **45 source commits / 51 development source checks**: 45 retained PASS, 6 rejected source attempts. Unit0 V0 rejected, V00 PASS. Final V1–V12 all PASS. Total **65 checks: 58 PASS / 7 rejected**.
- No 3/3 failure; B5 passed on its third/last attempt. No interrupted or mutation-observed invocation; no failed-run commit.
- Idris2 **0.8.0**, seeded TTCs retained, target-only touches logged with exact path and old/new ns. No mass touch, build deletion, package check or cold rebuild.
- Detached Python `-I`; serial per-lane compilers. Target's own Building line and exact target hash mandatory. Source hashes sampled each250ms and rechecked by guard/immutable final plan; sampling is not continuous filesystem surveillance.
- Highest sampled RSS **2,283,376 KiB (~2.178 GiB)** at C8-2, above the small-state expectation but far below unlocked18GiB/heavy48GiB interruption thresholds. Final V11 was 1,952,816 KiB (~1.862 GiB). This is sampled RSS, not a continuous peak.
- **0 heavy locks/waits/stale removals**, so no shared lock was required. No main compiler signalled. 5 unique main-lane compiler PIDs observed, each classified `main-lane compiler (separate worktree)`.
- Recorded compiler wall times sum ~198.70s; this is not elapsed shift duration. Proof construction, diagnosis, documentation, evidence verification and owner/reviewer coordination are separate.

| Rejected invocation | Diagnostic / resolution |
|---|---|
| V0 | Wrong final source-dir for Unit0 old target; no Building/PASS. Dynamic target root, V00 passed. |
| A14-1 | Nat plusSuccRightSucc rewrite direction; sym fixed A14-2. |
| B5-1 | Direct checked Refl stuck reducing post-insert registry validity. |
| B5-2 | Direct source-validity Refl still stuck at inherited active view invariant. B5-3 uses inherited checked target validity/Preservation, with real raw equations; no state/proof reconstruction. |
| B8-1 | Imported public producer referenced checkedFromRaw without direct module import during dependent conversion. Explicit CP4ProgressNoDeadlock import; B8-2 passed. |
| C8-1 | Export-opaque rootPhaseFromSnapshot cannot expose literal supplied-square endpoint. Exact same constructor recipe, no signature change; C8-2 passed. |
| C10-1 | Unbound implicit value family on ORemove raw evaluator application. Fully instantiate all applyAction parameters; C10-2 passed. |

## 7. Reproducible final evidence

The exact 12-module leaf-before-dependent plan was committed in **ffb3106d BEFORE V1**, with source hashes/dependencies. It has not been rewritten. Every V1–V12 record has its own Building line, exit0, immutable current hash and no mutation. This is intentionally not a whole-package build. Never rerun these immutable invocation IDs; a further owner-requested validation needs a new plan/IDs and seeded-build rules.

Artifacts:
- `O6-L2R3-MICRO-UNIT-LEDGER.json`: all 45 names, commits, attempts/hashes/RSS and scope counts.
- `O6-L2R3-DECLARATION-ORIGINS.md`: full unit/commit table and copied-definition/constructor-recipe inventory.
- `O6-L2R3-COMPILER-EVIDENCE.tar.gz`: exact source snapshots, logs, raw records, target touches, plans, receipts, runner tests and qualifications.
- `O6-L2R3-COMPILER-LEDGER.json`: independently reconstructible normalized records/commit matching and archive SHA256.
- `O6-L2R3-INDEPENDENT-VERIFICATION.json`: separately executed compiler-free acceptance report, published only on PASS.

Reproduction (this lane cwd only):

```text
python3 -I research-tests/run-l2r3-evidence-tests.py
python3 -I research-tests/run-l2r3-independent-verify.py
git diff --check
git diff --stat 1893851e HEAD
git status --short
```

The mechanical verifier authenticates git objects, per-source commit receipts, final hashes/Building lines, serialized invocations, allowed paths, exact P2 doc-only repairs, no unsafe new Idris syntax, caps, archive bytes, and clean scope/staging. It is NOT independent human mathematical proof review. Archive publication and later report-only receipts cannot recursively be in the archive; they are authenticated from the live append-only receipt ledger. The published report names the HEAD it actually verified, not its later publication commit.

## Status

**Fully checked in their stated scope:** actual attached release/barrier grammar and forward inclusions; full located/order variants; SAME C12 trace with literal attached gap0; actual ordered R/S bundle gap0; generic leastness; scoped external-order reversal rejection and uniformly strict placement rejection; two native retirement-replay branches; two ordered physical post-release root phases with local 2→1→0 and exact runtime/support equality.

**Conditional/partial:** attached zero gap needs residual coverage + actual-bundle membership + physical interval separation; forced closure needs actual predicate linkage; root phase zero is post-release-local; root replay branch requires actual child-parent metadata.

**Open, not silently stated as proved:** global last-release/key/barrier classifier and placement normalizer; complete root assignment/maximal nonoverlapping decomposition; arbitrary-prefix external-order transport; remaining action replay dispatchers and whole R191 fold; global availability-aware measure/normalization; CP3/O19 diff and full canonical/confluence obligations.

**Next:** independent human proof review; owner-signed research-to-CP3 diff only after generalized coverage/placement and remaining native dispatchers are discharged. Do not edit production here, reverse-coerce attached blocks, restate exhausted fixtures, re-enter nested R174 producers, or redefine blocked inversions away.
