# O6 revision 136: Deletion O7 aligned-transition surface revision audit

## Authorized delta and necessity witness

This is the supervisor-pre-authorized Unit A revision.  It changes only O7's
correlation surface after revision 135 established that its quantity-0 payload
shape is otherwise sound.

`O6-R135-DELETION-O7-DICTIONARY-CORRELATION-STOP-AUDIT.md` records the disposable
`R135O7DictionaryProbe`.  Reclassifying one actual L-Begin trace head under O7's
external dictionaries failed at the exact equation with:

```text
Mismatch between: transitionNameEq and externalNameEq.
```

The parallel key dictionary remains subject to the same executable `DecEq`
identity requirement.  Consequently O7 now receives exactly:

```text
AlignedTransitions name key world error value nameEq keyEq trace
```

at quantity 0.  No state equality, dictionary equality, action/tag equality,
raw checked equation, blanket trace equality, canonicalization bundle, or
output-shaped occurrence premise is added.  This is the minimum existing
producer-owned family that authenticates each transition's action, tag, checked
equation, and recursive tail under precisely O7's two explicit dictionaries.
The occurrence list, sound located payloads, opening-ordinal uniqueness,
completeness, and empty-scan conclusion are unchanged.

## Consumer-side discharge

The only real call site is `chooseClosingStepSpike`.  Its existing
`CanonicalizationPremises` contains `chainReplayCapital`, whose first erased
field is the exact required `replayAligned`.  The call now supplies:

```text
replayAligned (chainReplayCapital premises)
```

with no conversion, equality assumption, or new caller premise.

The tracked `R7DeletionBoundariesPositive` fixture was updated in the same
series.  `scanOnly` exposes the exact new premise, while
`scanFromCanonicalPremises` constructs it from the same real canonicalization
capital.  Thus both the direct boundary and the actual consumer-owned discharge
are checked.  No consumer is left unable to construct the alignment.

## Scope and attempt accounting

This surface revision changes no hole body and consumes **0/3** O7 fill
attempts.  O7 remains the next and only open DeletionChain obligation.  The hole
census remains **18**, split **5/4/8/0/1** until the body is filled.  Production
`src/`, `dgamma.ipkg`, immutable CP3, and the byte-frozen adjacent-swap surface
are untouched.
