# O6 revision 37: delete-control closure and lifecycle-head replay

## Scope

Shift #45 remains private to the research O6 body. The frozen
`adjacentSwapSuffixSpike` declaration/body hole, `SealedSuffixReplaySpine`,
opaque adjacent result, manifest, production `src/`, package, and immutable CP3
theorem are unchanged. No output-shaped premise, caller-selected checked
transition, endpoint, map, guard, occurrence, ordinal, or bundle was added.

## O-Remove delete controls close

`pointwiseControlAfterDelete` implements the approved direct-entry tactic. Its
function clause exposes both registries as
`MkCoeffectContext entries unique`. The same-actor branch invokes
`localLookupDeleteEntriesSelfO5 nameEq actor entries unique` directly on each
side, while every other actor uses `lookupDeleteOther` and the incoming
pointwise relation.

This elaborated after the ordinary pattern-variable correction and was committed
independently:

- `5e477a1` — `preserve pointwise controls after owner deletion`.

The revision-36 `DecEq` wall did **not** recur through the approved tactic. The
conditional pinned negative requested only on recurrence was therefore not
created.

## O-Remove guard transport closes

The first local constructor-destructing guard formulation was discarded after
its bounded failures. The retained formulation is structurally different and
uses two indexed projections:

- `pointwiseFiberRetiredSame`;
- `pointwiseFiberInactiveSame`.

`pointwiseRemovalGuardRelated` extracts the two source Boolean clauses from the
producer-owned composite guard, transports retirement and lifecycle mode through
those projections, and combines them with exact target childlessness from
`pointwiseNoChildPreserved`. It never reopens `removeSuccessView` and never
constructs R34's independently oriented child observation.

The projection form elaborated on its third attempt after parenthesizing Boolean
equality annotations and making the target `hasChild` indices explicit:

- `6d33734` — `transport pointwise remove guards`.

## Bounded full O-Remove head stop

The full private `replayPointwiseRemoveHead` was opened only after both delete
control and guard transport were committed. It consumed the approved
`PointwiseRemoveSourceObservation`, reconstructed the pointwise target owner,
target childlessness and composite guard, formed the concrete delete endpoint,
and reached checked-target/effect/control/RAR packaging in source form.

Its fresh three-attempt budget stopped on elaboration annotations, not on the
old dictionary wall or R34 orientation:

1. the quantity-zero `pointwiseRemoveSourceObservation` call was first bound at
   unrestricted quantity and was rejected as inaccessible;
2. changing it to an inferred `0` binding was rejected because erased local
   bindings require an explicit type (`No type declaration ... fromInteger`);
3. explicit erased types advanced to the target childlessness declaration, then
   stopped because its unparenthesized Boolean equality parsed as `Bool` versus
   `Type`:

```text
While processing replayPointwiseRemoveHead,targetNoChild:
  Mismatch between: Bool and Type.

let 0 targetNoChild : hasChild @{nameEq} ... actor replayedRegistry = False
```

All full-head code was reverted after the third attempt. The exact next fresh
attempt should retain the explicit erased types and parenthesize the Boolean
proof declaration:

```idris
0 targetNoChild :
  (hasChild @{nameEq} {name = name} {key = key} {value = value}
    {world = world} {error = error} actor replayedRegistry = False)
```

The analogous source childlessness declaration should also be parenthesized for
consistency before the head is reopened. No new capital or interface change is
needed. Because neither the delete-self `DecEq` mismatch nor the R34 orientation
recurred, no new expected-failure probe was added.

## L-Divert closes with native target indices

`replayPointwiseDivertHead` implements the approved native-index design:

1. `foreignDivertReplayData` fixes the source owner and endpoint;
2. the related target endpoint is reified immediately from the pointwise lookup;
3. `replayedRemaining`, `replayedAccumulator`, and `replayedView` remain the
   target owner's native dependent indices;
4. `pointwiseConcreteTargetFiberSame` relates the two exact target queries;
5. the mismatch is transported at `sourceView`, and only then changed to
   `replayedView` through `viewsSame`;
6. `divertLifecycleControlRelated lifecycleSame` constructs the next lifecycle
   without forcing source and target indices; and
7. the producer owns target applicability, preservation, effect frame, replaced
   controls, exact map, singleton RAR, occurrence, ordinal, alignment, and
   endpoint.

After layout-only corrections, the semantic implementation needed two attempts:
the first simultaneous located-view pattern was rejected because
`observedOwner` refined to the concrete owner. Splitting through
`foreignDivertReplayData` and then eliminating `ownerShape` elaborated.

Commit: `69b6786` — `replay pointwise divert suffix heads`.

## L-Leave closes by the same pattern

`replayPointwiseLeaveHead` specializes the accepted L-Divert structure to
`Active` owners using `foreignLeaveReplayData`, `activeRightControls`, and
`leaveLifecycleControlRelated`. It preserves native target accumulator/view
indices and performs mismatch transport in the same two stages. It also owns all
checked, endpoint, map, RAR, occurrence, ordinal, and alignment capital.

The complete head elaborated on its first attempt.

Commit: `e32a363` — `replay pointwise leave suffix heads`.

## Safe boundary and estimate

The complete generic semantic heads are now O-Insert, O-Retire, L-Begin,
L-Divert, and L-Leave. O-Remove has all source/guard/delete-control foundations
but its full producer remains open at the exact erased Boolean annotation above.
L-Unload and L-Advance remain unopened, as do whole-suffix RAR/ordinal
composition and final adjacent-result assembly.

L-Unload accumulator transport was not opened because the O-Remove full-head
budget and two complete lifecycle heads consumed the shift's clear proof budget.
L-Advance was not opened, as required.

Holes remain **20**, split **6/4/8/1/1**. The required O6 estimate remains
**18–34 shifts**: two lifecycle families closed, but O-Remove, both heavy
remaining action families, whole-trace capital assembly, and final opaque-result
construction remain charged.
