# O6 revision 123: action-occurrence origin STOP-AUDIT

## Scope

Shift #121 (overall #175) resumed from accepted HEAD `cd7ba626`. It followed
copy-probe, retained lookup package, body external-order, and occurrence
assembly in order. The disposable probe, retained root-lookup package, whole
same-external proof, and six positional occurrence foundations closed. The
first complete adjacent action-origin producer then exhausted its independent
three-attempt budget. Its entire uncommitted unit was removed; the safe retained
boundary is `aba00c4`.

No `ReplayInvariantBundle` field, revision-19/20/21 surface,
`AdjacentSwapResult`, frozen declaration, or hole was modified.

## 1. Disposable exact-copy probe — passed and removed

An exact copy of `CP5ConfluenceLocalDiamondSpike.idr` was written temporarily as
`research-tests/DGamma/R47RootLookupPackageCopyProbePositive.idr`; only the
module header was renamed. The copy preserved the explicit
`import Decidable.Equality`, then appended the proposed package and a
single-elimination `RootOrchestrationStep` consumer.

The fresh check emitted:

```text
R123_ROOT_LOOKUP_COPY_PROBE=passed
R123_ROOT_LOOKUP_ONE_ELIM_CONSUMER=passed
R123_DISPOSABLE_COPY_PROBE_REMOVED=passed
```

The temporary source and TTC/TTM were removed in full and never tracked.

## 2. Retained lookup package — `75b620e`

`RootControlLookup` and `produceRootControlLookup` landed on attempt 1. The
private package owns:

- the exact source fiber and source lookup equation;
- the exact target fiber and target lookup equation;
- `FiberControlRelated sourceFiber targetFiber`;
- source `fiberParent = Root`; and
- target `fiberParent = Root` transported from the relation's exact parent
  equality.

All proof fields are quantity 0. Construction consumes
`pointwiseControlLookupFound` once and consumes its dependent relation under one
elimination. No detached target fiber or lookup can enter.

## 3. Whole same-external body capital — `b102cca`

The body external-order unit landed on attempt 1. It contains:

- a total private `RootOrchestrationStep` decision;
- forward and backward root classification transport through the retained
  lookup package;
- recursive `SealedSuffixReplaySpine` same-external preservation;
- exact prefix reflexivity; and
- `adjacentWholeSameExternal`, composing prefix, supplied pair relation, and
  suffix through `framePairExternalOrderSpike`.

O-Retire and O-Remove consume `RootControlLookup` under one elimination. No
lookup is reconstructed independently.

## 4. Retained positional occurrence foundations

Six checked commits remain:

- `a0a460a` — structural occurrence index, action occurrence-to-membership, and
  producer-owned located action reconstruction;
- `ba00099` — involutive adjacent ordinal transposition and registration-
  generation bijection;
- `6aa79d2` — the located-action package additionally owns exact action and tag
  identity to its selected transition;
- `aba00c4` — source/target suffix embedding index equality, suffix lower bound,
  and located ordinal reindexing.

The helper units needed three, three, two, and one attempts respectively. They
are private, total, and do not alter a frozen surface.

## 5. Adjacent action-origin producer: three-attempt stop

The removed unit attempted a private `AdjacentLocatedActionOrigin`, owning one
exact source occurrence, source/target tag equality, and the complete
`AdjacentSwapOrdinalRelation` for a target `LocatedActionOccurrence`.

### Attempt 1

The package omitted explicit source/target state indices. Idris rejected the
record because the four `Transitions` endpoint states could not be inferred.
The later private-view access error was cascading.

### Attempt 2

After adding all endpoint states explicitly, the prefix branch reached the
ordinal relation but could not identify the index of an append-left occurrence
across source and target tails:

```text
Mismatch between: pairFirst and replayedFinal.
```

This showed that the endpoint-parametric append embeddings required their own
structural ordinal producer rather than definitional reduction.

### Attempt 3

Structural prefix and moved-right/moved-left ordinal producers were added.
Compilation then stopped at two boundaries:

1. a mechanical nonlinear pattern in the private successor lift for
   `AdjacentSwapOrdinalRelation`; and
2. the substantive dependent occurrence-view boundary:

```text
Mismatch between: OccursLater ?postpone and OccursHere.
```

The `R97AppendOccurrenceView` is indexed by one `OccursIn` proof, while the
consumer branch tried to refine a separately reconstructed occurrence proof
from `LocatedActionOccurrence`. This is the same stored-vs-reconstructed
correlation class: the target region and its exact local occurrence must be
owned by a producer indexed directly by the original located occurrence and
consumed under one elimination.

The budget was exhausted. No fourth attempt, alternate classifier, occurrence
correspondence, result construction, or hole edit was made. All code from this
unit was reverted.

## Required next boundary

A future design/probe must replace the independent
`actionOccurrenceOccurs`/`r97ViewAppendOccurrence` consumer pair with a
producer-owned four-region view indexed directly by the original
`LocatedActionOccurrence`. Each constructor must own its exact prefix,
moved-right, moved-left, or suffix local occurrence together with the exact
whole occurrence/ordinal equations. Only then may the action-origin producer be
retried. The retained structural ordinal and embedding lemmas can supply the
constructor fields; no closed bundle field needs reopening.

## Status

- exact-copy lookup probe: **passed and removed**;
- retained root lookup package: **proved**;
- body same-external relation: **proved**;
- positional occurrence foundations: **proved**;
- adjacent action-origin producer: **failed after 3; removed**;
- whole action-registration occurrence correspondence: **unopened**;
- `AdjacentSwapOperationalOccurrenceFold`: **unopened**;
- `AdjacentSwapResult` and body: **unopened**;
- holes: **20**, split **6/4/8/1/1**.

```text
NO_FROZEN_SURFACE_CHANGE_REQUIRED
R123_ACTION_ORIGIN=failed_after_3
```
