# R198 Unit A analysis — before any proof attempt

Baseline 2c9c2e19; source reads pinned to that HEAD. This is an analysis, NOT
an inhabitant of o20CanonicalSynchronizationGoal or a confluence refutation.

## Exact requested result

`o20SynchronizeCanonicalHistories` must accept the existing convergence
inputs, the literal PermutedCanonicalExecution, both original raw-uniqueness
proofs and GeneratedOrchestrationMatched, and return
`o20CanonicalSynchronizationGoal execution leftUnique rightUnique generatedMatched`.
The TYPE expands to O20HistorySynchronization at the actual conjugated map:
left original->canonical->operational replay; original generation bijection;
inverse/right original->canonical replay. It owns native paired paths between
the literal endpoints and both final GenerationTraceScan environments. It does
NOT equate those paths with the supplied trace values.

## Accepted facts and what they actually give

* SameExternalOrchestration, original->canonical sameInputs, actual permutation
  same-inputs, and R197 root attachments give the same external root inputs IN
  ORDER, opposite right canonical occurrences, and both physical ordinal laws.
* E8 RegistrationTraceCorrespondence matches surviving child births by exact
  component, parent generation and activation-local child POSITION. R195's
  o20OriginalGenerationDisposition retains closing alternatives with actual
  parent Unload evidence. R194 supplies the double original/replay attachment
  on supported births; R195 carries all original history through replay.
* E9 GeneratedOrchestrationMatched matches every original generated Retire/
  Remove with kind, generation, inverse-position and relative-order laws. The
  R178 role mismatch is excluded by E9, not an accepted counterexample.
* o20WholeCanonicalRoles excludes failure/diversion/recovery in the canonical
  word. LocatedOpenEpisodeBlock and ActorLifecycleOnly give one contiguous
  Begin/body ending Active per supported actor; bodies may interleave actor
  lifecycle steps with generated Insert. Operational permutation fixes actor
  block order, not an internal iterator/child shuffle.

## Newly identified coverage question: intra-block iterator positions

The asserted implication "same lifecycle roles per actor + same generated
birth positions => same retained ROLE SEQUENCE" is not immediate. Frozen
ParentRegistrationYield (CP3:216ff) licenses a child by the parent's ACTUAL next
step, but does not require that a yielding step be consumed exactly once per
insertion or that repeated equal tags have distinct registration identities.
E8's matchedPerActivationPosition counts retained CHILD BIRTHS, not lifecycle
Advances. For a parent program [yield(tag0), yield(tag0)] consider:

```
left:  root0; Begin0; child1; Iter0; Finish0; Begin1; Finish1
right: root0; Begin0; Iter0; child1; Finish0; Begin1; Finish1
```

Same root, same child component/parent/child position, same actor order [0,1],
no Retire/Remove (E9 empty on both). Both insertion sites have next tag0. The
child physical birth ordinals are 2 and 3. This is a CANDIDATE to test natively,
not an already-checked example. It is unrelated to the exhausted R192 C4
11-edge child-retirement relocation: no Retire relocation is attempted.

R197's StampedHistoryMore advances BOTH counters in lockstep from Z/Z;
StampedHistoryEpsilon has literally zero edges on both sides. Thus even the
weaker endpoint/scanner goal may be too strong: it must end with scanners
remembering different birth ordinals while paired inserts write equal stage
ordinals. It cannot silently identify these stamps or count a real Iter as
an epsilon. A native one-origin fixture and a precise necessary invariant
are required before promoting this to candidate A13 or requesting any change.

## Preliminary disposition and next bounded steps

Coverage NOT YET established. Probe the repeated-tag intra-parent schedule
with actual checks, source-step provenance, equal endpoint (ONE origin), and
E9 applicability; preserve explicit scope if full independent canonical capital
is not packaged. No synchronization, controls or bridge by a new premise. No
protected statement/frozen family change is proposed at this point. Gate the
exact fixture result before deciding whether A can proceed or needs a research
synchronization-family repair. B still separately needs original->canonical
all-name MaybeFiber observations under expectedBridgeBijection; C ineligible.


## A source freeze, 10:01Z — supersedes the preliminary disposition

A has26 attempted MICRO-UNITS,25 retained declarations and33 actual compiler
invocations (26 PASS,7 rejected). A20-1 was a compiler PASS whose commit guard
rejected an extra EOF blank line; its exact whitespace-only A20-2 recheck was
PASS and committed. It is not two proofs. A3 alone exhausted3/3 and was fully
reverted to A2; its three failed native-path snapshots remain evidence.
No fourth attempt or alternative own-parent square was launched. See the stop
audit and owner ruling; no repeated-tag native pair/full capital exists.

A4–A11 prove a structural necessary condition, culminating in
`o20SynchronizedScanForwardOrdinalFixed`: the OLD synchronization forces the
forward generation map to preserve every final-left live birth ordinal,
including the actual `GenerationTraceScan` attachment. This is not a compiled
accepted-pair countermodel. The owner activated an UNPROTECTED research-goal
repair on this necessity alone; verbatim gates are in the ruling document.
The old goal/fold are now superseded candidates and remain byte-identical.

A12–A18 supply actual transition projections, `O20OccurrenceStampedHistory`,
its finite cut fold, `O20HistorySynchronizationModulo`, the new exact-conjugated
`o20CanonicalSynchronizationGoalModulo` TYPE, and the empty-origin cut fold.
A23 `o20CanonicalModuloHistoryCut` specializes that fold to the literal accepted
capital and literal operational execution; it consumes synchronization and
produces the existing `O20HistoryCut`. No successor cut or endpoint oracle is
an extra input. It does NOT produce `O20AllNameCut expectedBridgeBijection`.

A19/20 own the R191 eleven native stages with two actual supplied-word
occurrences/stamp equations at every stage and the actual endpoint history
cut. A22 also owns BOTH genuine scanner certificates for this eleven-edge
identity synchronization. A21 retains R193/R195's actual closing disposition
and present-vestigial internal cut at a ZERO-EDGE terminal occurrence path.
A21 is NOT a new full eight-edge closing-history traversal or canonicalization
fixture; the initial internal cut is the already-proved original-endpoint cut.
The inherited R195 identity action-replay theorem is separately retained.

A24/25 project genuine left/right native paths from the new family; A26 proves
the TWO PROJECTED paths have equal edge counts. It does not identify either
projected path with its supplied word, equate the supplied words' physical
counts, or assert equal birth ordinals. The family attaches actual occurrences
but does not encode whole-word/per-actor ordered coverage. Its honest limit is:

> whole-word / per-actor ordering and coverage producer remains to prove; arbitrary skips are NOT asserted to be zero native edges

`o20SynchronizeCanonicalHistoriesModulo` has NOT been declared/inhabited.
Whole-word coverage/order, occurrence exchanges and an accepted-input producer
remain debt. The new family is downstream-sufficient for an INTERNAL history
cut if inhabited; endpoint ALL-name rebasing is still the independent B seam.
C has zero attempts and remains ineligible. A is frozen; no extension requested.
