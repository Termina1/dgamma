L2R6 FINAL GATE at 87e9fb20 was ACCEPTED by the supervisor as an explicitly PARTIAL bounded milestone (first real native iteration instances closed); stand-down authorized; independent reviewer launched

# L2R7 shift audit

## Start and authority

Exact clean start: 87e9fb20ebf2c18719d327f83b6b65b685afebad, branch cp5-thm73-lane-a8a10, worktree /Users/vyacheslavshebanov/Work/dgamma-lane2. L2R6 GRIND-SHIFT-AUDIT and DECLARATION-ORIGINS read FIRST, then updated L2R5 draft. No main-worktree access, no heavy lock/window access. New research sources only; no inherited proof edits authorized at start.

## New ruling (verbatim)

FORCED-ROOT CONTROL DISPOSITION = option (1) RICHER BUNDLE WITH CONTROLS — the forced-root bundle admits, in orchestration order, ORetire/ORemove of a root already inserted in this or an earlier bundle (elements: OInsert root with release/barrier witness | ORetire root with an already-bundled witness | ORemove root likewise); the attached block's body = core ++ bundle-with-controls; the gap statement is unchanged.

Rationale: the paper's clause (a) keeps orchestration steps in their original order, so a forced root's retire/remove cannot precede its forced insert and belongs with it; ForcedRootNeverRetired stays as the interim scope for theorems not yet lifted. ActorLifecycleOnlyAttachedC is a NEW research grammar; L2R3 remains untouched.

## Status

Source work capped at A22/B16/C12/D8: **57 retained declarations in17 new modules**, last source commit **435bd364**. Overall **PARTIAL**. General phase/move/iteration/NF producers are NOT claimed from their types. Every retained declaration has a fresh own-target PASS and guarded receipt. No further source attempt after the caps; Unit E final validation/publication follows. Three-attempt budgets retained, no extension.

## Early supervisor scope ruling

A(i)'s release-decoder/classifier prerequisites may be implemented in A slots; C completes remaining correspondence. D8 reproduction remains A's acceptance criterion.

C(iii): do NOT attempt literal anchorOf-unchanged. Stable release identities and explicit physical ordinal-shift equations are the intended anchorTransport result. If R crosses a foreign own-child Remove whose key is shared by later S, that release's physical cut shifts right; literal current-cut equality is false. L2R6 sources stay untouched. General distance invariance requires actual proof, not this ruling as a premise.

## A8 stop audit

A1–A7 retained (7 declarations including deferred A4): typed AnyHit, membership lift, observed-head eliminator, true-fold and observed-fold bridges, general anyHitObserved producer (A4 passed2/3 after full revert and the two bridge lemmas), SharedKey type. A8 elemDecConsObserved exhausted3/3: first rejected a binder collision; next two could not identify the native dependent elemDec case with its explicitly observed case. FULL REVERT to A7 source bytes; no retained failed declaration, no fourth attempt or restatement. Shared-key extraction and hence decoded releases/phase producer remain blocked at this seam. Stop gate sent before continuing.

## A stop resolution and cap

Supervisor RATIFIED A8 stop as an "observed-Dec over library decider" wall; future owner work must observe the library result at its call site, not reconstruct its dependent case. No re-entry. A continued on independent classifier correspondence and reached A22: **21 retained declarations**; general observed per-occurrence classifyForcedSound (190703a3) and classifyForcedComplete (e353c4cc) now close against independent ForcedOnTrace. A22 ForcedClassificationAt includes membership in the ORIGINAL classifier output; its executable producer follows in C. General phases, admitted-move existence and accessibility normalizer are NOT produced, and D8 is NOT reproduced from general producers.

A4's first attempt was fully reverted before adding A5/A6 foldl bridges, then passed its original remaining budget at2/3. Prelude.any is a left fold, not definitionally a head disjunction. A18 passed2/3 after replacing a forced index pattern, no two-head matching. No other A rejection except the stopped A8.

## Phase transport candidate and supervisor resolution direction

Candidate (NOT checked counterexample): R moving left through a foreign actor B's contiguous extended core temporarily splits B's lifecycle-to-release interval. Later S's old all-roots ForcedRootPhase invariant may then fail. Supervisor resolution direction, NOT a proven lemma: carry phases only for the placed prefix and selected moving root; re-establish remaining phases after each outer step. Named OPEN obligation **coreContiguityRestored**: after the moved root exits a foreign block, that block's extended core is byte-identical to before. The existing L2R6 all-roots invariant types are untouched and are NOT silently inhabited using this different premise.

## B authorized fixture-first scope

Supervisor explicitly AUTHORIZED SAME-BUNDLE controls only for this bounded grammar: local history is empty at attachedC entry; inserts extend it; controls need Elem in that local history AND actual source lookup/Root parent. Cross-bundle controls remain OPEN under the named **authenticated prefix/generation history** connector. Arbitrary initial priorRoots are not admitted by the wrapper. This scoped candidate is not the full cross-bundle implementation of the original richer-control ruling.

B2 initially freshly PASSED, but its guarded commit correctly rejected an extra EOF blank line. Exact whitespace-only trim was freshly checked as V1 before commit b3746a68. This is a validation, not an extra proof attempt. Guard now records a validation receipt's attempt as "validation" (instead of indexing a nonexistent hyphen). B5 passed2/3 after a Python template token accidentally matched %search; failed bytes archived, no retained typo.

## Independent L2R6 review steering

Parent reported ACCEPT-WITH-NOTES, no proof blocker and no fixes applied by reviewer. P2 authorizes exactly three L2R6 runner HEADER-ONLY repairs naming the actual D9/predecessor/comment exceptions. Exact old/new/header-body preservation specs are in O6-L2R7-HEADER-REPAIRS.json. L2R7 artifact header also now states its true non-Idris/draft/manifest/header-repair allowlist. No inherited proof edit or Idris comment exception is authorized this shift.

## Unit B — same-bundle controls closed at authorized scope

B1–B16, a79d8557…cbc6d783,16 retained declarations. New OrderedForcedRootBundleC/ActorLifecycleOnlyAttachedC; source-aware Retire/Remove controls need local already-inserted history, and the wrapper starts EMPTY. Sound bundleIntoC/attachedIntoC inclusions (f5fe40c5/60130345). New full LocatedOpenEpisodeBlockAttachedC/BlockBeforeAttachedC keep installedness, no-other-episode, final activity and exact physical decomposition; the gap is still a native trace, not defined empty.

controlState/controlNativeExecution/controlTrace produce the ten native one-origin edges: original Begin0/Finish0/Retire1/Remove1/R/S prefix, then Retire3/Remove3, then Begin2/Finish2. controlFixture (**28c7cc15**, B13 passed2/3 after a missing direct rootControlAction import) simultaneously builds parent0 [0,8), actor2 [8,10), literal residual gap0, and BOTH ForcedRootControlInBundle dispositions. R,S,Retire3,Remove3 are INSIDE the ordered bundle. No old NeverRetired statement is changed or claimed for this controls trace.

B14/B15 define full C bundle-occurrence and NF-coverage records. B16 **attachedCZeroGapInNormalForm (cbc6d783) is PROVED**, not merely stated: residual-head coverage + C NF + UNIVERSAL physical interval separation imply zero gap between complete C bodies. These premises retain their old strength; no general producer for them is hidden in this conditional theorem. Cross-bundle controls and authenticated prefix/generation history remain OPEN. The new grammar is a checked SAME-BUNDLE Tier-1 candidate only, not the full cross-bundle ruling or a production rehome.

## Unit C — classifier/decoded catalog closed; anchor contract and one instance

C1–C12,52a22cb3…94273eaa,12 retained declarations (plus A's classifier prerequisites). **observeForcedClassification** is an executable single-constructor producer, computing the exact original classifyForced flag and carrying membership in its output plus GENERAL soundness and completeness against independent ForcedOnTrace. C1 passed2/3 by explicit eta expansion to reconcile erased proof parameters with the record's erased callback fields. classifierCorrespondenceFixtures (54545866) applies the general theorem to both full original fixtures (key R and barrier S), obtaining flags by completeness applied to independent forcing proofs, NOT scalar Refl over nested fixture builders.

adjacentOrdinalMap (2d0db779) explicitly moves crossed action right1, selected root left1, others unchanged. AnchorTransport (99c3a794) is a precise PER-ROOT CONTRACT: actual source/target trails + admitted native move; catalog root/key-seed identity and ordinal-map equations; located own-child Remove on both traces with parent/shared-key witnesses; exact anchorOf/max equations; and `afterAnchor = S (adjacentOrdinalMap cut (pred beforeAnchor))`. anchorTransportFixture (**4db206f1**) inhabits it for the genuine R-over-Begin2 instance, using the same Remove1 occurrence at3 and anchor4→4. It is NOT the hypothetical shifting-S example, nor GENERAL anchorTransport/other-root distance invariance. No raw-name-only identity or literal all-root current-cut equality is asserted.

**scanCatalogBirth (8d579501) is GENERAL and CLOSED:** any member of the catalog COMPUTED from a native AvailabilityTrace yields an actual LocatedActionOccurrence of its root insertion, with `catalogOrdinal = scanOffset + locatedActionOrdinal`. Structural trail induction, explicit Action/membership helpers and exact count transports; no supplied birth/coverage oracle and no independently reconstructed dependent states. C8 passed2/3 after correcting plusSuccRightSucc orientation. **locatedBirthBound (94273eaa)** proves the general strict trace bound. This is proof-erased constructive decoding, not a runtime decision procedure. It is the root-catalog decoder, NOT the stopped shared-key release-scan decoder.

## Unit D — general per-entry placed coverage, NF still open

D1–D8,3739b52e…435bd364,8 retained declarations, all1/3.

- **placedCatalogCoverage (fabb56e1): GENERAL PlacedBundle catalog equality → actual global AttachedBundleOccurrence** at the exact catalog ordinal. The same containing block/core/bundle are reused; the chosen birth is freshly decoded with the general C11 theorem and interval bounds are proved. It does not mistake the representative R occurrence for another root S.
- **placedRootCoverage (8c3b1141):** additionally produces filtered placed-roots membership from genuine global catalog membership and an explicit observed accepted anchor match. No rootInBundle callback supplied.
- **placedCoverageFixtures (c390795b):** both actual full originals instantiate the GENERAL theorem, producing fresh R/R/S occurrences at4/4/5. No fixture AttachedNormalForm is used or promoted.
- **GeneralAttachedNormalForm (435bd364): TYPE ONLY.** Exact real BlockBeforeAttached gap, FrontNormal, NeverRetired, all genuine ForcedRootPhase witnesses and actual same-anchor placements. All RootOrchestrationStep kinds remain in the conclusion. General produceAttachedNormalForm is OPEN: front/control occurrence exclusion, latest-birth/generation linkage and authenticated region embedding must connect all relevant gap occurrences to the per-entry kernel. This is not an arbitrary mixed-region restatement or an assumed coverage function.

**No all-premises-produced zero-gap closes** for attached or attachedC. NF alone would still need residual-head coverage and universal separation. The concrete control gap0 and new conditional C theorem are distinct checked results. General produceForcedRootPhases / produceAdmittedDistanceMove / normalizePhaseDistance and reproduction of L2R6 D8 FROM those general theorems do NOT close. No new phaseScanOk implementation, native Insert/Insert applicability proof, all-kind phase/extensional replay or accessibility recursion is claimed.

## Unit E — draft/review tooling and final validation plan

Docs-only run-l2r7-draft-sync.py preserves ALL original Tier-1 code fragments, CP3 relocation hash, predecessor checked-source hashes and all30 applied renamings. Every Tier-2 connector and signature row now states the exact L2R7 producers/residues. No global connector row is promoted wholesale. A separate exact SAME-BUNDLE controls candidate is included with source/fragment hashes and explicit cross-bundle debt. Production CP3, old statement inventory and old generator are untouched.

The exact three predecessor runner HEADER-ONLY P2 repairs preserve executable ASTs. L2R7 artifact allowlist is now accurately documented. The source-commit guard explicitly restricts validation-based declaration publication to the sole B2/V1 rstrip-only repair; V10+ cannot be used to bypass proof-slot caps. This hardening does not grant new source attempts. **25/25 compiler-free tests PASS** on actual helper/assertion ASTs: target/cap/attempt/ownership/freshness/PASS/mutation guards, no companion authority, exact header repair, stopped A8 absence, B2 whitespace, final plan hashes/order, all30 mappings and Python parsing.

E1 publishes the immutable17-target leaf-before-dependent plan BEFORE checks. All raw records remain append-only under /tmp/dgamma-l2r7. Final own-target validations and independent reconstruction are pending at this publication boundary; final E2/E3 audit updates will report their actual outcomes. No invented proof attempts after caps, no cap self-extension, no subagent launch. Parent owns the independent human proof-review gate.
