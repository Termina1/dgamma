# R193 lane2 integration boundary

## Version-pinned L2R1 results, not a source merge

Lane2 branch: `cp5-thm73-lane-a8a10`; artifact commit
`986342303cce59c27c157867cfeb124d3147a9f0`, reported source freeze
`156b4a0251f9f4a702c2b62c22b11dec10d9d8eb` and verification head
`f4581d8b88d6ee4abda33be56c4d3f7e475a543d`.

Main inspected the committed audit, ledger and independent report read-only,
and authenticated the original archive SHA256:
`b7f3a8272c5046fc08ec1457aed09064e01e9577dcf5d7d525a90ba88491bfbd`.
Copies of the reports and their inspection manifest are in the R193 evidence
archive. The original compiler archive remains at the versioned lane2 commit;
no lane2 Idris source was edited, merged or copied into main source directories.

L2R1 reports **35 retained declarations (B23/C12), 15 final checks**, 68 total
invocations: 51 matched outcomes / 17 non-PASS, including 5 interruptions.
These are lane2 report results, not main reruns or certificates for later L2R2
sources. Parent-owned manual review remains required.

### B — retained, still partial

Native foreign-child lookup framing and early Retire applicability; two actual
R191 Begin1/Retire3 and Finish1/Retire3 exchange squares; a relocated eleven-edge
trace spliced from those squares, with literal original `r191ChildGapState 11`
endpoint. Parent block `[3,7)` contains Retire3 moved from ordinal8 to6; remaining
blocks are `[7,9)` and `[9,11)`. Both physical orders and zero-gap results quantify
over every authentic `BlockBeforeExtended` witness for their concrete pair.

General Retire/Remove commutation and iterated foreign-run replay remain open.
The generic zero-gap cancellation result still consumes coincident physical
cut counts; extended grammar alone does not generally produce gap0. B24 exhausted.

### C — retained, still partial

Availability root-exchange splicing, executable root-birth inversion count and
an exact-one decrease for an authenticated Begin/root-insert square, arbitrary
prior lifecycle count and authentic suffix. The square remains an input; no
whole availability-normalization producer is supplied. Three copied whole
interfaces are defined, without general producers of their inhabitants.
An actual R174-shaped collision execution is checked, but L2R1 does not inhabit
full old-reject/new-admits availability-placement packages. C9/C17 exhausted;
C6/C11 parked, C10/C18–C20 blocked in that stage.

## Exact copied-definition inventory

| Original coordinate | Research counterpart | Scope/status at L2R1 |
|---|---|---|
| CP3:1786 `ActorLifecycleOnly` | `CP5ActorLifecycleOnlyExtended:17` | Inherited, unchanged; actual-source own-child Retire/Remove admitted |
| CP3:1824 `LocatedOpenEpisodeBlock` | `CP5ActorLifecycleOnlyExtended:86` | Inherited extended block, exact cuts/opening/installed/no-other-lifecycle retained |
| CP3:1873 `BlockBefore` | `CP5L2R1ExtendedZeroGap:18 BlockBeforeExtended` | New extended physical order; no gap0 assumption |
| CP3:3156 `CanonicalInputPlacement` | `CP5AvailabilityAwarePlacement:104 AvailabilityAwareCanonicalInputPlacement` | Inherited R178 earliest-compatible interval, root order, freshness, own lifecycle order, retained child clause |
| CP3:3240 `CanonicalSchedule`; `canonicalBlock:3256–3257`, `inputPlacement:3265` | `CP5L2R1PlacementCopies:20 AvailabilityCanonicalSchedule` | Extended blocks/order + R178 placement; other fields retained; original trace parameter |
| CanonicalSort:93 `CanonicalSupportTransport` | `CP5L2R1PlacementCopies:56 AvailabilityCanonicalSupportTransport` | Original-order witness retained; placement transported relative to same source trace |
| CanonicalSort:118 `SortedClosingFreeTrace` | `CP5L2R1CanonicalSortCopies:24 AvailabilitySortedClosingFreeTrace` | Extended block/order/ranges + R178 placement; replay/invariants/endpoint/no-withdrawal/registration fields retained |

No other placement-dependent producer or protected O17/O20/O21 interface was
copied in L2R1. No coercion back into strict frozen placement is supplied.
The CP3 canonicalBlock citation correction to3256–3257 is retained.

## Required protocol qualifications

- Initial S0-1/S0-2/B1-1 RSS unavailable because the old classifier omitted
  `chez`; not zero-memory measurements.
- C9-1 source was mutated while its compiler was active after a rejected commit
  guard. Only its own process group was interrupted; no PASS or commit resulted.
  Identity of the prelaunch snapshot with bytes actually compiled is NOT asserted.
- C10-1 preflight rejected two declarations before launching any compiler;
  conservatively consumes attempt1, with no preflight source snapshot.
- Later C9/C11 interruptions are not rejection proofs; C17-3 exit0 with an
  import error was correctly non-PASS. No failed declarations remain retained.
- Lane2 fetched a later paper revision (Confluence80); it did not substitute that
  revision for frozen paper/CP3 authority (Confluence73).

## L2R2 and main D boundary

At23:39Z, L2R2 was observed at `141a1d802cea0a9ad3e492719d7fa0e050ad7311`
(C20 commit). Its final authenticated handoff was still pending. No L2R2 closure
or final-validation result is inferred from commit messages. Later findings
must be recorded in an explicit addendum, not retroactively credited to L2R1.

Main D's whole-block resolver induction uses frozen `ActorLifecycleOnly`
(lifecycle / yielded Insert), **not** this extended Retire/Remove grammar.
It has not been ported to these copies. Main's baseline B/C checks authenticate
main baseline bytes only, and cannot certify either lane2 stage.
