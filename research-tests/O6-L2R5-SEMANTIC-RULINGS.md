# L2R5 semantic scope ruling

Start guard: exact 769d332dd520c494fff7ffbf2d4e8c9f43c72e7d, clean tree, branch cp5-thm73-lane-a8a10, worktree dgamma-lane2. All predecessor sources and production remain frozen. No companion bundle authorized.

## Supervisor response to the pre-source semantic gate (verbatim)

> SEMANTIC RULING — all three readings are correct; here is the constrained schedule domain (no redefinitions of inherited types): (1) LEGALITY INVARIANT = admissible CURRENT cut (the root's declared provisions are declaration-free at its current cut, and no root-orchestration step of ANOTHER root is crossed), transported along moves; EarliestAvailableRootBirth is the TERMINAL property (asserted only at count 0), never an invariant. (2) ITERATION STRUCTURE = two levels: OUTER induction over root inputs in ORCHESTRATION ORDER (place roots one at a time; roots earlier in the order are already in place, so between a root's target position and its current position there is never an unplaced root-orchestration step — the paper's "in their original order" clause); INNER measure = DISTANCE (number of steps between the root's target position and its current position; target = end of the front region for non-forced roots, end of its anchor bundle for forced roots). Each admitted one-step left move crosses either a foreign lifecycle step or a foreign actor's own-child ORetire/ORemove/child-OInsert (independent by the squares) and decreases the distance by EXACTLY one, regardless of step kind. The L2R4 anchor count stays the PAPER-FACING inversion measure (distance ≥ anchor count; count 0 at distance 0); do not claim the count alone drives the iteration — audit the [Life, unrelated ORetire, Birth] example as the reason. (3) COVERAGE SCOPE = FRONT-NORMAL traces: all orchestration of NON-forced roots, INCLUDING root ORetire/ORemove of fibers outside A (they take no lifecycle step, per the paper), precede the first lifecycle step; under that premise (produced by the outer normalizer, stated explicitly), every root-orchestration occurrence inside an inter-block gap is a forced OInsert belonging to a bundle → general AttachedNormalForm producer for gap regions. Unrestricted mixed-region NF: PARK (structural, correct). Deliverables: annotation transport + admissible-cut transport (A(i)); outer/inner iteration with the distance measure on the variant (A(ii)), fixtures (A(iii)); zero ⇔ attached normal form for iteration-produced traces (A(iv)); Unit C under the front-normal premise. The CP3 draft must expose exactly these scope obligations (front-normal form, orchestration-order outer induction, distance vs inversion measure).

## Precision obligations retained

1. `EarliestAvailableRootBirth.noEarlierCompatibleRootCut` cannot hold before an admitted left move. It is not the current-cut availability invariant. Terminal earliest still needs an actual all-earlier-cuts proof; zero anchor inversions alone is insufficient.
2. A positive anchor count need not expose an adjacent lifecycle/root pair. The word `Life actor; Other (unrelated ORetire); Birth forced` has a positive foreign-lifecycle count but no such adjacent pair. Ordered orchestration must not be reversed to hide this obstruction. Distance descent needs genuine independent native squares for every permitted crossed action, not an assumed per-step replay oracle.
3. `RootOrchestrationStep` includes root Retire and Remove, whereas `OrderedForcedRootBundle` admits root Insert only. Unrestricted mixed-region catalog-to-NF is structurally parked. Front-normal form and inter-block region authentication must exclude non-insert root orchestration and non-forced roots; these are obligations to produce, not fields to assume invisibly.
4. Fixed original release IDs must not be recomputed from changed current ordinals. The existing native annotator uses `S ordinal` for each Remove and an ordinal/name root assignment function. Transport must authenticate stable event identities across the real move, including any moves across child Remove, rather than assert identical numeric annotations by fiat.

## Resource and evidence plan

Copied L2R4 runner guards; L2R5 paths/caps/clock, exact unchanged V0 bootstrap target only. Checks detached under Python -I, target-only mtime touch, own Building line, source SHA sampling, one lane compiler. No heavy invocation intended. Main-lane compilers are separate-worktree processes, never signalled. Immutable final validation plan and independent compiler-free evidence reconstruction will precede publication. Artifact commits do not constitute mathematical proof.
