# L2R5 semantic scope ruling

Start guard: exact 769d332dd520c494fff7ffbf2d4e8c9f43c72e7d, clean tree, branch cp5-thm73-lane-a8a10, worktree dgamma-lane2. All predecessor sources and production remain frozen. No companion bundle authorized.

## Supervisor response to the pre-source semantic gate (verbatim)

> SEMANTIC RULING — all three readings are correct; here is the constrained schedule domain (no redefinitions of inherited types): (1) LEGALITY INVARIANT = admissible CURRENT cut (the root's declared provisions are declaration-free at its current cut, and no root-orchestration step of ANOTHER root is crossed), transported along moves; EarliestAvailableRootBirth is the TERMINAL property (asserted only at count 0), never an invariant. (2) ITERATION STRUCTURE = two levels: OUTER induction over root inputs in ORCHESTRATION ORDER (place roots one at a time; roots earlier in the order are already in place, so between a root's target position and its current position there is never an unplaced root-orchestration step — the paper's "in their original order" clause); INNER measure = DISTANCE (number of steps between the root's target position and its current position; target = end of the front region for non-forced roots, end of its anchor bundle for forced roots). Each admitted one-step left move crosses either a foreign lifecycle step or a foreign actor's own-child ORetire/ORemove/child-OInsert (independent by the squares) and decreases the distance by EXACTLY one, regardless of step kind. The L2R4 anchor count stays the PAPER-FACING inversion measure (distance ≥ anchor count; count 0 at distance 0); do not claim the count alone drives the iteration — audit the [Life, unrelated ORetire, Birth] example as the reason. (3) COVERAGE SCOPE = FRONT-NORMAL traces: all orchestration of NON-forced roots, INCLUDING root ORetire/ORemove of fibers outside A (they take no lifecycle step, per the paper), precede the first lifecycle step; under that premise (produced by the outer normalizer, stated explicitly), every root-orchestration occurrence inside an inter-block gap is a forced OInsert belonging to a bundle → general AttachedNormalForm producer for gap regions. Unrestricted mixed-region NF: PARK (structural, correct). Deliverables: annotation transport + admissible-cut transport (A(i)); outer/inner iteration with the distance measure on the variant (A(ii)), fixtures (A(iii)); zero ⇔ attached normal form for iteration-produced traces (A(iv)); Unit C under the front-normal premise. The CP3 draft must expose exactly these scope obligations (front-normal form, orchestration-order outer induction, distance vs inversion measure).

## Precision obligations retained

1. `EarliestAvailableRootBirth.noEarlierCompatibleRootCut` cannot hold before an admitted left move. It is not the current-cut availability invariant. Terminal earliest still needs an actual all-earlier-cuts proof; zero anchor inversions alone is insufficient.
2. A positive anchor count need not expose an adjacent lifecycle/root pair. The word `Life actor; Other (unrelated ORetire); Birth forced` has a positive foreign-lifecycle count but no such adjacent pair. Ordered orchestration must not be reversed to hide this obstruction. Distance descent needs genuine independent native squares for every permitted crossed action, not an assumed per-step replay oracle.
3. `RootOrchestrationStep` includes root Retire and Remove, whereas `OrderedForcedRootBundle` admits root Insert only. Unrestricted mixed-region catalog-to-NF is structurally parked. Front-normal form and inter-block region authentication must exclude non-insert root orchestration and non-forced roots; these are obligations to produce, not fields to assume invisibly.
4. Fixed original release IDs must not be recomputed from changed current ordinals. The existing native annotator uses `S ordinal` for each Remove and an ordinal/name root assignment function. Transport must authenticate stable event identities across the real move, including any moves across child Remove, rather than assert identical numeric annotations by fiat.

## Resource and evidence plan

Copied L2R4 runner guards; L2R5 paths/caps/clock, exact unchanged V0 bootstrap target only. Checks detached under Python -I, target-only mtime touch, own Building line, source SHA sampling, one lane compiler. No heavy invocation intended. Main-lane compilers are separate-worktree processes, never signalled. Immutable final validation plan and independent compiler-free evidence reconstruction will precede publication. Artifact commits do not constitute mathematical proof.

## Revised endpoint ruling (verbatim)

> RULING: correct diagnosis — ordered-bindings snapshot equality is a REPRESENTATION artifact (insertBinding prepends), not a semantic obstruction; two commuted inserts of distinct names are registry-equivalent. Do NOT park the iteration and do NOT constrain moves to avoid child inserts (that would leave real gaps). REVISED ENDPOINT RELATION for the NORMALIZER ITERATION ONLY (existing squares keep their snapshot codomain): define `RegistryExtensional` = world equality + pointwise `lookupFiber` equality for every name (state it as a record with erased fields; add the derived declared-key-occupancy/memberKey agreement as a lemma, not a field). Obligations, in this order, each a NEW statement: (1) snapshot equality ⇒ RegistryExtensional (so all existing squares feed the iteration); (2) the root-OInsert / foreign child-OInsert exchange concludes RegistryExtensional (both alternate checked edges from the original edges; distinct names) — this is the paper's Lemma 71(2) orchestration/activation-registration exchange; (3) TRANSPORT: a native checked step (checkedApplyAction) at RegistryExtensional-equal states yields the same tag and RegistryExtensional-equal results — prove per action kind via the lookup/occupancy predicates it reads; if a kind is a wall, stop that kind at 3 and record the residue; (4) the iteration's endpoint claim becomes RegistryExtensional (state that the frozen ConfluenceResult's relations are pointwise, so this is the faithful relation; record the ordered-bindings artifact in the audit and the CP3 draft as a note for the owner). Fixture instances: use the ALTERNATE six-/seven-edge prefixes (Begin2 before R) as the iteration inputs, ending at the attached positions; the original fixtures are already normal — say so. Keep the checked A(i) commits (e9abbd95, 5822f06e, cb60b7d7). Then B observed frame and C under front-normal scope as ordered.

## A11 stop and ONE new observed statement authorization (verbatim)

> RATIFIED: A11 twoHeadLookupRightObserved 3/3 stop (reduction of nested decEq under the No branch). Next branch = continue A12–A20 with ONE permitted NEW statement in the OBSERVED-VALUE shape for the two-head lookup (not a restatement): take both decEq results as explicit observed arguments with equations — `(d1 : Dec (n = r)) -> (decEq n r = d1) -> (d2 : Dec (n = c)) -> (decEq n c = d2) -> Not (r = c) -> lookupFiber n (insert r (insert c reg)) = lookupFiber n (insert c (insert r reg))` — and case on d1/d2 only (Yes/Yes excluded by distinctness); 3 attempts; if it dies, that seam is closed for the shift (two 3/3). Then the extensional congruence/transport lemmas for the action kinds that read only lookups/occupancy (Insert, Retire, Remove first; Begin/Advance via their observed producers). HARD TIME RULE: reserve ≥60 minutes before the attempt cutoff for Unit D (CP3 diff draft) — it is the owner's deliverable and must not be squeezed out; B (observed lifecycle frame) and C (front-normal coverage) take whatever remains between A's cap and D's reservation, in that order. No heavy checks.

A12 used this exact authority for an actual inserted-registry statement with both observed Dec arguments/equations and produced alternate freshness. It passed A12-2 (0efb83c1). A11 remains absent/exhausted; the authorization is not reusable for another statement.

## B5 provider UNIT STOP and bounded C scope (verbatim)

> RATIFIED: B5 observed-provider producer 3/3 stop + full revert; the provider seam is now closed for lane work (two 3/3 in that seam across L2R4 B7 and L2R5 B5 = UNIT STOP). Record precisely: B3's executable RetirementProviderFrame and B4's ProviderHeadObservation stay as retained capital; the residue is a hidden dependent index in the native retired-head guard that survives every explicit split — this is a candidate for a MAIN-lane fix at the guard's definition (a producer-owned observed guard in the base module), not lane work; put it into the CP3/R196 notes as "lifecycle replay roles need an observed retired-head guard at the source". Advance to C with the honest scope you propose: produce the actual runtime root-insert scan + lookup-completeness (catalog FROM the trace) and prove it on both fixtures; do NOT call a raw catalog an AttachedNormalForm and do NOT fabricate front-normal/placed-gap premises — state the exact structural input the assignment needs from the (open) normalizer. Then Unit D with its ≥60 min reservation intact.

## D two-tier exact draft boundary (verbatim)

> RULING on Unit D boundary: the honest partial exact-draft boundary is accepted AND I want the connectors spelled out — but in two clearly separated tiers. TIER 1 (exact, checked-backed): the proposed production text for (a) the attached grammar incl. AttachedRelease/OrderedForcedRootBundle with the REORDERING note (transitionCount/LocatedActionOccurrence/locatedActionOrdinal must move before 1786 — list the exact moved ranges and confirm no semantic change), (b) LocatedOpenEpisodeBlock/BlockBefore attached copies, (c) the availability-aware placement record as inherited (R178) — each line-referenced to the CP3 origin and to the checked research declaration + commit that backs it. TIER 2 (UNCHECKED specification clauses, explicitly labeled "NOT YET SIGNABLE — no compiled definition exists"): the front-normal classification, the least-closure forced set as a TRACE predicate (linking ForcedRootInput to actual root occurrences), the last-release/anchor assignment, and the bundle-placement linkage into CanonicalInputPlacement — write them as proposed Idris record/field TEXT (so the owner sees the shape), marked UNCHECKED, with the exact research obligation each one waits on (iteration/existence/coverage/selector zero-gap projection). Add a one-page "signature status" table: for each proposed CP3 change → backed-by (commit) / unchecked / blocked-by. The owner must be able to see at a glance what a signature today would and would not certify. Do not invent compiled definitions in the lane to fill Tier 2.
