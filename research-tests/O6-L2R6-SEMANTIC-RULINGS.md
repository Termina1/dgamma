# L2R6 supervisor rulings

## Parent steering: L2R5 review P2
Independent L2R5 ACCEPT-WITH-NOTES, no proof blocker. Unit E must add the four actually applied placement renamings missing in serialized REHOME-MANIFEST and keep generator/manifest in sync.

## Provisional L2R7, NOT this shift
1. Richer forced-root bundle grammar: ordered OInsert with release/barrier witness, ORetire/ORemove with already-bundled witness (this or earlier bundle). Fixture: barrier trace extended by Retire3/Remove3 after S. No L2R6 implementation authority.
2. Interim scope: ForcedRootNeverRetired for quiescent traces; open-set/lifecycle argument remains a producer obligation. Current insertion-only attached body has no post-bundle interval for a control.

## Phase-premise gate
YES — state the general obligation TYPE with the explicit per-forced-root PHASE premise ("the assigned anchor release lies after a located lifecycle/core step of the releasing actor", i.e. the release is a genuine own-child ORemove inside an actor's extended core, not a pre-lifecycle removal of an initially-retired child), with its producer left OPEN and named; do NOT assert an unrestricted theorem you know is insufficient.

D8 milestone accepted as stated: real native 1→0 and 2→1→0 iterations with RegistryExtensional endpoints. D9–D12 remain within cap; no cap extension.

## D9 body-only companion authorization
AUTHORIZED — D9 as a gated body-only companion bundle (L2R2 C4 precedent): ONE new declaration (ForcedRootPhase) + body-only change to L2R6Anchors.targetPosition with the corrected target = max(anchor + same-anchor rank, one past the latest EARLIER different-anchor / non-forced root birth); both files hash-recorded at launch, both own Building lines, one guarded receipt with both hashes; the fixture values 0/0/1/2 must be re-checked as part of the same bundle (any drift = revert). Record the rationale verbatim: external order forbids crossing an earlier root birth, so the target must not lie before it; in the phase + FrontNormal + NeverRetired domain non-forced controls are at the front and same-anchor roots are ordered by the outer induction. This counts as D9 within cap; no B extension. Then D10–D12 for the general existence/iteration TYPE under the phase premise and, if a slot remains, fixture instantiation; then E.

Runner bundle has three source snapshots: new phase target, body-only Anchors, and byte-unchanged PlacementFixtures (freshly touched and rebuilt to enforce the numerical fixture contract). No predecessor source/body bundle is authorized.
