# R207 semantic steering record

The initial requirement was an unconditional repaired producer and a fresh
CrossTrace PASS. The source grammar expansion does not imply the old legacy
classifier. Supervisor steering during this shift explicitly permitted the
following **checked-partial** migration (paraphrase, not a verbatim quotation):

- CrossTrace's statements, body, and two O20 holes stay byte-identical. It is not
  given legacy premises and no stale TTC is accepted as a proof.
- Before E, inventory exact unconditional requirements. The 36 original types
  were archived from `fec95252` in `O6-R207-CROSSTRACE-NEEDS.json`.
- E may prove the total expanded source observation and the actual transport /
  primitive swap lemmas that follow; a source pair must not store the desired
  diamond, swapped result, replay guard, or global commutation assumption.
- After E26, remaining Unit0 slots may honestly migrate unfrozen WholeBlock /
  Reached consumers to explicit legacy-restricted results. Keep the original
  unconditional claims as TYPE-only OPEN obligations with **no inhabitant,
  postulate, or hole**. Where an original signature mentions the unavailable
  unconditional Cartesian run, quantify/package a hypothetical expanded run
  and preserve the same-run linkage. Exact original types remain archived.
- The result is not promoted to the frozen CrossTrace boundary. A fresh native
  failure there is the honest frontier unless the unconditional inventory closes.
- Minimal pruning of lexically unused imports in MAIN-owned R204 consumers is
  permitted, with per-module evidence and at most two checks. No frozen import
  pruning/splitting and no removal of genuinely used CrossTrace exports.

No authorization to edit production, frozen files, the four hole declarations,
E's 26-slot cap, or the original exhausted A15 statements is implied. The one
new-premise A15 restatement remains gated on its genuine trusted context; it is
not relocated into a smaller module. Independent reviewer / final supervisor
gate still required; this file is not a final acceptance ruling.

## Final U010 ruling (verbatim supervisor reply)

> APPROVED for U010 (≤3 attempts): migrate the five MAIN-owned unfrozen consumers in CP5O20BlockResolverFrameSpike to the explicit LegacyActorOnly premise (same pattern as RightOpeningTransport), preserving all five expanded unconditional statements in ONE TYPE-only obligation record (no inhabitant, OPEN docstring) + archived pre-migration text; nameEq threaded explicitly; no CrossTrace/production/frozen change. If it dies 3/3, revert and keep the exact fresh BROKEN record. Then Unit 0 is at cap; proceed to docs/validation (changed + dependents + package) and the gate; an external reviewer will be launched at your gate.
