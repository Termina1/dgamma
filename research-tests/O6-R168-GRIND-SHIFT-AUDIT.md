# O6 R168 — semantic heads discharged; readiness/tag seals assembled; ordinal-path 3/3 stop

## Baseline, scope, and mandatory stop

Started **2026-09-05 22:08:56 UTC**, branch `cp5-thm73-scoping`, at the required
HEAD **`8c7f30d3e528adad1c7a196ec2867acf8e7a18b7`**. The initial tree contained
only permitted untracked `paper/` and `review-o6-body-adversarial.md`; the
process scan found no Idris compiler. Idris reports **0.8.0**. Before the first
check, `idris2 --help` verified `--source-dir <dir>` and `--check`. Read R167
first (all 651 lines), then R166, R165, and the R146 strategy memo. Read the
extracted paper in full; ranges hidden by output truncation were reread in
bounded chunks.

**STOP at B48, 3/3**, final failure **2026-09-06 00:18:14 UTC**. Restored the
entire failed `scopedOrdinalPermutationPath` declaration and the third attempt's
trial spelling of the already-checked B47 producer. Safe proof HEAD is
**`8248ea2`**. No further proof or visibility attempt followed the stop; only
fresh validation and this audit. The failed worker is absent from retained
source and from the freshly regenerated DeletionChain TTC. This is an
**elaboration stop**, not a semantic counterexample, structural impossibility,
or permission to change the R146 route.

Unit A is **COMPLETE**: live folds emit `ScopedEnrichedHeadReplays`, and the
full TARGET bundle assembler is now unconditional on semantic-head capital.
Unit B has assembled all twelve readiness/subsequence-tag clauses and proved
the whole RuleTag seal at the **frozen** origin function. Its finite ordinal
permutation algebra is checked, but it has **not** yet supplied the global
registration-generation bijection and generated-ordinal equation of operational
capital. O9/O10/O11 are unchanged, **0/3** each. Hole delta **0**.

The four-hour timeout is **02:08:56 UTC**, the no-new-attempt boundary is
**01:28:56 UTC**, and the mandatory safe-gate deadline is **01:53:56 UTC**.
The budget stop occurs well before all three; time remaining does not authorize
another B48 attempt or another proof lane.

Changed paths only:

- `research/DGamma/CP5ConfluenceDeletionChainSpike.idr`;
- `research/DGamma/CP5ConfluenceLocalDiamondSpike.idr` — four authorized
  visibility keywords only;
- this audit.

Production `src/` and `dgamma.ipkg` remain byte-identical to `34b21c9`.
No frozen `deletionTheoremProof` call, raw-name-global exclusion cast,
O14/O17/O19 body, O21 withdrawal branch, or route change. No new `with`, let
alias, hole, postulate, `believe_me`, `assert_total`, or partial definition.
`%default total` remains in force. The original nine DeletionChain with-blocks
are unchanged. No subagent was launched.

## Unit A — COMPLETE: producer-emitted full semantic replay

### Local semantic construction

`scopedConcreteAdvanceStageFamily` constructs the existing singleton-stage
family by calling the checked LocalDiamond locator at the actual two owner
cells. The cells share their component, remaining program and committed view;
parent/retirement equalities are explicit, and the accumulators and tables are
**not** equated.

`scopedReloadingCellSame` transports the actual target lookup through the three
control equalities. `scopedLifecycleAdvanceStageFamily` performs one elimination
of `LifecycleControlRelated`: Reloading uses the concrete stage family;
Inactive/Active/Unloading contradict the source's accepted LAdvance through
existing raw evaluator equations. `scopedOwnersAdvanceStageFamily` separately
eliminates the fiber-control package.

**`scopedAdvanceReplay` (`34b86fc`) proves complete singleton RAR**, including
arbitrary related-input generator maps and exact same-input outcomes for every
reachable iterator stage. It combines the stage family with
`pointwiseRelatedLifecycleMaps` and `singletonAdvanceRAR`. This is stronger
than equality of the one tag or one actual outcome observed while firing.

`scopedLifecycleReplay` dispatches all lifecycle actions. The tag and named
wrappers retain the separate stored/supplied checked equations introduced by
R166: the stored proof indexes the actual `Fired`; no equality between proof
terms is assumed. `scopedNamedActionReplay` adds the orchestration cases.

### Actual owner extraction and fold integration

Target lifecycle lookup is produced by the existing successful-action theorem,
not guessed from a raw name. The target cell is located in the ordered planned
registry, reflected through the inactive-leaf plan to the **same exact source
cell**, and related controls authenticate its component/program/view. This
reflection suffices for semantic replay even where deleting the selected
effects means that the whole source and survivor states are not effect-equal.

| Local consumer | Source of owner controls / exclusion | Commit |
|---|---|---|
| `scopedRelationalNamedReplay` | Full ordered controls at the actual relational boundary | `f4a1a57` |
| `scopedSelectedNamedReplay` | Selected foreign controls; kept selected lifecycle is impossible via `DeleteEpisodeGenerationLifecycle` | `4541934` |
| `scopedPostCloseNamedReplay` | Post-close foreign controls, with the actual dispatcher's lifecycle-foreign evidence | `75713d3` |

The post-close dispatcher passes its existing foreign-owner proof for foreign
steps, and the selected O-Retire case supplies the impossibility of its being a
lifecycle action. Selected O-Remove uses its orchestration singleton. Selected
L-Begin continues to switch the **whole head plus suffix** to the enriched
relational fold, whose actual pre-boundary is already authenticated by the
existing clean-Inactive producer. No false foreign-owner claim is made there.

The fold-output clauses are erased certificates at the canonical ready trace:

| Field / join | Status | Commit |
|---|---|---|
| `postCloseOutputSemantic` | Live post-close **and relational** kept/deleted/end producers emit the exact head family | `819d509` |
| `interiorOutputSemantic` | Live selected interior kept/deleted/end producers emit the exact head family | `b3d7e5a` |
| `scopedAppendSelectedCloseSemantic` | Single-ends elimination retains every kept semantic head while deleting the close | `9a761d1` |
| `selectedOutputSemantic` | Selected closed assembler retains the family across deleted opening and close | `3beeec6` |
| `scopedEnrichedHeadReplays` | Constructs `MkScopedEnrichedHeadReplays` by projecting the actual selected/suffix fields | `6b3c777` |
| `scopedEnrichedTargetFromHeads` | **No head-certificate argument remains**; full unchanged TARGET `ReplayInvariantBundle` at exact enriched `DeletionResult` | `fb52b0d` |

All existing surviving-trace registration discipline, parent controls, target
totality, alignment, quiet/noFailure and well-formedness clauses remain intact.
The internal `scopedEnrichedTargetBundle` is still a lemma accepting RAR;
`scopedEnrichedTargetFromHeads` now discharges that input itself using the live
fold certificates. “Unconditional” here means no **extra** semantic-head
premise: it still takes the original premises, candidate and producer-owned
folds. It does not claim that O9's other capital has been assembled.

A15 moves already-checked semantic dependencies before their output-record
consumers. It is an order-only DeletionChain surface unit, not new proof capital
and not a LocalDiamond order edit. A16/A17/A19/A21 are connected surface
migrations with no new top-level declaration. Unit A used **21/21 successful
micro-units, 1/3 each**, plus the separate visibility unit E1.

## Unit B — readiness and whole tags COMPLETE; generation permutation PARTIAL

### Twelve readiness/subsequence clauses assembled

`ScopedReplaySeal` stores readiness, its constructor-by-constructor tag family,
and an exact endpoint proof together. `scopedVerbatimHeadSealAt` uses the
actual observed `fireNamed` value and equation; a successful named endpoint is
transported to the source endpoint by the evaluator's exact pair equation,
without identifying its stored checked proof with the source's checked proof.
The prefix induction uses source alignment, the exact source generation scan,
and the selected births' lower bound. It does not substitute an arbitrary
ready trace for the original source prefix.

`scopedReadyCanonicalEnds` constructs `ReplayReadyEndsAt` at the canonical
`scopedReadyFinal`, including each producer-owned kept endpoint. The exact
before subsequence's tags are separately proved by induction on its original
`generationPrefixIdentitySubsequence` producer; no public result is treated as
if it implied tags on its own.

**`scopedEnrichedReadinessSeals` (`59191b4`) assembles all twelve clauses at
`scopedEnrichedDeletionResult ... aligned`:** before/center/after readiness,
ready tags and endpoints (nine clauses), plus the three exact frozen
subsequence tag families. `ScopedDeletionReadinessSeals` is a packaging lemma
for those twelve clauses, not a substitute for the stronger unchanged
`DeletionProducerOperationalCapital`.

B5 originally used a nested pair pattern under its dependent-pair elimination.
H1 (`eab97c6`) immediately normalizes it to one dependent-pair elimination and
`fst`/`snd` projections, under a separate fresh check/commit. This is disclosed
as a protocol-hygiene correction, not hidden as documentation work or omitted
from the invocation ledger. No retained new helper uses that nested pattern.

### Whole-origin RuleTag seal proved at the frozen function

`ScopedRuleAt` is a finite position witness retaining the actual transition's
tag. Separate head and tail projections prove tag uniqueness at one ordinal.
Append lemmas preserve exact prefix offsets; `scopedLocatedRule` uses the
located occurrence's **stored trace equation** and its exact prefix count.

`scopedSubsequenceOrdinalTags` turns the executable
`generationSubsequenceSourceOrdinal` equation and the stored tag family into a
shared tag and proofs at both segment positions. The keep/delete cases use
`scopedMapSuccEliminate` for explicit count equalities where indices differ.
It does not infer that an arbitrary existential occurrence must be the frozen
one merely because their actions agree.

The before/center/after tag-pair joins use each segment's real offsets. The
after case transports both associations of the two count sums explicitly.
`scopedDeletionEmbeddedTags` then proves the paired tag property of every
**`DeletionSurvivingOrdinalEmbedding`** constructor.

**`scopedDeletionWholeTags` (`597ba52`) seals the actual frozen
`deletionWholeSourceOccurrence (deletionWholeOccurrenceOrigin ... occurrence)`**.
It consumes that producer's stored ordinal embedding and uses tag uniqueness
to compare the two actual located occurrences. No occurrence map was replaced,
no opaque source occurrence was reconstructed, and the frozen origin function
was not edited. The theorem is applicable directly to
`scopedEnrichedReadinessSeals`; it is not a premise deferred to an O9 caller.

### Finite permutation algebra — checked constructor, missing path equation

B31–B47 build honest, total numeric capital:

- `ScopedOrdinalPermutation` stores executable forward/backward maps and erased
  two-sided inverse laws. Composition and fixed-head lifting are proved.
- `scopedFlipOrdinal` swaps zero and one and is proved involutive, using
  separate single-elimination workers.
- `scopedRotateOrdinals bound` is a finite composition of those transpositions
  and lifts. `scopedRotateSuccessor` proves it sends `S index` to `index` for
  `index < bound`.
- `ScopedOrdinalSpine` carries exact source/target lengths. Its append operation
  adds those lengths. `ScopedOrdinalPath` carries an exact kept source/target
  position; `scopedOrdinalPathBound` proves its target is within the survivor.
- **`scopedOrdinalSpinePermutation` (`8248ea2`) constructs a permutation for any
  such finite shape**: Keep lifts the tail permutation; Delete rotates the
  retained interval after lifting the tail permutation.

The B47 commit's “global finite position permutation” refers to a total
permutation of ordinal space, **not** to an already-produced permutation of the
live deletion result's registration generations. Its inverse laws are checked,
and construction is by finitely many finite rotations. A separate support-bound
theorem was not opened. No `RegistrationGenerationBijection` has yet been
constructed from this algebra at the exact live result.

B48 was intended to prove

```text
(sourceCount, targetCount : Nat) ->
(spine : ScopedOrdinalSpine sourceCount targetCount) ->
(sourceIndex, targetIndex : Nat) ->
ScopedOrdinalPath spine sourceIndex targetIndex ->
ordinalForward (scopedOrdinalSpinePermutation spine) sourceIndex = targetIndex
```

This is a precise description of the removed theorem, not a retained signature
or new hole. Keep-Here/Keep-Later elaborate; the Delete-Later clause fails at
conversion of the producer's forward map. Attempt 1 used explicit constructor
indices with an implicit theorem telescope. Attempt 2 made every count, spine,
and position argument explicit and split the nested congruence. Attempt 3 tried
an explicit single `case spine` spelling of the already-checked B47 producer;
it exposed the generated **`case targetCount of ...`** in the diagnostic rather
than curing the conversion. All three errors are quoted below. Both attempted
surfaces were completely restored at the stop.

The exact remaining producer chain is therefore:

1. cure the B48 producer-equation/index-elimination boundary under a fresh
   owner-authorized design, without a new with-block or an unchanged fourth
   attempt;
2. derive numeric spines/paths from each actual generation subsequence and
   append the three paths at their exact source/target count offsets;
3. obtain the frozen whole origin's source ordinal from its stored embedding,
   lift the numeric permutation to `(raw name, birth ordinal)`, and emit the
   generated-registration equation at **that** origin;
4. combine it with the proved readiness and whole-tag seals to construct the
   unchanged operational capital; only then open O9.

A producer-owned equation view or a runtime shape whose numeric indices are
stored with explicit count equations is a candidate next design, **not a proved
fix or permission granted in this shift**. The wall does not demonstrate a false
semantic statement or justify unfreezing production. The R146 verbatim
route-change condition was not met.

## O9 / O10 / O11 and fresh census

| Unit | Attempts | Outcome |
|---|---:|---|
| O9 `enrichDeletionChainStepSpike` | **0/3** | Not opened: exact global generation permutation/ordinal seal and complete operational package remain absent. |
| O10 `deleteClosingEpisodesCoreSpike` | **0/3** | Not opened; depends on O9. |
| O11 `assembleClosingFreeAccountingSpike` | **0/3** | Not opened; depends on O10. |

No O9/O10/O11 closure commit exists. Fresh census is unchanged:
**10 = CanonicalSort 2 / CrossTrace 4 / DeletionChain 3 / LocalDiamond 0 /
RenamingComposition 1**, delta **0**. All original hole names and statements
are unchanged. This prefix does not prove Theorem 73 or a complete deletion
chain.

## Authorized LocalDiamond visibility — exact four-keyword diff

Before any visibility edit, a blocking decision identified the private stage
records and their dependent constructor/projection use. The supervisor replied:

> RULING: AUTHORIZED. The R167 ruling anticipated exactly this case
> ("public export ONLY where a consumer must reduce through the definition —
> state which and why"): records whose constructors/fields a DeletionChain
> consumer must construct or project require public export, and the
> stored-projection reduction you describe is the legitimate reason. Scope:
> `public export` on the TWO existing record declarations
> SingletonAdvanceStageReplayFamily and LocatedSingletonAdvanceStageReplay;
> plain `export` on singletonAdvanceRAR and locateSingletonAdvanceStageReplay.
> Conditions unchanged: zero body/signature/order changes, no new LocalDiamond
> declarations, one visibility micro-unit = one commit after a fresh SEEDED
> LocalDiamond check, every name listed with its DeletionChain consumer and the
> reduction justification in the R168 audit; LocalDiamond holes stay 0. Also
> record in the audit that removing these keywords reconstructs 8c7f30d
> LocalDiamond byte-for-byte. Duplicating 500 lines is rejected. Proceed.

| Exported name | Visibility | Checked DeletionChain consumer |
|---|---|---|
| `locateSingletonAdvanceStageReplay` | `export` | `scopedConcreteAdvanceStageFamily` (`a9622d9`), the exact stage-locator function passed to the family constructor |
| `singletonAdvanceRAR` | `export` | `scopedAdvanceReplay` (`34b86fc`), combining its stage family and strong generator-map relation |
| `SingletonAdvanceStageReplayFamily` | `public export`, explicit exception | Constructed by `scopedConcreteAdvanceStageFamily`; its stored `locateAdvanceStageReplay` projection supplies the existing RAR assembler. It must expose its constructor and dependent field type, not just an opaque type name. |
| `LocatedSingletonAdvanceStageReplay` | `public export`, explicit exception | The dependent result type of that stored locator at each `selected`/`stage` in `scopedConcreteAdvanceStageFamily`. Its value is produced by the exported locator and its fields are consumed by the existing `singletonAdvanceRAR`; DeletionChain does not itself pattern-match this inner record. The constructor/field visibility was expressly authorized as part of this two-record package, not inferred as permission to expose evaluator bodies. |

E1 (`8c20d59`) passed **1/3**, fresh seeded LocalDiamond check
**22:12:45–22:20:49 UTC** (about 8m04s). Maximum sampled RSS was
**49,240,288 KiB (~47.0 GiB)**, sampled every five seconds with an 80-GiB abort
threshold. This is a sampled maximum, not an exact OS peak. No OOM, TTC/TTM
removal, fresh package rebuild, or concurrent compiler occurred.

The final LocalDiamond diff versus `8c7f30d` is exactly **two added `export`
lines and two added `public export` lines**, the latter covered by the quoted
exception. No body/signature/order change or new declaration. Removing exactly
those four keywords reconstructs **the entire `8c7f30d` file byte-for-byte**.
The twelve inherited R167 exports are unchanged. LocalDiamond hole count is 0.

## Incremental protocol and tests

There are **70 immediate checked proof/visibility commits**, **73 implementation
invocations**: 70 successes and the three B48 failures. There are **63 new
top-level declarations**; E1, A15/A16/A17/A19/A21, and H1 are surface-only.
A declaration-set check of every implementation commit verified at most one
new top-level declaration. Every B48 invocation contained its one in-flight
new declaration only; the third also revised B47's existing body, with no extra
helper. No multi-declaration probe was compiled.

All successful implementation logs have their own
`Building DGamma.CP5Confluence...` marker and exit 0. The checks were sequential,
with process inspection before each invocation. No orphan was encountered.
There was no `build/` deletion, cached-interface deletion, or from-scratch
rebuild. Source/fixture mtimes were touched to force fresh target checks;
production files were not touched or edited. Temporary scripts, snippets and
logs live under `/tmp`, not untracked repository research paths.

Common DeletionChain command:

```sh
idris2 --source-dir src --source-dir research --check \
  research/DGamma/CP5ConfluenceDeletionChainSpike.idr
```

E1 used the analogous LocalDiamond path. Fixtures used the same flags plus
`--source-dir research-tests` and their exact file paths below. The package
check was `idris2 --build dgamma.ipkg`, after verifying the retained **207/207**
production TTC seed. The final batch followed complete B48 restoration.

No test fixture was added or edited. The existing positive projection fixture
passed and all three intended negative boundaries still reject. The full R11
suite was not rerun. Compiler warnings remain the inherited DeletionChain
`surviving` shadowing warning and the inherited LocalDiamond warnings.

The non-proof A16 text-generation script initially selected a nonexistent
end-marker and exited **before writing source or invoking Idris**. The marker
and the caller's actual `foreignOwner` spelling were corrected before A16's
first (passing) compiler invocation. This is a script preparation correction,
not an omitted compiler attempt.

## Fresh-check micro-unit ledger

Every row names its immediate checked commit. Times are successful check end
times in UTC (date changes at midnight). E1 is LocalDiamond; all other rows
are fresh DeletionChain checks. B48's failures are listed separately afterward.

| Unit | Declaration / surface | Attempts | Commit | Check ended (UTC) |
|---|---|---:|---|---|
| E1 | `surface` | 1/3 | `8c20d59` | 2026-09-05 22:20:49 |
| A1 | `scopedConcreteAdvanceStageFamily` | 1/3 | `a9622d9` | 2026-09-05 22:23:16 |
| A2 | `scopedReloadingCellSame` | 1/3 | `b8aad09` | 2026-09-05 22:25:07 |
| A3 | `scopedLifecycleAdvanceStageFamily` | 1/3 | `956bc19` | 2026-09-05 22:27:25 |
| A4 | `scopedOwnersAdvanceStageFamily` | 1/3 | `8190ca8` | 2026-09-05 22:28:59 |
| A5 | `scopedAdvanceReplay` | 1/3 | `34b86fc` | 2026-09-05 22:30:33 |
| A6 | `scopedLifecycleReplay` | 1/3 | `56e46a9` | 2026-09-05 22:32:36 |
| A7 | `scopedLifecycleReplayTags` | 1/3 | `3ce8dfa` | 2026-09-05 22:33:52 |
| A8 | `scopedNamedLifecycleReplay` | 1/3 | `7b62a8f` | 2026-09-05 22:35:04 |
| A9 | `scopedPlannedNamedLifecycleReplay` | 1/3 | `d5160f3` | 2026-09-05 22:38:25 |
| A10 | `scopedLocatedNamedLifecycleReplay` | 1/3 | `418caba` | 2026-09-05 22:39:49 |
| A11 | `scopedNamedActionReplay` | 1/3 | `844e67a` | 2026-09-05 22:41:14 |
| A12 | `scopedRelationalNamedReplay` | 1/3 | `f4a1a57` | 2026-09-05 22:43:05 |
| A13 | `scopedSelectedNamedReplay` | 1/3 | `4541934` | 2026-09-05 22:44:26 |
| A14 | `scopedPostCloseNamedReplay` | 1/3 | `75713d3` | 2026-09-05 22:45:42 |
| A15 | `surface` | 1/3 | `62d2f30` | 2026-09-05 22:47:13 |
| A16 | `surface` | 1/3 | `819d509` | 2026-09-05 22:49:56 |
| A17 | `surface` | 1/3 | `b3d7e5a` | 2026-09-05 22:51:39 |
| A18 | `scopedAppendSelectedCloseSemantic` | 1/3 | `9a761d1` | 2026-09-05 22:53:06 |
| A19 | `surface` | 1/3 | `3beeec6` | 2026-09-05 22:54:21 |
| A20 | `scopedEnrichedHeadReplays` | 1/3 | `6b3c777` | 2026-09-05 22:55:35 |
| A21 | `surface` | 1/3 | `fb52b0d` | 2026-09-05 22:56:53 |
| B1 | `scopedPrefixIdentityTags` | 1/3 | `364330b` | 2026-09-05 23:01:37 |
| B2 | `ScopedReplaySeal` | 1/3 | `6ce759e` | 2026-09-05 23:02:59 |
| B3 | `scopedPrependReplaySeal` | 1/3 | `70cf710` | 2026-09-05 23:04:20 |
| B4 | `scopedVerbatimHeadSealAt` | 1/3 | `994d8ff` | 2026-09-05 23:06:23 |
| B5 | `scopedPrefixHeadNotOwned` | 1/3 | `e7573b6` | 2026-09-05 23:07:40 |
| H1 | `surface` | 1/3 | `eab97c6` | 2026-09-05 23:08:59 |
| B6 | `scopedPrefixReplaySeal` | 1/3 | `2009915` | 2026-09-05 23:10:23 |
| B7 | `scopedReadyCanonicalEnds` | 1/3 | `f23722c` | 2026-09-05 23:11:47 |
| B8 | `ScopedDeletionReadinessSeals` | 1/3 | `00bb733` | 2026-09-05 23:13:13 |
| B9 | `scopedEnrichedReadinessSeals` | 1/3 | `59191b4` | 2026-09-05 23:14:46 |
| B10 | `ScopedRuleAt` | 1/3 | `6b02900` | 2026-09-05 23:17:58 |
| B11 | `scopedRuleHeadTag` | 1/3 | `28520be` | 2026-09-05 23:19:15 |
| B12 | `scopedRuleTail` | 1/3 | `6edf06c` | 2026-09-05 23:20:28 |
| B13 | `scopedRuleAtUnique` | 1/3 | `f2c13d0` | 2026-09-05 23:21:46 |
| B14 | `scopedRuleAppendLeft` | 1/3 | `b378a17` | 2026-09-05 23:23:21 |
| B15 | `scopedRuleAppendRight` | 1/3 | `d2598f8` | 2026-09-05 23:24:41 |
| B16 | `scopedLocatedRule` | 1/3 | `3b723c7` | 2026-09-05 23:26:07 |
| B17 | `ScopedOrdinalTagPair` | 1/3 | `20c0979` | 2026-09-05 23:27:52 |
| B18 | `scopedMapSuccEliminate` | 1/3 | `5c19839` | 2026-09-05 23:29:09 |
| B19 | `scopedTagPairPrependSource` | 1/3 | `95956bd` | 2026-09-05 23:30:31 |
| B20 | `scopedTagPairPrependBoth` | 1/3 | `1567a7a` | 2026-09-05 23:31:53 |
| B21 | `scopedSubsequenceTagsKeepAt` | 1/3 | `ea710c1` | 2026-09-05 23:33:31 |
| B22 | `scopedSubsequenceOrdinalTags` | 1/3 | `7a30748` | 2026-09-05 23:35:09 |
| B23 | `scopedTagPairAppendLeft` | 1/3 | `1c7f338` | 2026-09-05 23:36:49 |
| B24 | `scopedTagPairAppendRight` | 1/3 | `d40fd4f` | 2026-09-05 23:38:01 |
| B25 | `scopedDeletionBeforeTagPair` | 1/3 | `88026d6` | 2026-09-05 23:39:59 |
| B26 | `scopedDeletionCenterTagPair` | 1/3 | `70df96f` | 2026-09-05 23:41:18 |
| B27 | `scopedDeletionAfterTagPair` | 1/3 | `2dfa18a` | 2026-09-05 23:42:41 |
| B28 | `scopedDeletionEmbeddedTags` | 1/3 | `b1ab882` | 2026-09-05 23:44:10 |
| B29 | `scopedLocatedTagPairSame` | 1/3 | `e7da44b` | 2026-09-05 23:45:37 |
| B30 | `scopedDeletionWholeTags` | 1/3 | `597ba52` | 2026-09-05 23:46:58 |
| B31 | `ScopedOrdinalPermutation` | 1/3 | `019c0e5` | 2026-09-05 23:50:28 |
| B32 | `scopedComposeOrdinalPermutation` | 1/3 | `c0755a4` | 2026-09-05 23:51:45 |
| B33 | `scopedLiftOrdinalMap` | 1/3 | `d4ed47a` | 2026-09-05 23:53:02 |
| B34 | `scopedLiftOrdinalInverse` | 1/3 | `da66ceb` | 2026-09-05 23:54:20 |
| B35 | `scopedLiftOrdinalPermutation` | 1/3 | `2787c00` | 2026-09-05 23:55:42 |
| B36 | `scopedFlipOrdinalTail` | 1/3 | `dd9007d` | 2026-09-05 23:57:03 |
| B37 | `scopedFlipOrdinal` | 1/3 | `72cdf08` | 2026-09-05 23:58:22 |
| B38 | `scopedFlipOrdinalTailInverse` | 1/3 | `70c4270` | 2026-09-05 23:59:38 |
| B39 | `scopedFlipOrdinalInverse` | 1/3 | `424134f` | 2026-09-06 00:00:54 |
| B40 | `scopedRotateOrdinals` | 1/3 | `5d21fce` | 2026-09-06 00:02:12 |
| B41 | `scopedRotateSuccessorAt` | 1/3 | `0db3e79` | 2026-09-06 00:03:43 |
| B42 | `scopedRotateSuccessor` | 1/3 | `189df7b` | 2026-09-06 00:04:59 |
| B43 | `ScopedOrdinalSpine` | 1/3 | `bb9a086` | 2026-09-06 00:06:23 |
| B44 | `scopedAppendOrdinalSpine` | 1/3 | `4654a60` | 2026-09-06 00:07:44 |
| B45 | `ScopedOrdinalPath` | 1/3 | `a69799f` | 2026-09-06 00:09:16 |
| B46 | `scopedOrdinalPathBound` | 1/3 | `416adcc` | 2026-09-06 00:10:34 |
| B47 | `scopedOrdinalSpinePermutation` | 1/3 | `8248ea2` | 2026-09-06 00:11:56 |
| B48 | `scopedOrdinalPermutationPath` — removed | **3/3** | — | Final failure 2026-09-06 00:18:14 |

## Exact B48 failed compiler diagnostics

The inherited shadowing warning and wrapper timestamps are omitted below;
the complete error diagnostics are quoted. No fourth attempt, rename-based
budget reset, or new proof lane followed the third failure.

### B48 attempt 1/3

```text
Error: While processing right hand side of scopedOrdinalPermutationPath. Can't solve constraint between: scopedRotateOrdinals targetCount and scopedOrdinalSpinePermutation (ScopedOrdinalDelete tail).

DGamma.CP5ConfluenceDeletionChainSpike:26314:3--26315:83
 26314 |   trans (cong (\ordinal => ordinalForward (scopedRotateOrdinals targetCount) (S ordinal)) (scopedOrdinalPermutationPath later))
 26315 |     (scopedRotateSuccessor targetCount targetIndex (scopedOrdinalPathBound later))
```

### B48 attempt 2/3

```text
Error: While processing right hand side of scopedOrdinalPermutationPath. Can't solve constraint between: scopedRotateOrdinals targetCount and scopedOrdinalSpinePermutation (ScopedOrdinalDelete tail).

DGamma.CP5ConfluenceDeletionChainSpike:26315:3--26317:83
 26315 |   trans (cong (ordinalForward (scopedRotateOrdinals targetCount))
 26316 |       (cong S (scopedOrdinalPermutationPath sourceCount targetCount tail sourceIndex targetIndex later)))
 26317 |     (scopedRotateSuccessor targetCount targetIndex (scopedOrdinalPathBound later))
```

### B48 attempt 3/3

```text
Error: While processing right hand side of scopedOrdinalPermutationPath. Can't solve constraint between: scopedRotateOrdinals targetCount and case targetCount of
  { 0 => MkScopedOrdinalPermutation id id (\ordinal => Refl) (\ordinal => Refl)
  ; S targetCount => scopedLiftOrdinalPermutation (scopedOrdinalSpinePermutation tail)
  ; targetCount => scopedComposeOrdinalPermutation (scopedRotateOrdinals targetCount) (scopedLiftOrdinalPermutation (scopedOrdinalSpinePermutation tail))
  }.

DGamma.CP5ConfluenceDeletionChainSpike:26317:3--26319:83
 26317 |   trans (cong (ordinalForward (scopedRotateOrdinals targetCount))
 26318 |       (cong S (scopedOrdinalPermutationPath sourceCount targetCount tail sourceIndex targetIndex later)))
 26319 |     (scopedRotateSuccessor targetCount targetIndex (scopedOrdinalPathBound later))
```

## Final fresh seeded gate evidence

Every check below is after restoration to proof HEAD `8248ea2`. Times are
wrapper-observed check windows (the RSS polling loop may add up to five seconds
at the end). The local proof/fixture mtimes were touched; their logs contain
fresh `Building` markers. Seeded production closure is intentionally a cached
package check, not a from-scratch rebuild.

| Check | Start–end UTC, 2026-09-06 | Exit | Outcome |
|---|---|---:|---|
| gate-deletion | 00:19:36–00:20:37 | 0 | Passed |
| gate-positive | 00:20:37–00:20:42 | 0 | Passed |
| gate-direct-negative | 00:20:42–00:20:47 | 1 | Expected rejection, fragments verified |
| gate-filler-negative | 00:20:47–00:20:52 | 1 | Expected rejection, fragments verified |
| gate-map-negative | 00:20:52–00:20:57 | 1 | Expected rejection, fragments verified |
| gate-seeded | 00:20:57–00:21:18 | 0 | Passed |

Positive fixture: `R11DeletionCertificateProjectionPositive`.

Negative fixtures retained their intended mandatory symbol and diagnostic:

- `R11DirectDeletionStepCloneNegative`:
  `cloneDeletionStepWithAlternateMap`; `occurrences and alternate`.
- `R11DeletionFillerMapCertificateNegative`:
  `fillerMapCannotConstructDeletionCertificate`;
  `generationSubsequenceSourceOrdinal`.
- `R10DeletionStepMapCloneNegative`:
  `replaceDeletionStepOccurrenceMap`;
  `alternate and step .deletionOccurrenceCorrespondence`.

Total compiler invocations are **79**: 73 implementation checks plus six gate
checks. The three expected negative-fixture exits are not proof attempts.

The final asserting validator independently checks each package module's TTC
path, working-tree CP3 blob, complete production diff, frozen byte ranges,
visibility-only reconstruction, unchanged hole names, per-commit declaration
counts, fresh markers, prohibited additions, whitespace, and index cleanliness.
Frozen extraction starts at `0 adjacentSwapSuffixSpike :`, excludes the preceding
`public export` and trailing newline, verifies the following newline, and takes
the statement prefix through `adjacentSwapSuffixSpike =`.

```text
head: 8248ea2
fullSurfaceBytes: 1470
fullSurfaceSHA: 2d01486bf953f11191b758ac3cfb5722d1d02b1a192b6e552adc8a3f58199ecf
statementBytes: 1154
statementSHA: 3aae5a9fbc5b14e0411b4a91e557a6f3dc68c9a6282b9ec2b3fc658cec337adf
seeded: 207/207
missing: []
CP3Blob: 2c697e532e83989de8591fa6a4378747c6a501c0
ipkgBlob: da0c007ee08c4648e459296eb6f0e72a40e2ac89
productionDiffEmpty: True
reviewSHA: 61fc23ae4cea4565b442c840be39c41746ecbac73b8c2f73d04f1e3b4f4681e8
holes: {'CanonicalSort': 2, 'CrossTrace': 4, 'DeletionChain': 3, 'LocalDiamond': 0, 'RenamingComposition': 1}
holeDelta: 0
withCounts: [9, 9]
proofCommits: 70
newDeclarations: 63
implementationInvocations: 73
implementationFailures: ['B48-1', 'B48-2', 'B48-3']
noStagedFiles: True
LocalDiamond: +2 export / +2 public export only, expressly authorized
LocalDiamond reconstruction vs 8c7f30d: exact byte equality after removing keywords
Production diff vs 34b21c9, src/ + dgamma.ipkg: EMPTY
Added believe_me/assert_total/postulate/partial/with/let/hole/deletionTheoremProof: NONE
%default total: retained
Whitespace: PASS
No staged files at proof gate: PASS
Only permitted untracked paths: paper/ and review-o6-body-adversarial.md
```

Source diff before this audit: DeletionChain **1346 insertions / 241 deletions**,
net **1105** lines (including order-only moves); LocalDiamond **4 insertions**.
No other source, fixture, package, README or NOTES file changed. The adversarial
review file was never modified, staged, committed or deleted. The failed B48
source and trial B47 case-expression have no retained delta. No untracked
research artifacts are present. After this audit's commit the final gate must
still have no staged files and only the two permitted untracked paths.

Reviewer/owner gate is **required**. These are reproducible self-checks, not an
independent acceptance review.

## Status

**Fully proved this shift:** arbitrary-input LAdvance stage/generator semantic
replay from exact related owner controls; all-action named singleton replay;
actual owner reflection through deletion plans; live semantic-head emission and
retention through selected interior/closed, post-close and relational folds;
`ScopedEnrichedHeadReplays` production; the **unconditional full TARGET**
`ReplayInvariantBundle` assembler at the exact enriched `DeletionResult`;
untouched-prefix readiness and endpoint seals; all twelve before/center/after
readiness/subsequence-tag clauses; exact whole-origin RuleTag preservation at
the frozen occurrence function; numeric permutation composition/lifting,
finite transposition involution, finite rotations and their bounded successor
law; finite deletion shapes, exact paths and target-position bounds; a generic
shape-to-ordinal-permutation constructor carrying both inverse laws.

**Partial:** operational Unit B. The numeric constructor is not yet connected
to a proven kept-position equation (B48 stopped 3/3), to the actual three
subsequences, or to the frozen generated-registration origin. No exact global
`RegistrationGenerationBijection`, generated-ordinal seal, or complete
`DeletionProducerOperationalCapital` assembler is claimed. Existing readiness
and whole-tag seals are actual checked producers, not residual caller premises.
O9's endpoint/external/accounting/next-premise assembly remains unopened.

**Merely stated:** the same ten research holes, split **2/4/3/0/1**;
O9/O10/O11 remain unchanged at **0/3**. No new escape hatch or hole.

**Next only after the reviewer/owner gate:** select a genuinely different
producer-owned equation/count representation for the B48 conversion boundary,
then connect the finite ordinal paths and permutation to the exact three-way
result and generated-origin equation. Assemble full operational capital without
an arbitrary identity map or filler occurrence, then open O9, O10, and O11 in
order under fresh budgets. Never widen production, infer a generation from raw
name equality, downgrade the target bundle, add a with-block, call frozen
`deletionTheoremProof`, or reset the spent B48 budget without an owner ruling.
