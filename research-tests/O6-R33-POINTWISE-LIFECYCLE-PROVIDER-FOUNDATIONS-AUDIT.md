# O6 revision 33: lifecycle/provider foundations and bounded source stops

## Scope

This is research-only O6 body work in
`CP5ConfluenceLocalDiamondSpike`. The frozen `adjacentSwapSuffixSpike`
declaration, sealed spine boundary, opaque adjacent result, manifest,
production tree, package, and immutable CP3 theorem are unchanged.

## O-Remove staged normalizer

The authorized staged normalizer split executable evaluation into four views:
owner lookup, `retired`, `isInactive`, and `hasChild`. Three bounded elaboration
attempts were made and the entire uncommitted helper was reverted.

The revision-32 composite lazy-Boolean wall did **not** recur. Consequently the
conditional requirement to add a dedicated expected-failure probe for that same
wall was not triggered. The final staged branch reached the strictly smaller
proof-orientation diagnostic:

```text
Can't solve constraint between:
  hasChildIn actor (source .bindings)
and:
  False.
```

The next attempt must return the final child equation with the orientation of
the `with ... proof` binder (`sym childView` in the successful `False` branch)
and must give each dependent projection explicit `{name,key,value,world,error}`
arguments. The three-attempt family budget is exhausted for this shift; no
partial helper or output-shaped premise was retained.

## L-Begin source package

A source-owned `ForeignBeginPlanView` wrapper was attempted three times. It
successfully isolated owner lookup, but matching directly on that lookup refined
the evaluator while the public view expected its unrefined `applyAction`
equation. The final diagnostic was:

```text
Rewriting by Just oldFiber = ?u did not change type
  applyAction (LBegin actor) (MkSystemState ambient source) =
    Just (LBeginTag, afterState).
```

The wrapper was reverted. The next attempt must first call public
`lifecycleOwnerPresent` outside a local `with`, retaining its existential
`lookupFiber = Just owner`, and then call `foreignBeginPlanView` on the original
raw equation. This is source-index normalization only; it does not request new
capital. The L-Begin family budget is exhausted for this shift.

## Retained constructive foundations

Five lemma-sized proof commits remain:

1. `pointwiseControlAfterReplace` preserves `ControlEquivalent` when the same
   named owner is replaced by newly related lifecycle controls, regardless of
   registry list order.
2. `beginEffectFrameRelated`, `divertEffectFrameRelated`, and
   `leaveEffectFrameRelated` specialize checked actual frames for the three
   control-only lifecycle actions.
3. `pointwiseProviderCandidateSame` proves name-local runtime-provider
   candidacy from pointwise control plus exact actor-table effect relation.
4. `pointwiseProviderCandidateAtName` locates the related target fiber and
   retains its lookup, control relation, and candidate equality together.
5. `providerInFromLocatedCandidate` reconstructs an actual provider scan result
   from any located true candidate, independently of its list position.

Commits:

- `b5a62e2` — preserve pointwise controls across lifecycle replacement;
- `1bb91cb` — specialize control-only lifecycle effect frames;
- `11f1477` — transport provider candidacy pointwise;
- `656ab8e` — locate pointwise provider candidates;
- `1c70f87` — recover provider scans from located candidates.

These are the dependency-order foundations needed by L-Begin, L-Divert, and
L-Leave: those actions observe target resolution, while pointwise controls do
not assume equal registry list order.

A final helper intended to turn `ProviderOfSound` into a true candidate was
also tried under a bounded budget and reverted. It reached only an explicit
implicit-index ambiguity at `valueFromProvider`; the next proof must spell
`{value = value} {world = world} {error = error}` on that equality. No semantic
or output-capital blocker was found.

## Safe boundary

No complete new semantic head closes at revision 33. Fully closed heads remain
O-Insert and O-Retire. O-Remove and L-Begin are stopped at the exact bounded
source-normalization diagnostics above. L-Divert and L-Leave own checked effect
and replacement-control foundations but have not been opened as full producers.
L-Unload and L-Advance remain untouched because the lighter provider-selection
dependency is not yet closed.

Whole-suffix RAR/ordinal composition and adjacent-result assembly remain gated
behind all six open semantic families. Holes remain 20, split `6/4/8/1/1`.
The remaining estimate stays **18–34 shifts**: retained provider foundations
reduce the lighter lifecycle bodies, while no action head closed and both source
normalizers remain charged.
