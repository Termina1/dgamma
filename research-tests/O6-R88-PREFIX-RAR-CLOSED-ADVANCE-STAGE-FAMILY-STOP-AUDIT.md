# O6 revision 88: identity-prefix RAR closed; advance-stage family stop

## Scope

Grind shift #96 (overall #150) resumed at accepted revision 87 `f84416b` and
opened only the field-9 whole-RAR campaign. The identity prefix leg closed. The
first moved-pair foundation, a monolithic singleton L-Advance stage-family
constructor, exhausted its own fresh three-attempt budget on dependent statement
and erased-local annotation layout. The entire unit was removed. No moved-pair
RAR, field, package, occurrence, result, assembly, or body change remains.

## Retained capital

`identityRelationalReplayCorrespondence` passed on its first visible fresh
attempt and is retained at `84c1b81`.

For any exact trace it constructs:

```text
RelationalReplayCorrespondence trace trace
```

with:

- generator origin equal to the target generator itself;
- generator-map relation supplied by checked
  `replayTraceGeneratorMapRespects` for every observed `DecEq key`;
- iterator-stage origin equal to the stage itself;
- exact iterator outcome equation `Refl`.

This is the authorized unchanged-prefix RAR leg. It introduces no proof
irrelevance or dictionary equality.

## Removed unit

The removed `singletonAdvanceStageFamilyFromOwnerLookup` attempted to build a
`SingletonAdvanceStageReplayFamily` for two exact checked `LAdvance actor`
singletons from equality of their actor lookups. Its intended construction was
sound and reused existing machinery:

1. eliminate a target `StageFromAdvance`;
2. reduce its occurrence to the only singleton transition;
3. expose the exact `MkFiber ... (Reloading remaining accumulator view)` owner
   cell;
4. transport target lookup presence backward through the supplied exact lookup
   equality;
5. delegate immediately to checked `locateSingletonAdvanceStageReplay`, using
   the same component/parent/retired/table/remaining/accumulator/view on both
   sides.

The three deaths were mechanical:

1. the signature left dependent `value` inference under a bare `lookupFiber`,
   while the body could not resolve the private local singleton occurrence
   helper after the malformed declaration;
2. ordinary explicit `name/key/world/error/value` arguments alone were
   insufficient because both lookup expressions still omitted all explicit
   type parameters;
3. after making both lookups fully explicit, elaboration advanced through the
   stage and fiber eliminations and stopped only because the erased local
   `sourceFound` equality annotation lacked parentheses around the complete
   proposition:

```text
Mismatch between:
  Maybe (Fiber name key value world error)
and:
  Type
```

This is the same Idris erased-local layout class seen in revisions 83 and 85,
not a semantic wall. Per protocol, the whole uncommitted unit was removed at
attempt 3.

## Exact next decomposition

Do not retry the monolithic family declaration. Give each of these a fresh
budget:

1. `singletonAdvanceSourceFoundFromOwnerLookup`: a zero-hidden-argument helper
   with explicit `name/key/world/error/value`, fully explicit left/right
   `lookupFiber` expressions, and a parenthesized complete result equality;
   implementation is only `trans lookupSame movedFound`.
2. `locateSingletonAdvanceStageFromOwnerLookup`: consume one target stage,
   eliminate its singleton occurrence and exact reloading fiber, call helper 1,
   then delegate to frozen `locateSingletonAdvanceStageReplay`.
3. `singletonAdvanceStageFamilyFromOwnerLookup`: a one-clause wrapper
   `MkSingletonAdvanceStageReplayFamily` around helper 2, with no dependent
   proof logic.
4. Construct activation singleton RAR:
   - `LBegin`: `singletonNonAdvanceRAR` plus
     `activationTransitionMapOriginCong`;
   - `LAdvance`: `singletonAdvanceRAR` plus the split family and
     `advanceTransitionMapOriginCong`.
5. Construct orchestration singleton RAR with `singletonNonAdvanceRAR` plus
   `orchestrationTransitionMapOriginCong`.
6. Use the exact aligned source/moved pair and revision-21 classification to
   select these existing action-specific paths, reverse the source pair
   occurrence order producer-correlatively, and construct the moved-pair RAR.
7. Compose identity prefix, moved pair, and
   `sealedSuffixRelationalReplayCorrespondence` into the whole field-9 RAR.

No detached target RAR, generator map, iterator stage, dictionary equality, or
independence premise is authorized.

## Status

- identity prefix RAR: **closed**;
- moved-pair RAR: **open at the split L-Advance stage-family foundation**;
- whole original-to-target RAR: **open**;
- fields 1–8: **closed and frozen**;
- field 9: **open**;
- fields 10–15 foundations: **closed and frozen; package population pending**;
- occurrence fold/result/body: **unopened**;
- holes: **20**, split **6/4/8/1/1**.

The accepted remaining band is held at **1–10 shifts**. The body-closure review
boundary was not reached.

## Isolation

Production `src/`, `dgamma.ipkg`, CP3, the frozen 1183-byte adjacent interface,
revisions 19–21, fields 1–8, and fields 10–15 foundations are unchanged. No
escape hatch, hole, postulate, public surface, or unverified unit was retained.
