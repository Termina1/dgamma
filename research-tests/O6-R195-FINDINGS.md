# R195 findings synchronization (D1)

## Checked boundaries, not convergence completion

- A1–A10: exact `O20RootReplayOrdinals` specification; identity and composition
  producers; conditional conjugated-root attachment. The FULL generic replay
  record does not entail this law, as an actual singleton Void fixture proves.
  Owner ratifies this as a necessity witness, not a convergence counterexample.
- A11–A26: flat generation-only original-birth disposition, every-original-birth
  scanner coverage, replay-origin retention, and simultaneous physical ordinal
  attachment. Closing retains real parent-Unload evidence without a right
  canonical/raw-name match. R193's genuine present-vestigial fixture instantiates
  closing/identity-replay retention. No whole accepted paired-stage sequence.
- B1–B12: native full identity control/effect/history cut; actual R193 accepted
  ORIGINAL inputs coexist with that internal cut and impossibility of the
  supplied-current-map ALL-name cut. This upgrades the old name disagreement
  fixture with the full input history cut. It is NOT a canonical counterexample.
- B13–B16: actual current-generation disagreement produces every ORIGINAL
  VestigialEndpointGeneration field. R192's removed child supplies actual
  absence instead. Canonical absence/current-map control attachment is not owned.
- D5 `o20SupportedBridgeFromOwnedCut` still CONSUMES an ALL-name cut at the actual
  canonical/replayed endpoints. No new bridge producer or O20 body attempt.
  A26/B16 caps bind; source freeze3bd991710c76170d358dd5dadc07c676dfdb21f0.

## A11 remains exactly the approved revision

The R192 manifest remains frozen. Only the fourth bridge field's original
`isSupported child leftFinal = True` scope was approved; first three fields
still demand the exact all-name runtime relation. Generation-only closing
history and a full original vestigial package are not substitutes for it.
O21's statement and hole are unchanged. See O6-R192-A11-SURFACE-MANIFEST.json.

## A8/A10 OPTION A and A12 are decided, not pending design choices

Production remains frozen to34b21c9. The owner first requires research copies,
then one exact CP3/in-file-dependent diff and a memory-safe seeded rebuild plan,
then the owner's signature before production commit. Lane2 owns A8/A10/A12 and
O17 research; no lane-owned source is edited or merged here.

A12 = **ATTACH + immediate-after-LAST-release placement + barrier closure**:
key-forced roots attach as a trailing ordered bundle to the last freeing
actor's extended block. Forcedness is the LEAST set containing key-forced roots
and closed under later external orchestration order. Thus an always-available
root following a forced root is barrier-forced too; only roots before the first
forced root retain the unqualified input-first phase. Multi-key roots attach
to the last releasing block; all bundled roots retain original external order.
This is the owner-approved research cure, not proof of an arbitrary normalizer.

The R194 selector result is **one hypothesis from the CP3 diff**:
`o20StoppedOrderEqualsGoalZeroGap` already derives the non-gap clauses from
actual accepted/reached capital. Its remaining hypothesis is quantified literal
`ZeroGapPending gap = transitionCount gap = 0` at native inversion slots.
Selector body stays untouched. No separate A12Pending is needed inside that
frozen exact-zero theorem, because an empty physical gap contains no root step.

## Supervisor paper-level reading (attributed, not newly promoted)

Read-only main Git-object inspection pinned the L2R3 audit at
`d39d786a8c8644ec856e945bab16d0424dc9d904:research-tests/O6-L2R3-GRIND-SHIFT-AUDIT.md`.
This does not certify L2R3/L2R4 code or results. No lane worktree was accessed.
The copied audit and inspection manifest are in R195's evidence archive.
The relevant main paper lines2170–2399 were visibly re-inspected; this is NOT a
full-paper-rereading attestation. The supervisor's recorded reading is:

> Theorem 73(1) clauses — (a) "the same orchestration steps in their original order", (b) "those at a fiber the orchestrator inserted preceding every lifecycle step", (c) "each of the rest following the step that registered the fiber it acts on"; for a key-forced root, (a)+(c) place it after the child's O-Remove, which follows the parent's registering L-Iter — contradicting (b); the proof moves root inputs to the front only "past a lifecycle step of a different fiber by Lemma 71(2)" and never exchanges two orchestration steps; moving a root insert back past an O-Remove ENLARGES the registry, while Lemma 71(2)'s O-Insert-premise argument covers only "a smaller registry only relaxes" (single-source premise ∀m. p_n ∩ p_m = ∅). So A8/A12 are paper-level gaps and ATTACH is clause (c) generalized to forced roots.

Native fixtures remain scoped countershapes, not a formal refutation under all
paper metatheorem hypotheses. No new paper erratum is claimed from R195's
root-record necessity witness or original-endpoint rebasing obstruction.

## Root producer-contract decision and resource ruling

The three-keyword proposal was explicitly deferred because an exported default
map/proof cannot attach itself to an arbitrary stored correspondence. The
stronger DOCS-ONLY R196 plan is O6-R195-ROOT-CONTRACT-MANIFEST.md: erased root
laws on the two storing records, every actual constructor site, full consumer
chain, frozen-body check, and conservative dependent-cost inventory. No contract
field/keyword is changed in R195. Whole pairing and endpoint rebasing remain
separate even after a future root contract is inhabited.

Unchanged LocalDiamond validation retains52GiB; all other R195 checks48GiB.
The owner also permits52GiB for future prior-gated keyword/FIELD-only LocalDiamond
strengthening. The manifest explicitly asks that the R196 gate settle the
necessary new-helper check's classification; it does not invent a blanket52GiB
permission for arbitrary changed proof code. Reserve any R196 heavy rebuild
window through the supervisor with lane2; no reservation is claimed now.

## Exact outstanding construction obligations

1. Produce `O20RootReplayOrdinals ... (canonicalOccurrenceCorrespondence capital)`
   from the strengthened actual storing contracts, then the operational fold.
2. Produce a whole paired sequence on the ACTUAL two canonical/replayed words,
   with right counterpart/stuttering disposition and ordinal ownership for EVERY
   stage. The existing local family is `O20PairedExecutionWithRemoval ...
   internal initial initial replayedFinal (canonicalFinal rightSchedule)`; merely
   inhabiting that endpoint-indexed family without attaching both actual words
   would not meet this obligation. No new whole-pairing type/producer is claimed.
3. Fold native history successors from the identical empty origin, then prove
   `O20AllNameCut ... (expectedBridgeBijection inputs) replayedFinal
   (canonicalFinal rightSchedule)` at the actual endpoints. The current map's
   original vestigial alternatives cannot be rewritten into this cut.
4. In particular prove actual canonical absence for genuinely withdrawn original
   vestigial births, and exact current-map control agreement for remaining
   present names, including unsupported non-vestigial ones and their parents/
   captured providers. The opposite current-image absence must also be owned.
5. Apply D5 only after this cut exists. Original-endpoint mismatch -> full
   vestigial evidence is proved; the complete canonical-to-original remainder
   assembly required by unchanged O21 is still unproved. No O21 withdrawal proof
   or protected statement revision was attempted. No statement-level failure of
   the protected convergence theorem has been established.
