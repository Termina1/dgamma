# R201 source self-review (not independent acceptance)

Source freeze: `df921f18dc241bf815ff933328185001e525d668`.
Scope: the six new Idris sources listed in `O6-R201-VALIDATION-SCOPE.json`.
This is the author's read-through, not the required independent reviewer gate.

## What the new producer capital actually contains

- `o20DeletionRetainedBirth` eliminates the ACTUAL `originalRegistrationAccounted`
  alternative. A withdrawn case contradicts the actual candidate's nonselection
  only after both stored generation-exact equations are transported. The retained
  branch preserves the provided occurrence and reconciles the actual step's
  occurrence-map bijection with its stored producer bijection.
- `O20RetainedGenerationBirth` owns a parent, component, physical generated birth
  and forward-stamp equation. It contains NO Unload. The theorem immediately
  below it that constructs a deleted classification is a sufficiency CONSUMER:
  the caller supplies an Unload in THAT birth's own suffix. No coverage is thereby
  produced.
- `O20RegisteredUnloadFree` follows the ORIGINAL source scanner: even a filtered
  head advances the original ordinal/environment. The source producer derives
  exclusion from `selectedChildrenHaveNoEpisode`, alignment, environment
  uniqueness, and the public registered-Inactive invariant.
- Registered filtering retains all Unloads. Center episode filtering retains
  FOREIGN Unloads only. The split at `beforeGenerationScan` and
  `episodeGenerationScan` uses the actual candidate/result decomposition.
  No proof claims the selected parent's center Unload survives, and no proof
  joins a surviving birth and Unload in birth-relative order.
- `O20PhysicalInsertOriginPositions` connects an actual paired physical generated
  birth packet with ORIGINAL scanner per-activation positions at its generation
  coordinates. Its right equality reconciles the matcher's original child stamp
  with the same attached right birth's original origin stamp. It does not claim
  actual canonical activation-position preservation or event-record equality.
- Both native Insert targets are derived from the same checked evaluator
  equations via public single-constructor `ForeignInsertPlanView`. Actual target
  states are never independently reconstructed and identified by fiat.
- Located ordinals are actual preceding-trace counts. A18 uses the composed left
  canonical/exchange correspondence, right canonical correspondence and
  `expectedBridgeBijection`. Its generation map is the exact conjugation used
  by the unchanged modulo synchronization goal.
- **A18's `leftLive` and `rightLive` are arbitrary explicit PARAMETERS.**
  The theorem does not produce a scan showing they are the live environments
  reached at those native cuts. The native source/target states and physical
  ordinal counts are authentic; the incoming generation environments still
  have to be chosen from a whole scanned history by a future producer.
- A native Insert stage does not assume or prove an all-name predecessor cut.
  Applying its existing history-fold consumer will still require that cut, and
  a whole synchronization will require both native paths, scanners, order and
  real one-sided skips. Nothing declares an arbitrary edge zero.

## Exact open boundary

For the actual retained packet produced in the vestigial induction, the
unproduced birth-relative fact has the literal type

```idris
ActionOccurs (LUnload (retainedParent retained))
  (afterRegistration (retainedBirth retained))
```

The ambient telescope remains the actual accepted inputs, original vestigial
packet/classification, actual deletion step and candidate nonselection. This
snippet is NOT a newly postulated generic theorem and has no source hole.
`o20DeletionRetainedClosingBirth` still needs to produce the retained deleted
classification, rather than require this fact. In particular, if the original
close is the selected parent's deleted center close, derive an alternative
retained close in the same birth's suffix or derive selected-birth membership.
The current code establishes neither disjunct and no insufficiency of existing
capital was proved. No new-field oracle manifest was requested or inserted.

The rest remains ordered debt: actual global selection coverage -> exact
current-coordinate and all-name endpoint rebase -> D5; and canonical
per-activation/order transport -> root attachment using C6/C9 -> genuine skips
-> whole occurrence history -> `o20SynchronizeCanonicalHistoriesModulo`.
Both are OPEN, so C is ineligible and unattempted.

## Tests and rejected attempts

- `R201DeletionRetainedBirthPositive` uses the authentic R193/R195 eight-edge
  vestigial history; actual deletion step/nonselection remain explicit
  conditional inputs. It does not fabricate a canonical schedule.
- `R201PhysicalInsertStagePositive:r201NativeChildInsertStages` applies the NEW
  native target-observation producer to the real R191 child cut and the real
  R193/R195 child cut. It is not a concrete instance of the whole accepted A18
  telescope or canonical synchronization.
- A5-1: local script mistakenly replaced a `key -> Type` substring while
  generating a signature. A5-2 corrected that generated signature. No previously
  committed declaration was rewritten.
- A19-1: the fixture expected a fresh live-generation entry at the FRONT. CP3's
  operation appends a fresh entry. A19-2 changes only the expected incoming and
  outgoing lists, by explicit owner authorization. This is a fixture-expectation
  error, not a producer defect. Both source snapshots and exact correction
  hashes/replacements are archived.
- Full inherited final fixtures are in the immutable 172-check plan, including
  all seven expected-negative diagnostics AND their symbols. No failed
  development snapshot is labelled PASS.

## Scope and residual risks

No blocker found against the bounded partial statements during this read-through.
No source edit followed the source freeze. The correctness of the full original
paper, physical per-cut scanning, closing suffix joins and whole-history
confluence is NOT established by these supporting statements. No escapes added;
existing four-hole census remains `1/2/0/0/1`.

The complete extracted paper text (3882 lines,301314 bytes) was read in eight
consecutive read-tool ranges ending at EOF; SHA256
`9b40364ab67f944406c6f40980cdc78dbba044d6eb3125aa1087f2ffdfc9a78b`.
This is not a PDF-rendering/typography verification or a new paper-wide proof
review. The raw archive includes `paper-read.json`.
