# R194 checked partial verification

**Owner-ratified checked PARTIAL at52e1d308; independent reviewer pending.**
This is not Theorem73 completion and not a sibling selector body.

- Exact start: `b81362d88d8b52298ddd8a1bff41e741fde65d97`.
- Proof-source freeze: `408bd21e832cd231a727dfb8347c9a15d019f4e0`.
- Read-only evidence/archive anchor: `86b7424aa0d24d0fcbe07500f31415b130feb3f4`.
- **46 declarations /46 receipt-authenticated source commits**, A30/B16.
- **59/59 final checks PASS:52 positives /7 expected negatives**, completed
  2026-09-09T01:43:03.270848Z. All7 changed Idris targets have their own fresh checks.
- **111 compiler/build invocations:107 matched outcomes /4 rejected proof attempts.**
  Of the107,7 are expected negative compilations, not successful typechecks.
- **10 noncompiler evidence-contract tests PASS** with the corrected owner scope.
- No3/3 unit exhaustion, budget extension, interruption, target mutation, cold
  build, seed deletion, stale-lock cleanup or orphan kill.

## Proven and remaining

A produces canonical-coordinate bijections and simultaneous authentic supported-
birth origin/physical-ordinal attachment, plus a closed local history successor
from two actual checked Iter edges. These do **not** produce arbitrary whole
canonical pairing. Unsupported retained/closing birth disposition, replayed-root
ordinal attachment, generic last-Finish ownership, whole-cut folding, all-name
endpoint rebasing and exact present-vestigial treatment remain open. D5 still
consumes the missing all-name cut; no bridge producer or convergence-body attempt.

B discharges both former physical endpoint/reference component attachments.
Actual native lookups, installedness, original support, child exclusions and
whole-block resolver transport produce the selected inversion's own-cut safety.
`o20StoppedOrderEqualsGoalZeroGap` proves equality for the ACTUAL stopped reference
search, **conditionally on quantified ZeroGapPending at native inversion slots**.
ZeroGapPending remains exactly `transitionCount gap = 0`, not a safety oracle.
No separate A12Pending is needed in the frozen exact-zero theorem, since zero
actual transitions excludes an intervening root. No extended-grammar/root-
availability result or unconditional gap-zero theorem is claimed.

## Resource and baseline qualifications

LocalDiamond V2 passed on its FIRST planned unchanged-source52GiB run:
491.187s, sampled **50,461,904KiB (48.12422GiB)**. Source bytes match b81362d8.
All other checks retained48GiB. The11 known-or-observed heavy checks each held
and released `/tmp/dgamma-heavy.lock`; the resource audit authenticates them.
RSS values are retained1s samples, NOT OS high-water marks. Zero means no live
sample captured; affected units are enumerated in the resource audit.

A8-1/A8-2 both passed; a precommit docstring correction explains the second run.
The audit verifies exact code equality ignoring comments/blank lines. It does
not silently reclassify the first success. A15-1 and A29-1, B10-1 and B12-1 remain
rejected snapshots; all retained declarations passed before their own commits.

The original56-slot preflight plan is preserved, explicitly withdrawn UNRUN.
The supervisor rejected exclusion of inherited main-baseline variant modules.
All52 inherited targets exist, remain byte-pinned to b81362d8 and are included
alongside7 changed targets:59 slots, no exclusions. Main checks certify main
baseline fixtures, not ongoing lane2-created results. The verbatim ruling and
both plan hashes are linked below. No sibling-worktree operation or source merge.

## Frozen gate

Frozen audit PASS: `src/` and `dgamma.ipkg` byte-identical to34b21c9;
CP3 blob `2c697e532e83989de8591fa6a4378747c6a501c0`. All five protected spike
files, O19Surface, LocalDiamond and the R192 A11 bridge record are unchanged.
Protected hole census **4=1/2/0/0/1**. All207 package TTC seeds retained/refreshed;
LocalDiamond TTC125,368,223 bytes, refreshed01:29:13Z. Package success is a
seeded `--build`, not a forced207-module or cold rebuild. Individual Idris source
validations require their own fresh Building line.

No new hole, postulate, unsafe escape, partial, with, local let, forbidden binder,
frozen deletion-body call or scoped-to-raw cast. No main compiler/staged files.
Only pre-existing `paper/` and `review-o6-body-adversarial.md` remain untracked.

## Evidence and reproduction

- [Shift audit](O6-R194-GRIND-SHIFT-AUDIT.md) and [findings sync](O6-R194-FINDINGS.md).
- [Immutable59-slot plan](O6-R194-FINAL-VALIDATION-PLAN.json) and [scope](O6-R194-FINAL-VALIDATION-SCOPE.json).
- [Owner preflight ruling](O6-R194-PREFLIGHT-RULING.md), [withdrawn plan](O6-R194-PREFLIGHT-PLAN.json), [original scope](O6-R194-PREFLIGHT-SCOPE.json).
- [Independent automated audit](O6-R194-INDEPENDENT-VERIFICATION.json).
- [Frozen audit](O6-R194-FROZEN-AUDIT.json) and [resource audit](O6-R194-RESOURCE-AUDIT.json).
- [Consolidated compiler ledger](O6-R194-COMPILER-LEDGER.json).
- [Exact source/log/receipt archive](O6-R194-COMPILER-EVIDENCE.tar.gz).

Archive SHA256: `b5333f87231d3afbec52bb7ac802292e741e1875c7f4f2d381d6367e77e58d8d`.
The archive precedes its own artifact commit and does not contain or claim
that self-referential receipt. All46 source receipts and111 compiler records
are included and authenticated. Matching historical source commits are not a
substitute for the separately authenticated immediate guarded-commit receipts.

```sh
python3 -B -I -m unittest discover -v -s research-tests -p test_r194_evidence_contract.py
python3 -B -I research-tests/run-r194-frozen-audit.py
python3 -B -I research-tests/run-r194-independent-verify.py
python3 -B -I /tmp/dgamma-r194/resource-audit.py
```

Read-only audits use `/tmp/dgamma-r194`; restore that directory from the committed
archive when reviewing elsewhere. Freshness matching expects the original
checkout path recorded in the absolute compiler logs. Do not rerun the already-completed immutable
compiler plan, cold-rebuild, delete seeds, or infer completion of the open proofs.
The independent automated audit is not a substitute for the parent-owned reviewer.

## Owner final gate

The supervisor independently verified and RATIFIED52e1d308, authorized an
artifact-only note and close, and reports that commit pushed. The verbatim
ruling is in NOTES.md. Post-publication read-only audits also PASS at52e1d308.
No proof/Idris source or compiler work followed the ruling. The parent-owned
independent reviewer is being launched; its acceptance is not claimed here.
This gate note does not alter or regenerate the pre-artifact evidence archive.
