# L2R10 lane-owned Tier-2 synchronization overlay

**PARTIAL / COMPLETE CURE NOT SIGNABLE. No production change.**

This overlay supersedes ONLY the status descriptions at lines5–20 and572ff
of `O6-L2R5-CP3-DIFF-DRAFT.md`. That predecessor draft is byte-unchanged;
its proposed Tier-1 syntax remains uncompiled in CP3. This overlay grants no
production unfreeze and changes no evaluator, insertion provision guard,
registration scanner, external orchestration order, vestigial relation,
endpoint/effect/control equivalence or Theorem73 conclusion.

Source boundary: **637c0ca1**, **42 checked declarations in11 new modules**
(A14/B16/C2/D10). Of these, 26 are erased proofs, 12 executable definitions,
and4 type/record definitions. A type definition is not an existence proof.
Origins, per-invocation resource/diagnostic results and guarded git receipts
are in the L2R10 origins/ledgers/evidence archive. Human mathematical review
remains parent-owned; compiler-free evidence verification is not that review.

## Exact replacement status rows

| Tier-2 obligation | Newly checked backing | Exact remaining limit |
|---|---|---|
| Native release scan data fixtures | `L2R10OrdinalData.fixtureDictionaries` 265d4728 and `ordinalDataAgreement` 1f61af78: actual public ω trails; explicit concrete Nat/Bool dictionary values; native and old bounded scans both `[3]` on both fixtures | NEW equation formulation, not an inhabitant/retry of frozen L2R9 `OrdinalFixtures`; arbitrary located-release completeness remains open |
| Single-key scan agreement | `L2R10ReleaseAgreement.releaseScanAgrees` b24bd0f7: GENERAL `isYes (isElem wanted keys) = elemDec wanted keys`, using own observed library Dec and Bool equations | Whole-trail ordinal-list agreement and arbitrary shared-key/located-release decoder composition remain open |
| Data-derived phase checking | `L2R10PhaseScan.phaseScanOk` 8e5e630d computes actual catalog/old-maximum anchor, earlier key-seed ω membership, and preceding lifecycle inside a contiguous actor-core event word; `phaseScanFixtureObservations` 504f9885 proves two actual acceptances | Executable checker + fixtures ONLY; no `produceForcedRootPhases`, located phase certificates, `scanCatalogBirth` phase linkage, or anchor identity/ordinal transport |
| Own-child retirement lifecycle replay | `L2R10BeginAdapter.retirementBeginPlanAdapter` c6ee9eb9 uses original `ForeignBeginPlanView.ownerShape`; `replayRetirementBegin` 982aec58. `retirementAdvanceNative` 862cab09 / `replayRetirementAdvance` 7d53bc29 cover ALL native Advance outcomes/tags. `L2R10LifecycleRoles.replayRetirementLifecycle` d1f6370a inhabits the EXACT retained Begin/Iter/Finish role | Lifecycle role CLOSED, not whole ForeignReplay. The old `single` quantifies ALL8 Actions/all tags; standalone `LDivert`, `LLeave`, `LUnload`, actor/frame production and arbitrary snapshot-source assembly remain open. No exact full `single` or whole R191 replay was run/claimed |
| Split path and core contiguity restoration | NEW abstract-state `SplitNativeEdge` 3b57a7bc; actual root insertion 4→8 via `splitRootEdge` 72d9d446 | Remove8→9 C3 STOP3/3: two18GiB stops, exact unused-third48GiB stop, complete revert. No split packet, per-path core assembly, restoration record or `GeneralCoreContiguityRestored` instance. C4–C10 unattempted; old exhausted statements remain parked |
| Name-reuse premise for distance moves | NEW `GeneralAdmittedMoveExistenceUnique` TYPE e3b15e9b restores owner R173 `UniqueRawNameInsertions`; `uniqueChildBirthBeforeRootExcluded` c953937d derives a contradiction from actual same-trace earlier child/later root births with the same name | TYPE not inhabited; old L2R6 type untouched. Initially installed names outside a prefix need whole-trace/from-empty birth provenance. Candidate reuse scenario is uncompiled R173-necessity analysis, NOT a checked counterexample |
| Native selected-cut availability | `L2R10MoveCutObservation.observeSelectedMoveCut` 21dcd522 chains actual `selectNativeDistanceRoot`, physical source/action-word query, `classifyPredecessor`, native early OInsert evaluation and `scanCatalogBirth`; it retains exact split/zero-prefix/positive/source/evaluator equations | This is a typed square REQUEST, not a square/move. Missing/local/root predecessor cases and failed early applicability stay visible; no total existence inferred from classification |
| Existing single / ordered R,S cuts FROM new producer | `singleSelectedCutObservation` b79f126c, `bundleSelectedCutObservations` 637c0ca1 compute `(R=3, ordinal5, Begin2, True)` then `(S=4, ordinal6, Begin2, True)` from the new producer | These reproduce selected-cut/early-applicability observations, NOT old admitted moves by projecting fixture fields, and NOT D8 moves FROM a general admitted-move producer |
| General admitted moves / normalization | Existing predecessor root-birth/control exclusion kernels retained unchanged; new native request layer above provides actual cut data without oracles | Native located predecessor decoding, exclusion linkage, all classifier-case square replay (including two-observed-Dec Insert/Insert), forcing/phases, endpoint/anchor transport, exact-one decrement, `produceAdmittedDistanceMove` and `normalizePhaseDistance` all remain open |
| Global attached/C scope, zero-gap and placement | Prior checked embeddings and same-bundle/conditional gap capital unchanged | No `produceAttachedNormalForm`, all-premises-produced zero-gap, maximal decomposition/converse, terminal earliest or cross-bundle controls closure |

## Domain and proof fidelity

- R173 global no-reuse is an owner-level Theorem73 hypothesis, not an optional
  strengthening invented to hide a reuse bug. The new local exclusion uses
  actual located birth provenance and count order; it does not manufacture
  provenance for a nonempty initial state.
- `SelectedSquareCut` observes the actual source/action at
  `pred(catalogOrdinal)`. Its source/action-word equation is not yet a native
  `LocatedActionOccurrence`/adjacency decoder. Its early native evaluator may
  return `Nothing`; no proof field asserts successful crossing replay.
- D7's first attempt failed coverage on calculated search indices. The checked
  formulation observes generic distance/items with their exact actual
  equations, then uses `trans`/`replace`. The final producer supplies those
  equations itself. No supplied positive selection/catalog/oracle substitutes
  for native search.
- B's stronger all-tag Advance observation does not discharge separately
  requested lifecycle Actions. No narrowed action inventory is substituted
  for the original `single` contract. A NEW restricted actual-word replay
  would be future work, not a result of this shift.
- No axiom, hole, `believe_me`, `assert_total`, partial function, old theorem
  body repair, nonlinear pattern or unacknowledged quantity change was added.

## Review and operational boundary

The only predecessor edit is the independently authorized D4 COMMENT repair
in `L2R9ProviderHead`, commit74a162c5, fresh own-target V4. Its type/body/quantities
are byte-identical; exact before/after hashes are in COMMENT-REPAIR.json.
L2R9 review: ACCEPT-WITH-NOTES, no proof blocker. Its final plan preceded V6,
not the earlier V1.

Every new declaration was separately checked and guarded-committed. The C3
heavy cost check was authorized on exact C3-2 bytes only. Historical lock
polling occurred under the previous policy; the own polling worker was
stopped before compiler launch when the owner abolished the lock. Actual
C3-3 then launched without lock/window operations and sampled50375152KiB
before its48GiB guard stopped it. That is a bounded cost lower-bound datum,
not semantic failure or completed peak. Other-lane compilers were separate,
never signalled. Concurrent-heavy overlap timestamps are archived.

**Decision: keep Owner Option A / PARTIAL. Do not sign a complete cure or
claim Theorem73, a full replay, core restoration, or a normalizer.**
