# L2R1 Option A research-copy shift audit

## Baseline and boundaries

Worktree /Users/vyacheslavshebanov/Work/dgamma-lane2; branch
cp5-thm73-lane-a8a10; initial HEAD
77a9efe144fa706f2b83c6ef4c0096dbfcbf190b. Initial tracked/untracked tree CLEAN.
Idris2 0.8.0. No main-worktree access, no production/root-document/source-surface
edits. Mandatory R192 decision+audit, R191, R178, R174, R172 recon and R146 memo
read before code. Paper copy absent initially; supervisor asked to supply one
without main-worktree access. Conservative start20:54Z, attempt cutoff00:14Z,
validation cutoff00:29Z, gate00:39Z, timeout00:54Z (2026-09-08/09 UTC).

Scope: B<=24 new units, actual child Retire/Remove relocation then extended
blocks/zero-gap; C<=20 placement-dependent research copies/availability root
phase. No O17/O20/O21 statement edit; no src/, root docs, LocalDiamond edit,
O20 module edit, new with/let/unsafe proof. One new declaration/check, <=3
attempts, immediate Python-guarded PASS commit; exhausted statements stay parked.
R192 C4 eleven-edge raw producer is NOT restated.

## Supervisor protocol approval (before B1)

S0-1 trusted CrossTrace check exit0/5.21s had NO own Building, correctly
fresh=false/PASS=false. Supervisor approved TARGET-only source mtime touch to
force fresh checks: never dependencies/mass touches; record old/new nanoseconds.
Rebuilt TTCs may trigger dependent rebuilds: order validations leaf-last and use
the shared heavy lock if a >=19GiB dependency will rebuild. S0-2 own Building
PASS6.29s. Seeded build is preserved; no full package/cold rebuild.

Supervisor also approved generic runtimeSnapshot equality (EXACT world+ordered
bindings, not an observational weakening of those runtime fields) plus a REAL
checked successor; exact generic SystemState equality would wrongly identify
independently erased UniqueKeys terms. CP4RuntimeBindings:13/20/427/446 is the
precedent. R191 relocated fixture STILL must keep the LITERAL same final
SystemState. Zero-gap must concern the actual relocated trace, not a list order.
This equality-level decision must feed the future production-unfreeze memo.

## Monitoring qualification

Initial three checks' RSS counters are unavailable, not zero-memory claims:
the copied classifier omitted executable basename `chez`. This was found by
actual ps inspection and fixed before B1 commit/all subsequent checks. That
inspection saw the concurrent compiler referencing /Work/dgamma/; it belongs
to `main-lane compiler (separate worktree)` and was neither killed nor treated
as a violation. Lane checks have absolute lane2 source/target arguments. Only
lane2 compiler processes count for exclusivity/RSS; only the exact process
group launched by the wrapper may be signalled. Heavy lock is mkdir-atomic,
owner JSON/lane/pid/time/unit/path,20min wait, stale>25min AND dead owner only.

## Copied-definition inventory

Existing research capital (not new copies this shift):
- CP3:1786 ActorLifecycleOnly -> CP5ActorLifecycleOnlyExtended:17, only sound
  old-to-extended inclusion; added actual-source own-child Retire/Remove.
- CP3:1824 LocatedOpenEpisodeBlock -> LocatedOpenEpisodeBlockExtended:85,
  exact physical cuts/opening/body/installedness/no-other-lifecycle retained.
- CP3:3156 CanonicalInputPlacement -> R178
  AvailabilityAwareCanonicalInputPlacement:104, earliest compatible interval,
  exact root input order, birth freshness and OWN lifecycle ordering. Root
  retire/remove do not free keys. Never coerce this into strict old placement.
- CP3 CanonicalSchedule record begins3240; canonicalBlock is3256–3257 (NOT3241),
  inputPlacement3265. No schedule copied yet.

## Micro-unit ledger

| Unit | Content | Result | Commit |
|---|---|---|---|
| B1 | childForeignLookupFrame: actual checked foreign action preserves entire child fiber via Metatheory:3829/3843 | PASS1,1.05s | fdf76b61 |

B1 cites the native `applyActionLocalUpdate` and `systemLocalUpdateForeign`;
distinctness is a real child/action-owner inequality. Parent preservation is
not invented from names: the whole lookup is unchanged.

## Status

Checked partial initial capital only. No adjacent swap, relocated R191 fixture,
extended zero-gap completeness or availability-aware root phase yet proved.
No change of the frozen theorem holes or production semantics.
