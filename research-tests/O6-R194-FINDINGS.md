# R194 findings register synchronization

This updates proof-debt status, not the paper's statements or production types.
Paper text re-inspected at2260–2371 (Lemma72 and Theorem73); no full-paper
rereading attestation is made for this shift. Earlier reports remain historical.

| Boundary | R194 checked result | Still open / scope |
|---|---|---|
| Canonical ordinal pairing | `CP5O20CanonicalOrdinalAttachmentSpike.o20ReplayOrdinalBijection` conjugates the original bijection by both real replay bijections. Actual supported births carry the same opposite occurrence plus origin and physical-ordinal equations. | Not a whole canonical pairing; unsupported retained/closing births and roots need their own disposition/ordinal laws. |
| Actual paired successor | `CP5O20ActualHistoryAdvanceSpike.o20HistoryCheckedIterCut` owns both native Iter observations, lookups/guards and checked endpoint attachment. | It consumes two actual Iter edges and an input cut. It neither creates a target edge nor aligns arbitrary canonical traces. Generic last-Finish still exposes observations. |
| E60's two component attachments | `CP5O20OwnCutSafetySpike.o20BlockEndReferenceComponent` (:68) and `o20BlockOpeningReferenceComponent` (:98) derive both from authentic replay origins and original raw uniqueness. | R193's two attachment debts are discharged; its historical report is not rewritten. |
| Own-cut safety | `o20ReachedInversionOwnCutSafe` derives native candidate success, not an independently reconstructed safety packet. | Only literal ZeroGapPending remains beyond base original/reached accepted capital. No general gap-zero theorem. |
| Stopped order | `o20StoppedOrderEqualsGoalZeroGap` (:453) proves the ACTUAL stopped order equals its goal, under quantified gap0 at native reached inversions. | Conditional theorem, not a sibling selector body or unconditional completeness. |
| D5 convergence bridge | No R194 bridge/body attempt. | D5 still CONSUMES the missing all-name endpoint cut. Whole trace pairing, endpoint rebasing and exact present-vestigial treatment remain open. |

## Citation correction preserved

The frozen CP3 source, inspected again in this shift, has:
- `CanonicalSchedule` record at3240;
- `canonicalBlock` at3256–3257;
- `inputPlacement` at3265.

The R192 memo's P2 row already contains the corrected3256–3257 citation; no
spurious new correction or production edit is made. These are CP3 source-line
citations, NOT line numbers in the extracted paper.

## Qualifications which must survive consolidation

1. `ActionRegistrationReplayCorrespondence.replayGeneratedOrdinalPreserved`
   (LocalDiamond:2364) quantifies **generated** births. It is not a replayed-root
   ordinal theorem. Whole-root attachment is an explicit remaining obligation.
2. A unilateral role word and a local checked Iter successor do not create
   whole paired canonical stage data, even with the supported ordinal triangle.
3. The exact endpoint live map/history bijection must be rebased to the supplied
   current-name map for **all names**. Supported/non-vestigial agreement is not
   enough. The earlier physically present-vestigial example does not provide
   both independent canonical capitals and is not a convergence counterexample.
4. `ZeroGapPending` (RightOpeningTransport:99–103) is literally
   `transitionCount gap = 0`, not a convenient name for the four safety clauses.
   R194 proves the non-gap clauses from real capital. The final theorem assumes
   gap0 for each inversion at its own native membership slots.
5. No separate A12Pending is needed inside that exact-zero frozen theorem:
   a gap with zero actual transitions contains no availability-forced root.
   This says nothing about establishing gap0 or repairing the extended/root-
   availability variants being developed by lane2. Per the explicit preflight
   owner clarification, unchanged inherited MAIN baseline modules/fixtures ARE
   checked; those validations do not certify ongoing lane2-created results.
6. Theorem73's final transposition paragraph motivates B, but pure adjacent
   order inversion is not asserted to imply physical adjacency. The conditional
   theorem does not silently close the paragraph's mechanization debt.
7. No new unsafe escape, postulate, hole, partial function, dependent-record
   equality shortcut, frozen deletion theorem call, or scoped-to-raw cast.
   No new mathematical counterexample/erratum is promoted in this shift.
