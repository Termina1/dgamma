# R181 grind-shift audit

## Authenticated start / guard

Start 2026-09-07 **13:47 UTC**, branch `cp5-thm73-scoping`, HEAD
`58712360966dfe2b20b50eedd438ada3dcc2926b`. Tracked tree clean, only permitted
`paper/` and frozen `review-o6-body-adversarial.md` untracked. Idris **0.8.0**;
`--source-dir` and `--check` CLI spelling verified; no compiler orphan.
No new attempts after **17:07 UTC**, safe final gate by **17:32**, timeout **17:47**.

Research-only, seeded serialized compiler invocations via detached Python `-I`.
One new top-level declaration OR one signature/visibility surface per invocation;
fresh 3/3 budget per micro-unit, immediate source commit on PASS. No production
change, unsafe proof, postulate, partiality, new hole, new `with`, local `let`
alias, nonlinear pattern, frozen deletion theorem call or scoped-to-raw cast.
No O17/root/A8 integration, no O20 bodies, no O21 withdrawal proofs, no new
LocalDiamond declaration. O19 body requires explicit supervisor reply AFTER
committed full negative and research safety revision.

R180 A14 already authenticates the five lifecycle edges including consumer
Finish. It is not an empty-origin/full-safety trace. Its actual normalized
provider-value observations, raw edges and produced preservation domains are
reused; exhausted A5/A11/A13/B3 statements will not be retried.

## Micro-unit ledger

Compiler source snapshots, diagnostics, intervals, memory samples and commit
correlation are archived by the R181 check/evidence scripts.

Setup audit command initially rejected only the deliberately not-yet-committed
R181 harness files against its no-untracked gate; no compiler was started.

## Status

Initial census **5 = 1/3/0/0/1** (CanonicalSort/CrossTrace/DeletionChain/
LocalDiamond/Renaming). Full old-safety negative, revision and O19 body remain
unproved; nonempty O20 paired-prefix producer absent; O21 identity is closed
but withdrawal branches only authorized for sizing.

## Unit A — 11-unit mandatory stop (14:10 UTC)

| Unit | Accepted declaration | Immediate commit / result |
|---|---|---|
| A1 | `r181WholeTrace` | `ab0f080`, PASS1,1.041s |
| A2 | `r181TraceAligned` | `f6c87dc`, PASS2,2.093s |
| A3 | `r181EndpointObserved` | `0b6d288`, PASS1,2.099s |
| A4 | `r181EndpointReady` | `9f73ac9`, PASS1,2.105s |
| A5 | `r181ProtocolRank` | `f0abb88`, PASS1,2.066s |
| A6 | `r181PrecedenceRanks` | `519cb79`, PASS2,2.070s |
| A7 | `r181Protocol` | `1be77ec`, PASS1,2.072s |
| A8 | `r181TraceStructure` | `d307a1e`, PASS1,2.079s |
| A9 | `r181TotalityObserved` | `5b51762`, PASS1,13.525s |
| A10 | `r181WholeTotal` | `cd74615`, PASS1,13.501s |
| A11 | `r181InstalledCutsObserved` | **STOP3/3; entirely removed** |

A1 builds the ACTUAL two root insertions, then the five already authenticated
lifecycle edges. The seven-node constructor spine owns all indices; no new
Maybe builder or scalar observer over a nested certified builder was introduced.
A2 preserves the exact opaque `beginEquation` projection from R180: attempt1
wrongly chose a propositionally equal proof token; attempt2 uses the ACTUAL
stored proof. A3/A4 produce final quietness, noFailure and both Active facts by
observing the exact normalized table VALUE and its binding, closing all premises.
A5–A7 admit dependency-free providers at rank0 and dependency-bearing consumers
with empty provisions at rank1; the catalog is empty but the admitted component
universe is not. A6's first overlapping deep-pattern matrix was rejected by
coverage; its explicit nine-case matrix checks. No producer/consumer semantics
were borrowed from R172's empty-key fixture. A8 authenticates full discipline,
count7, and consumer `LBegin1=Nothing` at the SAME two-root source of the provider
block, eliminating the R180 separately-indexed early-cut gap.

A9 observes the exact normalized provider table before running the actual
all-boundaries `checkTraceComponentsTotal`, then A10 closes every observation
and extracts its OWN proof using `isJustTrueWitness`. This is actual whole-trace
Definition69, not endpoint-only totality or an assumed totality certificate.

### A11 exact stop

1. 11.392s: rewriting the normalized table did not change the installation-only
   goal (the installed bit does not inspect the table). No fallback scalar
   `Refl` observer was attempted.
2. 1.055s: switched the BODY to existing raw-edge `lAdvanceStartsInstalled` and
   actual named-fiber `installedAtFound` projection. One excess closing
   parenthesis in the explicit accumulator was a parser rejection.
3. 12.449s: repaired only that parenthesis. The provider's ACTUAL stored
   accumulator contains normalized identity composition, while the explicit
   candidate encoded an extensionally equal callback with a different suspended
   case tree. `installedAtFound` demands exact fiber equality and rejected the
   lookup equation at this function-valued field. This is a conversion wall,
   NOT proof that the installed flags or safety are false. No function
   extensionality, unsafe cast, local alias, hidden hole or fourth attempt.

The entire A11 declaration is removed and exact A10 source freshly restored.
No A11 source/TTC/TTM was left in a standalone probe (it was an uncommitted
addition to the existing module, whose successful cache is retained). Failed
snapshots/transcripts persist only in the evidence archive. A uses11/16 units;
the remaining5 are blocked at the mandatory gate, not silently rebudgeted.

### Full current-safety negative / exact remainder

**Not completed, and NOT shown excluded by the surface.** Now proved: actual
empty-origin count7 trace; real provider/consumer edges; alignment; complete
component totality; genuine protocol/discipline; quiet/noFailure/both Active
finals; exact pre-provider early consumer rejection. Remaining:

- InstalledTrace witnesses at the exact provider/consumer body cuts, without
  rebuilding/equating their function-valued accumulator. A generic observed
  lifecycle projection is the next DISTINCT prerequisite, not another A11 body.
- Both full LocatedOpenEpisodeBlocks, source-coordinate decompositions,
  no-earlier/no-later fields, BlockBefore, 3x2 disjoint ranges, coverage and
  ActorBlockDecomposition; raw insertion uniqueness.
- Actual component-specific TraceIndependent (both generated monoid commutation
  and outcome stability). The consumer has an empty program, but its root
  insertion's effect map clears table1: it is NOT universally identity.
- ReplayInvariantBundle provenance/rank/support fields, derivable from the newly
  genuine trace inputs after assembly, and full old-safety inhabitant.

No safety clause revision/map migration/positive or rejected revised fixture;
no O19 body request or attempt. The original producer path at CrossTrace699–714
still consumes safety, not constructs it; its selector is holed. O19 body banned.

### Supervisor ruling at the A stop

A11 stop ratified; A1–A10 substantive negative capital retained. Unit B proceeds
first. AFTER B, A's remaining5 units are explicitly authorized for a DISTINCT
generic observed-lifecycle→installed prerequisite (R163 Wall-2): actual observed
value and lifecycle equation produced on the checked Begin/Finish transitions,
then installation cuts as projections. Never another A11 installation-cut
statement or guessed accumulator. Unit C follows. No surface/body permission.
A-stop package PASS16.605s,207/207 seeds. Frozen audit passed at7ecf289.
