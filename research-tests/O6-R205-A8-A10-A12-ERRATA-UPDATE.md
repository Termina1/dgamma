# R205 interpretation and errata boundary

This is a production-definition migration, **not a new proof of Confluence**.
The exact owner-signed CP3 patch was independently reviewed faithful-with-notes.

- A8/A12 replace the earlier *mechanization's* strict all-roots-before-all-lifecycles policy with source-aware terminal-earliest placement, preservation of external orchestration, and root-before-its-own-lifecycle. R172's old strict-root-first/root-before-another-child conclusions are retired; their failure is not a counterexample to the revised predicate or a newly established error in the paper.
- A10 distinguishes `ActorLifecycleCore nameEq` from attached `ActorLifecycleOnly nameEq`. Source-owned child controls and ordered same-bundle forced roots are not admissible targets for a blind old-actor-only→old-extended reverse coercion.
- CanonicalSort's existing two-case scan remains a **subset** of Core. Its first-attempt checked migration neither makes it a general core scanner nor produces forced phases. The final placement uses the already-owned `accountedExternalInputs`; no new premise or oracle was introduced.
- Five LocatedOpenEpisodeBlock evidence fields are erased. `BlockBefore` now uses the agreed append association. R181's actual concrete bodies and Refl block order still check after only constructor/wrapper migration.
- O19Surface independently checks unchanged (V165). CrossTrace's O19 body hash is unchanged but **the CrossTrace module has not been revalidated**, because its imported old block-word classifier fails. Unchanged bytes or retained TTCs do not prove compatibility.

## Semantic residues, not lexical repairs

1. `CP5ActorLifecycleOnlyExtended`: old inclusion domain no longer denotes just the strict grammar.
2. `R18ExternalOrderProducerPositive`: old two-case body-head classification does not cover the enlarged grammar.
3. `R172O17OpenParentRootReuseCandidate`: retired strict-placement refutation.
4. `CP5O20RightOpeningTransportSpike`: NoGeneratedChild alone is not an owner-frame theorem for existing-child controls/attached roots.
5. `CP5O19OriginalBlockClassSpike`: new node kinds need a semantic observation/classification argument.

The separate R137 constructor-telescope failure is recorded as **pre-existing**, per supervisor ruling; no CP3 causation or repair is claimed. Exact native errors, source hashes and all blocked import paths are in the rebuild-state and broken-module manifests.

## Tier 2 remains open

General forced-phase producer; general normalizer; the four lifecycle-shape premises; cross-bundle/source-owned control obligations. No hole body, postulate, cast to raw scope, arbitrary normalization witness, or semantic re-proof was added to discharge them.

Production rebuild completeness, disposition completeness, and all-research-native-PASS are different facts. R205 currently has the first two, not the third. Lane2 release/re-seeding remains a supervisor decision.
