# O6 L2R14 CP3 diff-draft research overlay — PARTIAL

Baseline574a8098; lane2 cp5-thm73-lane-a8a10. Research only: NO src/, package, README/NOTES, protected CP3/CP5, or predecessor changes. No integration claim. **NOT SIGNABLE / Owner Option A.** Source boundarya33379be; ordered capsA14/B16/C10 reached, no extension; D≤3 docs-only. See the grind audit, derived origins/ledgers, immutable raw archive and independent verification for actual commands/receipts.

## A — native phase-release extraction, NOT full phases

`produceForcedPhaseOrigins`7aa1f64c derives an authentic event observation and GLOBAL own-child ORemove release FROM `produceForcedPhaseEntry` (not a restated catalog assumption). `phaseSeedActor`, `phaseReleaseAtOrdinal`, `phaseSeedRelease`, `phaseReleaseNativeOwner`, `phaseProducedReleaseCount` establish event actor/check, native release/source classifier, true release maximum count and birth bound. The seed already uses releaseOrdinalScan, so this extraction needs no old/new whole-trail scan agreement.

**OPEN:** `phaseReleaseCheckToNativeOwnedInterval`: align the observed event to its native occurrence/source/actor, derive the preceding contiguous earlier lifecycle/core, localize the release INSIDE that core, and align its release-ending core count. Then prove the complete maximum/anchor/local core fields required by `produceForcedRootPhases`. That producer remains UNIMPLEMENTED; no new general inhabitant of the predecessor phase TYPE or reproduction of all L2R12 phase fields. Global count transport is NOT the maximal contiguous core endpoint theorem.

## B — general original adjacency, producer-owned fixed iteration

`scanCatalogActionQuery`7c62f2d4 derives queries FROM authentic scanRootCatalog membership. `selectedAlignedAdjacent`22662e21 gives two actual original native edges for ANY selected positive-distance cut at the EXPLICIT `AlignedTransitions` premise; their intermediate state is literally shared. It is not structural equality of independently supplied DecEq dictionaries and does not erase that premise.

`fixtureIterationsFromProducers`6310c426 constructs actual single1→0 and bundle2→1→0 DistanceIterations FROM L2R13 terminalFixtureMoves and firstBundleFromProducer. Its whole RegistryExtensional endpoints follow by iterationEndpoint and actual native terminal zero. The old admitted moves / fixtureIterations are NOT construction inputs. `fixtureProducedMeasuresAgree`98ce2c65 checks all THREE numerical before/after measures against D8, NOT proof-record equality. `fixtureDistancePathFromProducers`65060ef9 derives numeric2→1→0 solely from the actual producer decrements/measures and zero.

**OPEN:** scalar frame→GLOBAL oldFrame/newFrame, phase/front/NeverRetired/raw-uniqueness and native release identity/ordinal transports, aligned-native domain/discharge, general Begin/Advance-to-move integration, arbitrary suffix extension and outer induction. GeneralAdmittedMoveExistenceUnique remains TYPE-only; `normalizePhaseDistance` remains ABSENT; no PhaseIterationResult. Fixed iterations now genuinely use general LOCAL move producers, not a general phase-preserving existence or normalization theorem.

## C — LOCAL square produced from explicit per-action shapes

Authorized route: raw registry-operation commutation + native packet determinism, NOT the forbidden literal exchange-chain intermediate native8→9 Remove. `LocalSquareActionShapes`8707cebc contains five immediate old/new action shapes, common actor payloads and separate early/late root shapes, NO local/whole endpoint equality. `coreNativeSnapshotFromShapes`951907be folds five real packet edges then cancels the transient child. `packetWholeTransportFromActionShapes`6acc5ef4 PRODUCES PacketWholeEndpointTransport, including its LOCAL RegistryExtensional square, from two such folds and operation algebra. Old/native suffix decomposition and inherited Root/Retire NativeSuffixFrames remain explicit.

`fixtureLocalActionShapes`651aa8e8 discharges all raw action shapes from actual retained native packets and immediate raw-operation Refl only. Following the narrowly authorized freshV0 visibility companionfce77af0, `fixtureLocalSquareFromActionShapes`a33379be constructs the transport, proves its computed late state is actual6, reproduces the EXACT LOCAL6~16 and DERIVES whole7~17 by native S suffix replay. No f50b73f8 local proof, old whole endpoint equality or old transport record is supplied. These are equality/theorem-type results, NOT proof-record identity or a new re-export of the full CoreRestorationFixture record.

**Exact general residue:** **raw per-action state-shape premises are general hypotheses, discharged only on the fixture**. This authorized reduction is checked; an unconditional generic native-shape producer is not. C2 deletion commutation is also checked independently; the final algebra uses C1 replacement commutation and C3 complete transient-child cancellation. NO split Remove, no intermediate Remove state, no general native variant of the forbidden fixture statement. No >18GiB declaration or unsafe escape.

## Correspondence boundary / next work

All40 new declarations are recorded separately in O6-L2R14-DECLARATION-ORIGINS.md. Three record TYPE declarations are neutral about general inhabitance; the general adjacency producer and fixed shape fixture provide only their stated inhabitants. Existing uninhabited theorem TYPE aliases are NOT promoted by wrapper names. This overlay does not change the paper Thm73 status.

Next: first close the named A native interval seam; then consume the checked B original adjacency in actual invariant/global-frame-preserving moves and a normalizer. For C, remove raw shape premises only through a separately authorized semantic producer, not by resurrecting the forbidden native split Remove. Main/protected sources and predecessor artifacts remain frozen.
