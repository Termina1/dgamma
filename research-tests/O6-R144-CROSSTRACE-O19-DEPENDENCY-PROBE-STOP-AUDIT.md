# O6 R144 — CrossTrace O19 dependency countershape probe stop audit

## Why the first CrossTrace hole stopped before its body

After O17 was restored and audit-stopped, R144 opened only
`operationalAdjacentBlockSwapSpike`.  Its input `AdjacentActorSwapSafety`
excludes the two direct generated-child licensing directions, but it does not
state that the actor blocks are incomparable in the endpoint support relation
or that the right block's opening is applicable before the left block.

That absence is material to the implementation: the public A/A local diamond
requires an actual checked `earlyRight` transition.  A provider block followed
by a dependent consumer block is a plausible accepted source ordering, but
transposing it asks the consumer to begin before its provider becomes Active.

Per the countershape-first rule, no O19 body was attempted.  A disposable
calculus fixture used the public `CalculusChecks.providerComponent` and
`consumerComponent` and fixed the critical pair to provider `L-Finish` followed
by consumer `L-Begin`.

## Probe attempt accounting

The disposable fixture exhausted **3/3** attempts and was fully removed.

1. Missing direct `Coeffects`, `Core`, and `Unified` imports prevented the
   intended state definitions from elaborating.
2. With the exact imports, the crucial negative reduced and checked:
   consumer `L-Begin` at the pre-finish state is `Nothing`.  The checked source
   equations then stopped only at explicit well-formedness normalization.
3. Staged raw/check equations established the provider `L-Finish` raw step, but
   full checked packaging stopped at `fiberViewInvariant`; the post-finish
   consumer resolution also required a producer-owned exact `targetFiber`
   equation rather than bare reduction.

The attempt did not reach a complete refutation of the full O19 telescope, so
no surface revision is requested and no partial fixture is retained.  The
next authorized O19 campaign must start with small, producer-owned exact
well-formedness and target-resolution packages for this same public provider /
consumer pair.  Only after a checked full countershape or a theorem excluding
it may the operational producer body or any non-quantity surface revision be
opened.

`operationalAdjacentBlockSwapSpike` therefore remains at **0/3 body attempts**
and is semantic-probe-stopped.  The CrossTrace class may proceed serially to
its independent support-order matching obligation; no later O19 operational
selection body may pretend this stopped producer is established.
