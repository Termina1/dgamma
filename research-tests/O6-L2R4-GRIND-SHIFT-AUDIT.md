# LANE 2 L2R4 — checked research shift audit

## Boundary and result

- Worktree ONLY `/Users/vyacheslavshebanov/Work/dgamma-lane2`, branch `cp5-thm73-lane-a8a10`. Start exact **61d262b2976f9ab7c52fb500c83c307a72f29c49**, clean tracked/untracked tree; all three required L2R3 audit/addendum/origins documents read first.
- Last proof commit **d7b3f304**; immutable final plan/tools **d39d786a** published BEFORE V1. **A22 capped; B14 reached with B7 fully reverted, B15–B18 explicitly left unused by supervisor; C12 capped.** 47 retained declarations in15 new modules, each separately fresh checked and immediately guarded committed. No new source attempt after C12.
- **Result is PARTIAL overall:** Unit A's localization, both universal no-straddling instances, nonvacuous fixture-region coverage and both non-conditional unchanged zero-gap applications are checked; native orchestration-domain replay is checked; global fixed-anchor arithmetic and both native moved global zeros are checked. Lifecycle dispatch/full `single`, whole R191 replay and a global operational normalizer remain OPEN/PARKED.
- No main-worktree cd/read/edit/build/commit; no src/, research/, root-doc, package, O20/O19, LocalDiamond, canonical/cross-trace/renaming edits. No retained predecessor file edited; no body-only companion bundle gated or used. No subagents launched by this child worker. The paper ruling is inherited supervisor authority, not a new paper/PDF audit here.

## Unit A(i) — universal actual-step localization

**A5 cb58200d** inducts on the unchanged `OrderedForcedRootBundle`; **A6 8e75dd58** lifts the SAME local native occurrence through its core, opening, physical prefix and suffix with the actual member/block decomposition equations; **A7 2af1e39a** selects the local position by subtraction/count equalities.

Exact theorem (universe/state implicits retained in source):

```idris
attachedInteriorRoot :
  (member : AttachedBundleOccurrence name key world error value nameEq keyEq global action ordinal) ->
  (cut : Nat) -> LT (bundleOffset member) cut ->
  LT cut (bundleOffset member + transitionCount (memberBundle member)) ->
  RootInsertionAt name key world error value global cut
```

`RootInsertionAt` stores the actual inserted name/component, **LocatedActionOccurrence (OInsert root Root component) global**, and producer-owned `locatedActionOrdinal rootOccurrence = cut`. It is not merely an action-word tag. Quantification is over ANY authenticated bundle occurrence/decomposition, not selected catalog members. No literal equality of independently reconstructed SystemState values is used.

## Unit A(ii) — universal no-straddling, both fixtures

A8–A11 authenticate native ordinal lookup against arbitrary dependent occurrences. A9's `nativeActionAt` is an **erased total specification**, since inherited `transitionAction` requires intermediate endpoint indices erased by Transitions; this is deliberate quantity, not a partiality escape. Unit C's separate actual-trace annotation is runtime executable via the Fired constructor.

**A12 120fbca8** `beginCutNoStraddling` proves ANY observed LBegin cut cannot lie strictly inside ANY attached bundle: localization would put an actual root OInsert at that same ordinal, contradicting observed LBegin.

- **A13 b8dd0ec6** `smallNoBundleStraddles : NoBundleStraddlesCut %search %search smallTrace 5`.
- **A14 8baa59f7** `barrierNoBundleStraddles : NoBundleStraddlesCut %search %search barrierTrace 6`.

Native Begin2 observations come from the SAME explicit checked fixture traces. This closes L2R3 A23's universally quantified producer, not just the chosen catalog's endpoint arithmetic.

## Unit A(iii) — general coverage producer and NONVACUOUS fixtures

**A15 f1c6e75d** `catalogNormalForm` constructs the exact unchanged AttachedNormalForm from lookup-complete actual bundle catalog coverage. The exact stronger remaining input for an arbitrary region is:

```idris
(ordinal : Nat) -> (action : Action name key value world error) ->
nativeActionAt region ordinal = Just action ->
AttachedBundleOccurrence name key world error value nameEq keyEq global action (offset + ordinal)
```

This input covers EVERY native action in the supplied root-only region, hence every root-orchestration occurrence. It is not a generic mixed-region/root classifier or a global normalization theorem. **A16 51a6b4a5** `catalogConsObserved` proves completeness by one Nat split from an actual head member and a recursively complete tail, transporting action/count equations.

**A18 8a254f81** `fixtureRootRegions` discharges this callback on BOTH actual pre-attachment regions:

| Region | Width | Catalog members used | Quantification |
|---|---:|---|---|
| C12 native [4,5) |1| L2R3 A22 c12CatalogR | Every actual dependent occurrence in the one-edge root region |
| Barrier native [4,6) |2| L2R3 A22 barrierCatalogR / barrierCatalogS | Every actual dependent occurrence in the two-edge root region |

The record owns literal native region equations and widths1/2. This is **NONVACUOUS AttachedNormalForm coverage**, not just three selected witnesses or NF on an empty gap. The general classifier/last-release assignment obligation remains open beyond these regions.

**VACUOUS-RISK qualification:** the old risk is resolved for these two NONEMPTY root regions by a general lookup/cons coverage producer and its real instances. The POST-attachment residual NF used below is still necessarily/honestly vacuous. Arbitrary-trace mixed-region coverage and a global attached schedule are not claimed; the warning persists for those open normalization obligations.

## Unit A(iv) — unchanged theorem applied without open parameters

**A21 58b71b4f** `smallGapViaNormalForm` and **A22 b042b76e** `barrierGapViaNormalForm` APPLY the unchanged L2R3 `attachedZeroGapInNormalForm` with **no theorem premise left as a caller parameter**.

| Premise | Source |
|---|---|
| earlier/later/ordered | Exact original smallAttachedBlocks / barrierAttachedBlocks fixture data |
| physical | A19/A20 typed, simultaneous observation of the SAME native residual trace as NoTransitions, then congruence on transitionCount |
| offsetPhysical | Same typed observation: whole attached body ends at5 /6 |
| covered | Original L2R3 residual-head coverage, transported along the exact observed gap equation (empty case Unit) |
| normal | Original L2R3 residual AttachedNormalForm, transported to the exact residual gap; **honestly vacuous** |
| separated | **NEW genuine all-bundle producer:** A13/A14 no-straddling + unchanged L2R3 separateBundleObserved, actual isLTE end/cut decision |

No `forcedRootGapCount` field is consumed in these applications. Physical residual emptiness nevertheless IS already fixture data, and the theorem application is not a new discovery that an unknown nonempty gap normalizes to zero. Nonvacuity belongs to the independently proved pre-attachment coverage above. No nonempty root region is falsely asserted to have gap0/separation from its containing bundles. No L2R3 premise is weakened.

## Unit B — original-edge-only role capital, ratified lifecycle park

See **O6-L2R4-B7-STOP-AUDIT.md** and **O6-L2R4-B-ROLE-AUDIT.md**, including both exact supervisor rulings.

| Role | Checked status | Commit / declaration |
|---|---|---|
| ROOT OInsert | Proved, original edge only; no ownership premise | **0f2afa91** replayInsertAfterRetirement |
| Other-parent child OInsert | Proved for EVERY Parent, including retired-child-as-parent | same B13 |
| Foreign ORemove | Inherited proved; reused unchanged | L2R3RemoveDispatch.replayRemoveAfterRetirement |
| Foreign ORetire | Proved, original edge only | **0ff957b3** replayRetireAfterRetirement |
| LBegin | PARKED | B7 observed dependent Bool wall,3/3 exhausted/reverted |
| LIter/LFinish | STRUCTURAL PARK | L2R2 square is Remove, not Retire; missing retirement target/provider frame crosses stopped B7 seam |
| Other lifecycle constructors/tags | OPEN | Not silently dropped from generic Action domain |
| Dispatcher assembly | **Orchestration-domain ONLY**, total by all8 Action constructors | **a6579477** replayOrchestrationAfterRetirement |
| Full all-role `single` | OPEN/PARKED | Exact unchanged callback remains in L2R2ForeignReplay |
| Whole R191 ForeignReplay instance | OPEN/PARKED | Cannot supply nonLifecycle for actor1 lifecycle edges or substitute deletion-state square |

B1–B4 prove exact ordered-binding distinct replacement commutation; B5/B6 and B8–B13 use original native single-constructor views, derive alternate raw/checked edges and target validity, and use runtimeSnapshot coherence at arbitrary well-formed retired-source snapshots. No alternate checked edge or alternate endpoint equality is a user premise in the final role producers. B14 explicitly retains its `isLifecycleAction action = True -> Void` domain premise and is never mislabeled as full `single`.

**B7 stop:** 3 rejected attempts; full file removed before V0 fresh validation of retained RetireReplay. `providerIn` retains hidden dependent `flag` versus `True` even after explicit parameters and lifecycle observation equations. Supervisor ratified the stop and future L2R5 **new observed-Bool+equation statement shape**, not a B7 restatement. No other 3/3 exhausted unit. B12 succeeded on its third/last attempt, not a failed3/3. B15–B18 unused by express ratification.

## Unit C — ratified release-anchor global measure

See **O6-L2R4-C-DESIGN.md** for the verbatim ruling and precision notes.

A faithful compatible-cut count based on `rootCutCompatible` would give **1,1,0** on Begin2,R,S → R,Begin2,S → R,S,Begin2: moving R exposes a fresh S inversion because compatible cuts cannot cross a root input. It is not a strictly decreasing global step measure. Native-key-only counting fails moved zero for always-free barrier S. Supervisor ruled to retain EarliestAvailableRootBirth as LEGALITY and use **fixed bundle-release anchors** as the variant's measure.

- **C1–C5, final91a6e3bb**: `anchorInversions nameEq history events`, executable total fold. Foreign lifecycle events contribute to a birth only after its assigned release marker; all roots in one bundle share that marker; non-forced roots use Nothing and count from trace start. Roots themselves DO NOT reset history.
- **C6/C7 c8e8c28e**: native Action classifier + executable `annotateAnchors` over actual checked AvailabilityTrace. Every lifecycle constructor is included. Root anchor policy depends on occurrence ordinal AND name; release markers are actual ORemove ending cuts. Correct assignment/transport is still a semantic obligation, not inferred by this function.
- **C8 dbed501d**: exact-one local algebra for any historical context and suffix, observed name decision.
- **C9 bc83105b**, **C10 fce4098d**: prefix induction gives:

```idris
anchorSwapDecreases :
  (nameEq : DecEq name) -> (actor, root : name) -> (anchor : Maybe Nat) ->
  (front, back : List (AnchorEvent name)) -> Not (actor = root) ->
  (history : List (AnchorEvent name)) ->
  anchorInversions nameEq history (front ++ (AnchorLife actor :: AnchorBirth root anchor :: back)) =
  S (anchorInversions nameEq history (front ++ (AnchorBirth root anchor :: AnchorLife actor :: back)))
```

This is general exact-one algebra with arbitrary prefix **AND suffix**, not a generic native square/replay producer. Applying it to native admitted moves requires correct fixed annotations. The fixture package performs those real applications using the inherited checked availability-aware R and S phases.

**C12 d7b3f304** `anchorFixtureEvidence` includes:

- Actual full single-R moved native trail from cut0 (L2R2 smallMovedTrail), including pre-release Begin0/Finish0/Retire1/Remove1: global `singleGlobalZero`.
- Actual full R/S moved native trail from cut0, formed by the SAME four checked prefix edges plus L2R3 phase-owned movedTrail: global `bundleGlobalZero`.
- Exact native annotation equations, actual root release-cut4, original smallRootEarliest legality proof, actual R and barrier-S catalog offsets4.
- Full original and middle checked chunks; global counts **2→1→0**. Exact-one equations invoke C10 with unchanged prefix/suffix, not a weakened local-only measure. Both global zero equations retain the old lifecycle prefix rather than dropping it.

**Remaining normalizer obligation (not a proved theorem):** construct total native admissible swaps whenever a correctly annotated schedule has positive anchor count; preserve last-release/barrier assignment, all occurrence/generation identities, external order, coverage and native prefix/suffix replay; iterate using Nat well-founded descent until count0; then derive the full attached normal form using structural/decomposition facts. A zero FOREIGN-lifecycle count alone does not exclude same-actor lifecycle or intervening orchestration, so no bare zero⇔physical-immediacy/canonical biconditional is asserted. C12 cap leaves no iteration declaration/proof; the two actual fixture steps are checked.

## Guards, failed attempts, resources and evidence

- Idris2 **0.8.0**. `%default total`, `%unbound_implicits off` throughout. No new with, let aliases, holes, believe_me, assert_total, assert_smaller, postulates or partial definitions. Mentions of assert_total in compiler diagnostics arise from Idris's own Nat literal implementation, not source escape hatches.
- 47 source commits; **75 compiler invocations =63 PASS /12 rejected**, including V0 post-stop retained-source check and V1–V15 final checks. All final15 own-target Building lines/exit0/current hashes PASS. Original development attempts60 (including V0), final validations15. No invocation ID reused; no source mutation/interruption.
- Failed attempts: A2-1 reserved word `prefix`; A7-1 LT/LTE transitivity transport; A9-1 erased endpoint accessibility; A16-1 plusSuccRightSucc direction; B7-1/2/3 stopped dependent Bool seam; B8-1 missing direct Elem import (diagnostic despite exit0); B12-1 missing direct runtime-bindings lemma import, B12-2 rewrite over unsolved Boolean metavariable (fixed by explicit congruence); C11-1 missing direct attached-record imports (diagnostic despite exit0); C12-1 closed original/middle counting conversion (replaced by C10 algebra, preserving exact type).
- Highest sampled RSS **4,870,048 KiB (~4.64GiB)** at C11-2; no check approached19GiB. **0 heavy locks/waits/stale removals**. 5 unique main-lane compiler PIDs observed, classified `main-lane compiler (separate worktree)` and never signalled. Recorded compiler seconds sum ~143.48; this is NOT elapsed shift time.
- Seeded build retained, only TARGET .idr touched; every path/old→new mtime recorded. No mass touch, build deletion, cold/package build, foreground Idris check, unguarded commit, frozen-source edit or nested R174 producer re-entry.
- Python `-I`, detached, one owned compiler at a time, sampled target-mutation interruption, unlocked18GiB/heavy48GiB RSS guard. Target's own Building line mandatory; exit0+Error is rejected (two real diagnostics above). SHA bytes are rechecked at every source/artifact guard; sampling is not continuous filesystem surveillance.
- Copied actual-runner AST regression tests: **14/14 PASS**, including missing/empty/outside targets, wrong/main-worktree Building lines, strict caps/attempts, no retry after PASS, error-exit0 rejection, source-only companion authority and correct ownership classification. No compiler or signal is invoked by these tests.
- **O6-L2R4-DECLARATION-ORIGINS.md** gives all47 exact names/lines/commits/PASS attempts and every copy/recipe delta. **MICRO-UNIT-LEDGER.json** includes B7 stop and unused slots. **COMPILER-EVIDENCE.tar.gz / COMPILER-LEDGER.json** authenticate exact source/log/record bytes, target touches, plans/receipts and hash matching. Byte matches (including baseline) are independent of PASS/commit authority.
- **INDEPENDENT-VERIFICATION.json** is a separately executed compiler-free reconstruction of git objects, every source receipt, immutable plan, archive and clean scope. It is NOT independent human mathematical proof review; that required review gate remains with the parent. Publication/report commit receipts cannot recursively be in the archive and are verified from the append-only live ledger.

Reproduce only from this worktree with seeded build retained:

```text
python3 -I research-tests/run-l2r4-evidence-tests.py
python3 -I research-tests/run-l2r4-independent-verify.py
git diff --check
git diff --stat 61d262b2 HEAD
git status --short
```

Do NOT rerun immutable V1–V15 IDs. Any further fresh validation needs a new bounded plan/IDs and owner authorization; no additional source declaration is authorized after these caps/parks.

## Status

**Proved:** universal actual-step bundle localization; universal no-straddling on both fixtures; general lookup-catalog→NF and nonvacuous width1/2 fixture-region coverage; both exact unchanged zero-gap applications with produced universal separation; original-edge-only ALL-parent OInsert and foreign ORetire roles, inherited Remove and total orchestration-domain assembly; exact ordered replacement algebra; runtime anchor measure/native annotation; arbitrary-prefix-and-suffix exact-one algebra; both full native moved global zeros and real global2→1→0.

**Partial/conditional:** arbitrary catalog assignment, arbitrary native fixed-anchor annotation/last-release transport, semantic normalizer descent and zero→full attached NF.

**Parked/open:** lifecycle role replay in explicit observed-Bool shape (future new L2R5 statement), all-role single, whole R191 ForeignReplay instance, operational well-founded global normalizer, production integration and Theorem73. No production or frozen statement was weakened to conceal these gaps.
