# L2R13 lane-owned Tier-2 synchronization overlay

**PARTIAL / COMPLETE CURE NOT SIGNABLE. Owner Option A. No production change.**

Supersedes only the L2R12 overlay's connector status. All L2R1–L2R12 Idris
sources, src/ files, the original CP3 draft and protected root documents remain
byte-unchanged. The sole predecessor changes are the explicitly authorized P2
wording repairs in L2R12 origins/micro-ledger/publisher. No evaluator, external
order, endpoint relation, admissibility rule or Theorem73 conclusion changed.

| Connector | Checked promotion | Exact residue |
|---|---|---|
| Forced → Just anchor | `forcedAnchorJust` **6a750808** derives the observed Just maximum from the actual classifier's per-entry test, including unseeded barriers. Both nested native `any` hits, equality/bounds, filtered catalog membership and the library `concatMap` LEFT fold are decoded. | No native contiguous core is inferred merely from an anchor. |
| Authentic seed and birth | `produceForcedPhaseEntry` **7f6c47db** simultaneously produces the anchor, authentic accepted seed through `phaseSeedAtAnchor`, and actual root birth FROM `scanCatalogBirth`. | General `produceForcedRootPhases` remains **PARTIAL/unimplemented**: interval/lifecycle ownership, release membership WITHIN that interval, and release-ending core/maximality alignment remain OPEN. |
| Reproduction and count transports | `forcedAnchorsReproducePhases` **60ed426b** reproduces all THREE L2R12 phase anchors; `phaseMaximumIndex` **833afacf** and `phaseAnchorCountBound` **0e6434f8** derive authentic selected scan index, successor count and anchor≤birth. | Anchor reproduction is NOT a general phase inhabitant or equality of phase proof records. Universal maximum upper-bound transport and physical core indexing remain separate. L2R12 whole-trail agreement remains explicit generally; its existing fixture discharges are unchanged. |
| Native root successor transport | `checkedRootAcrossExtensional` **b1456c13** derives a native successor, original tag and RegistryExtensional equation from an ORIGINAL root edge at extensionally equal states. `squareFollowingRoot` **42b8c3ab** applies it FROM a ClassifierSquare and the actual following root edge. | Finite declaration-scan equality is an explicit scalar native frame, not proved from RegistryExtensional alone. Identification of the selected adjacent original root source/dictionaries from catalog birth remains OPEN. |
| Actual local admitted move producer | `terminalSquareAdmittedMove` **6f813b53**, visibility-only correction **54f188ce/V0**, builds full native prefix+pair runs, physical occurrences/ordinals, exact words, current cuts, square endpoint and decrement FROM `totalDistanceOneLeftFromFrame`. | Terminal-pair scope with explicit forcing and global scan decompositions. NOT a phase/front/NeverRetired/uniqueness-preserving general existence theorem. Those inherited global obligations remain unchanged and explicit. |
| Following-root extension | `extendAdmittedMoveByRoot` **5925ac4f** extends a produced move; whole endpoints come from independently PRODUCED native root replay plus determinism. A given checked new edge selects the desired native target. | Native scalar frame, extended forcing and distance decompositions remain explicit. Not arbitrary suffix replay. |
| D8 moves FROM producers | `terminalFixtureMoves` **4bf66cb8** gives single1→0 and bundle-terminal1→0; `firstBundleFromProducer` **b7c91f6a** gives bundle-first2→1 through the root extension. All use the SAME actual D8 native trails; no hand-built move fields or firstMoveSnapshot are projected. | No separate equality-to-hand-built-measure theorem or newly packaged iteration. Generic Begin/Advance adapters, global phase/release identity/ordinal/other-root transports, outer induction and `normalizePhaseDistance` remain OPEN. |
| Native suffix endpoint induction | `NativeSuffixFrames` **1a8e70cf** has no endpoint field. Root/Retire endpoint lemmas **ca1ae6d8/9825bdd8**, then `nativeSuffixEndpoints` **ffd2e55a**, derive arbitrary-length endpoints from actual checked per-edge equations and Root scalar frames. | Kinds are Root/Retire only; scalar finite scan framing is explicit. |
| Arbitrary-family whole packet endpoints | `PacketWholeEndpointTransport` **a16feec2** owns native passage packets, late-root/suffix split and `localCoreRootSquare`; `packetWholeEndpointsFromLocalSquare` **ea89f008** derives WHOLE endpoints. `coreContiguityFromLocalSquareFrames` **81f0aa10** combines it with the general core-word/count/location/placement route. | **the five-edge core/root LOCAL square is an explicit justified hypothesis, not derived from per-action swaps**. There is NO whole endpoint-equivalence hypothesis. The inherited actor2/child5/Nat/Bool/Unit/String core template remains fixed; state families and surrounding native traces/endpoints are arbitrary. |
| Fixed full core contract | `fixtureLocalCoreRootSquare` **f50b73f8** proves LOCAL6~16; `fixturePacketEndpointHypotheses` **3b02bb58** supplies actual packet/late-R/S frames. `coreRestorationViaNativeEndpointFrames` **6b44f2a7** inhabits CoreRestorationFixture FROM the GENERAL route. | Whole7~17 is DERIVED through native S replay. No `contiguityEndpoints` whole equality, old CoreRestorationFixture projection or native split Remove is used. This does not inhabit the unchanged universal hypothesis-free core obligation. |

L2R12's closed restricted-retirement callback, validity-strengthened actual
R191 whole replay and other retained connectors remain checked capital with
their original exact scopes; this shift does not weaken `ForeignReplay.single`.

**Evidence boundary:** 38 retained declarations, A14/B14/C10, source HEAD
`6b44f2a7`. Every publication has a guarded fresh-PASS receipt. B10 alone has an
explicitly authorized zero-declaration visibility/single-sentence correction.
C10-1's two-Building dependency rebuild was REJECTED; C10-2 passed freshly with
one target Building line. No exhausted statement, proof escape or production
edit. The audit and independent mechanical archive verifier are not a substitute
for parent-owned mathematical review. Complete Tier-2 cure remains **NOT SIGNABLE**.
