# R202 D1: capped proof freeze and immutable validation preflight

R201 FINAL GATE at fbe3ae90 ACCEPTED as a checked partial milestone; D4 sealed at bc791641; reviewer launched.

- Baseline: `bc791641`, main branch `cp5-thm73-scoping`.
- Proof freeze: `2a28e44e058ecf30a587c9151fc5de0bf52254c6`.
- B: 14 micro-units, 15 invocations, 14 checked/guard-committed declarations. Cap reached; B15 not started.
- A: 16 micro-units, 19 invocations, 16 checked/guard-committed declarations. Cap reached; A17 not started.
- Four failed development snapshots remain rejected: B2-1, A8-1, A13-1, A16-1. Each passed on attempt2. No exhausted/reverted unit.
- C: ineligible, zero attempts. D: at most four artifact commits; D1 is this preflight.

## Exact boundary

B produces same-segment retained birth/own-suffix retained closing witnesses, exact index order reflection, and an exact parent/component-preserving retained birth packet. It does **not** join the three deletion segments, derive the selected-center birth location from a removed-center parent close, inhabit `o20DeletionRetainedClosingBirth`, or prove global discarded coverage/all-name rebase/D5. No new-field insufficiency established.

A removes arbitrary live-environment inputs from an A18-style supported physical Insert producer: both preceding-trace live tables and their native scans are computed. Its all-name successor remains conditional on the real predecessor cut. Local activation-index laws show deleted births consume zero positions and retained births consume exactly one in the observed same activation. These are **not** canonical prefix position preservation, genuine skip construction, paired histories, or modulo synchronization. Fixtures explicitly distinguish actual prefix scans from conditional joins and local classifier-branch algebra.

## D1 evidence

`O6-R202-FINAL-VALIDATION-PLAN.json` is immutable, SHA256 `e0f83271b9b493b3f602771c8c300a3e0fc4c06891e1967a7efa80f7302abb34`.

All 171 inherited applicable main sources + 5 new sources = 176 source targets, plus the seeded production package = 177 checks. All invalidated imports inside the inherited 280-source inventory are included and topologically ordered. Unchanged research prerequisites outside that inventory/plan are source-pinned reused seeds, not fresh-PASS claims. No inherited applicable paths are excluded. Expected negatives retain exact diagnostic and symbol. Validation is pending at D1; it is not cold.

The no-lock/one-main-compiler rule applies throughout. Checks are detached and one-second RSS-monitored: unchanged LocalDiamond 52 GiB; all other declared-heavy main targets 48 GiB. Foreign overlap is timestamps only; no foreign compiler is signaled. Thirty source commits passed immediate exact-source guards. Frozen source/body/census audit passes; production/package remain byte-unchanged, census 1/2/0/0/1.

The inherited execution-policy metadata incorrectly said R201 for B1 through A6. Its enforcement fields and runner hash were already R202's. The label alone was corrected; old bytes, before/after hashes, exact affected invocations and correction timestamp are retained. Records are never relabeled. Ten policy-contract tests include strict rejection of unapproved old labels and changed enforcement; 21 evidence-contract tests pass. A stale explanatory timing comment in the pinned runner is nonexecuting; actual R202 cutoff expressions are 20:22/20:37 and are independently checked.

Owner confirmed the requested missing R201 audit/gate filenames were a naming error and authorized SUPERVISOR-GATE, DECLARATIONS, MICRO-UNITS, SELF-REVIEW, and A19-FIXTURE-RULING equivalents. Extracted paper text was read to EOF; no rendered-PDF verification is claimed.
