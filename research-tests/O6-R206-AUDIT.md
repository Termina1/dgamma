# R206 main-lane audit

R205 FINAL GATE at a699828e ACCEPTED checked-partial; sealed at 452420c7; production unfrozen by owner decision (CP3 blob eeaa70aa…, CP3StatementChecks migrated); CanonicalSort re-baselined eb0ab7b9…; lane 2 released.

## Scope and decision record

- Branch `cp5-thm73-scoping`, start `452420c73c59a6af2d204cafa6e722b3a0fff995`.
- Initially only untracked `paper/` and `review-o6-body-adversarial.md`; untouched.
- V19 prioritized, committed `5bb9c7f9`, immediately notified to supervisor for
  push/lane2 relay. Parent owns pushing; this worker never entered lane2.
- The supervisor approved a **checked-partial** semantic gate before Original-
  BlockClass edits: full old O19 classifier is false for attached grammar.
  CrossTrace fresh PASS was explicitly removed from this shift's promise.
  See `O6-R206-SEMANTIC-GATE.md`; no source/frozen unfreeze in R206.
- Independent B and A work was separately authorized on trusted green substrate;
  no new public O19 hypothesis, canonical agreement, or endpoint goal was assumed.

## Unit0: actual repair/retirement dispositions

1. **V19 PASS:** replace false attached→extended inclusion with the true
   `actorLifecycleCoreIntoAttached`; old direction retired in NOTES.
2. **OriginalBlockClass PASS:** `LegacyActorOnly` explicitly names the old
   actor-lifecycle/yielded-child grammar, and `o19LegacyActorCore` embeds it.
   Legacy two-case observation/AA-OA-AO-OO assembly is conditional on that shape.
   Complete expanded observers cover both attached wrappers, all five core
   constructors and all four bundle constructors. Child/root control lookups
   remain tied to their ORIGINAL source, not magically transported replay cuts.
3. **PaperBranchCompleteness PASS:** expanded lifecycle observation; old
   `o19OriginalClasses` requires both legacy shapes.
4. **RightOpeningTransport PASS:** foreign-owner/physical Begin framing is
   legacy-conditional. `NoGeneratedChild` alone does not prohibit controls of
   existing opposite actors or roots.
5. **R18 PASS:** old internal/external-order producers are legacy-conditional;
   the expanded head observer does not call root inputs internal.
6. **R192 PASS:** retained no-legacy/no-zero-gap counterexample now states
   `LegacyActorOnly`. No exhausted R192 C4/A9/B10 construction was retried.
7. **R172 RETIRED:** exact-byte move to `research-tests/DGamma/retired/`;
   **retired by A8/A12 (owner-signed unfreeze)**. Its old imports remain blocked;
   old TTCs were neither deleted nor treated as proofs.
8. **U016–U026 fixture PASS:** actual checked child Remove → forced-root Insert
   → same-bundle root Retire → root Remove, with genuine KeyReleased/AttachedRelease
   evidence. The expanded observer is applied to all four edges. The fixture
   proves non-legacy shape, a forbidden-child owner control despite NoGeneratedChild,
   and an observable root input. It is a BODY grammar fixture, not a sanctioned
   reached block or a swapping theorem.
9. **ActualCartesian fresh FAIL:** old unconditional classifier call lacks legacy
   shapes; `o19AdjacentBlockStartCount` also uses obsolete BlockBefore association.
   ReachedBlocks, OperationalAssembly/CrossTrace, CurrentNameRebase, native
   disappearance and chronological runtime assembly remain genuinely blocked.
   No repair through frozen bodies/casts/stale TTCs was attempted.

These are the consumer re-proof obligations predicted by the CP3 draft.
`O6-R206-O19-EXPANSION-DESIGN.md` specifies the R207 seven-form/49-product
starting matrix, source versus replay metadata obligations, affected frozen
O19/LocalDiamond statements and A10/A12 paper gaps. Proposal only.

## Visibility and original R204 A15

`734349ac` changes only the two whole-path projections from export to public
export, plus the requested docstring. Their types/bodies are byte-identical.
`O6-R206-VISIBILITY.json` records hashes and the **R204 A15 opacity diagnosis**.
Fresh V1 defining-module PASS; six dependent modules remain blocked. The original
ChronologicalOccurrenceHistory context cannot be checked. Its one newly permitted
A15 append restatement was **NOT ATTEMPTED**, not relocated or spent elsewhere.
R206 micro-unit A15 below is an unrelated finite event-coverage fixture.

## Unit B — checked conditional adapter,20/26

`CP5O20ControlRebaseSpike` proves exact ordered provider-word, parent, all four
lifecycle and full fiber re-renaming. Absent-domain transport observes the OLD
preimage of the NEW image: absence follows from the old full cut, or presence
contradicts actual source absence by bijection laws and present-domain agreement.
No absent-target oracle, reverse-domain agreement or global name-function equality.

`o20RebaseAllNameCutConditional` proves ambient, ordered bindings and controls at
ALL raw names with three separately named primitive agreements: current-present,
actual parent reference, actual lifecycle-provider reference. They are NOT
produced for the canonical/history map; R204 discharge and the actual-chain D5
consumer remain blocked by ActualCartesian. No generic D5 exhausted seam retried.

Native tests use the genuine six-edge removed-child endpoint with a nonidentity
absent-name swap; a nonempty parent/Active view metadata value; and a constructive
rejection when the map moves the present root to absence. B4/B17/B18 each needed
attempt2 (erased error, explicit lookup universes, layout); no3/3 exhaustion.

## Unit A — checked native chronology pairing,18/26

`CP5O20ChronologyPairingSpike:o20NativeChronologiesPaired` builds BOTH R203 native
scans and an exact-list permutation pairing in the SAME actual E8 recursion.
All nine correspondence constructors are covered; ordinary/deleted edges remain
physical source edges. Bilateral Elem coverage and equal retained counts are
proved; the same paired words receive complete original-prefix position/counter
proofs. Global child-birth order is explicitly absent from CP3's E8 match record.

This is an integration with the R203 native scans, not a claim that finite
matching itself is new: RenamingComposition has a private older pairing/fold
boundary with different list projections and a heavy import closure. No opaque
private projection was cast into a native chronology list.

Native six-edge retained and eight-edge closing fixtures prove literal produced
words, bilateral matching, prefix/counter application, and that empty retained
words DO NOT imply a zero physical path. A1/A3 needed attempt2 (reserved-word
parse / explicit indexed Elem consumer). No3/3 exhaustion.

Canonical ordered zip, canonical-prefix transport, unconditional predecessor
ALL-name cut (the original A6), supported/unsupported full runtime skips,
R198 whole runtime producer and `o20SynchronizeCanonicalHistoriesModulo` remain
unproduced. The existing conditional fold is not a discharged producer. No
R204 A15 relocation. **C INELIGIBLE** because A/B endpoint obligations are open.

## Final current-source validation

- 12 changed/new active source targets: fresh own-target PASS, with their
  import-closed dependent dispositions; one exact-byte retirement.
- Invalidation closure144: **22 positive fresh PASS,3 fresh expected-negative
  PASS,1 fresh ActualCartesian FAIL,117 blocked,1 retirement**. No unchecked
  invalidation entry; blocked is NOT checked. No forced all-tree recheck.
- Original133 blocked modules: **16 own-target diagnostics =12 positive PASS,
  3 authenticated expected-negative PASS,1 FAIL;117 still blocked**.
- Blocked union:89 ActualCartesian-only,2 retirement-only,26 overlap. Hence
  ActualCartesian closure115 and retirement closure28; union117, not143.
- Full inventory548 includes356 authenticated R205 seeds,36 old expected-negative
  outcomes,11 excluded legacy cases and pre-existing R137 (record only).
- Native receipts88 =78 accepted outcomes and10 rejected attempts, including
  superseded repairs; these are NOT88 distinct passing source modules.
- Seeded `VPACKAGE-1` PASS, no Building,16.742s. SupportSolution source and TTC
  never re-elaborated/touched; TTC SHA0572d487… and mtime1788997534601237763 unchanged.
- 30 compiler-free evidence/policy tests PASS, including forged Building/source,
  resource and overlap cases, source epochs, blocked imports, and seed revival
  after a failed/rejected native attempt. The report fallback was hardened;
  this changes no native result in this dataset.
- Independent compiler-free cross-check PASS: all88 source/runner/common/manifest/
  transcript snapshots; source commit chain; frozen/source hashes; dependency
  epochs and current direct-import source hashes; original133 accounting; archive.
  This is NOT an external reviewer; supervisor spot-check remains required.
- One owned compiler at a time; no lock/window, foreign signals or lane2 entry.
  513 overlap timestamps recorded. Peak sampled compiler RSS9,759,296KiB atU026;
  this is sampled RSS, NOT OS high-water/aggregate memory. No resource stop.
- Unit0=26 slots (25 proof units+retirement), B20/26, A18/26, visibility1/2,
  C0/6. Documentation/evidence has two earlier artifact commits, one final
  artifact commit planned, and one gate/seal commit reserved (cap4).

## Current checked source commits

| Source | Last source commit | Current native evidence |
|---|---|---|
| `research-tests/DGamma/R18ExternalOrderProducerPositive.idr` | `f63d14ac` | fresh PASS (`U013-1`) |
| `research-tests/DGamma/R192RemovedBirthCurrentNameProbe.idr` | `cf623b9d` | fresh PASS (`U014-1`) |
| `research-tests/DGamma/R206AttachedGrammarPositive.idr` | `24e1b83c` | fresh PASS (`U026-1`) |
| `research-tests/DGamma/R206ChronologyPairingPositive.idr` | `9782be1f` | fresh PASS (`A18-1`) |
| `research-tests/DGamma/R206ControlRebasePositive.idr` | `ebceb026` | fresh PASS (`B20-1`) |
| `research/DGamma/CP5ActorLifecycleOnlyExtended.idr` | `5bb9c7f9` | fresh PASS (`U01-1`) |
| `research/DGamma/CP5O19OriginalBlockClassSpike.idr` | `dbc94794` | fresh PASS (`U010-1`) |
| `research/DGamma/CP5O19PaperBranchCompletenessSpike.idr` | `bb0f43ba` | fresh PASS (`U011-1`) |
| `research/DGamma/CP5O20ChronologyPairingSpike.idr` | `b01be64c` | fresh PASS (`A11-1`) |
| `research/DGamma/CP5O20ControlRebaseSpike.idr` | `07857c80` | fresh PASS (`B13-1`) |
| `research/DGamma/CP5O20OccurrenceStampedHistorySpike.idr` | `734349ac` | fresh PASS (`V1`) |
| `research/DGamma/CP5O20RightOpeningTransportSpike.idr` | `0c84cca8` | fresh PASS (`U012-1`) |

## Evidence and gate

`O6-R206-DISPOSITION.json` is authoritative for current module usability;
`O6-R206-COMPILER-LEDGER.json` retains all attempts. The curated
`O6-R206-EVIDENCE.tar.gz` plus its manifest contains immutable native snapshots,
intermediate closure reports, tools and gate context, excluding unused drafts.
`O6-R206-INDEPENDENT-VERIFICATION.json` records the compiler-free cross-check.
Source checkpoints are guarded-committed; the archive's commit receipts end
before its storing artifact/seal commits (self-reference is not manufactured).

Production and dgamma.ipkg are byte-identical to452420c7. All frozen body/type
hashes equal the R205 post-unfreeze baseline; census **4 =1/2/0/0/1**. No hole
body, production field, public theorem premise or escape hatch changed.

Final disposition: **CHECKED-PARTIAL, supervisor gate pending**. Next is the
separately gated R207 expanded O19 pair/reached-state redesign, then canonical
history/current agreement and true D5/runtime synchronization discharge. Parent
owns push and any lane merge.
