# L2R5 A11 — stopped two-head lookup elimination

A1–A10 retained through c74c1260. A11 `twoHeadLookupRightObserved` exhausted 3/3, full uncommitted source file removed. No A11-4 or restatement is authorized. Raw source/log/JSON snapshots remain under /tmp/dgamma-l2r5/A11-{1,2,3}.* and will be archived.

Statement: for an arbitrary homogeneous fiber-binding tail, commute two distinct registry heads under an explicit observed lookup-name decision. This was a helper for the owner-authorized root/child-Insert exchange at `RegistryExtensional`, NOT the native square itself.

- A11-1: observed `Yes same` retained dependent transport over the tail; `Bind first left :: rest` failed to unify with `rest`.
- A11-2: `Yes Refl` left two separately named unifying patterns (`wanted`, `second`); rejected nonlinear-pattern requirement. No checked nonlinear code retained.
- A11-3: wildcard for the unified second name solved the Yes branch, but the No branch exposed a newly reduced inner lookup after the first rewrite; `with block in lookupEntries ... (decEq wanted first)` did not normalize to `lookupEntries wanted rest`.

This is an elaboration wall under the three-attempt budget, not a claim that two-head lookup commutation is false. No unsafe escape, local view, `with`, or mutation-during-check was used. Full revert precedes fresh V1 validation of the retained Extensional module.

## Retained capital and limits

- A2 e9abbd95: ordered fixed root/anchor annotations unchanged by lifecycle swap, arbitrary prefix/suffix. Native stable-ID reassignment remains separate.
- A4 5822f06e / A5 cb60b7d7: checked source freedom; current-cut legality at both sides of an admitted native exchange with its exact no-cross interval. Not earliest transport.
- A6 0fbc3d33: normalizer-only `RegistryExtensional` (world + all lookupFiber equations), erased fields.
- A7 21453f7c: exact snapshot equality implies that relation.
- A8 f8e12a2c: name-domain memberKey equation is derived.
- A9 49ef79d1 / A10 c74c1260: actual declared-occupancy witness and its bidirectional transport are derived from pointwise lookup. Equivalence with the finite `provisionsDisjointFrom` Bool scan is NOT proved.
- No original-edge-only Insert/Insert native square, extensional evaluator transport, distance iteration or iteration fixture endpoint is claimed.
