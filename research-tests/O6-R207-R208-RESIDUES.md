# R208 exact residues and frozen consumers

## What R207 actually proves

`CP5O19AttachedPairsSpike` has an unconditional, total **source** observer for
all seven admitted forms, retaining the actual transition, its native source,
the original core, parent/lookup equations, prior-root membership and the
`AttachedReason`. `o19ObserveAttachedPair` is the Cartesian product of these
observations and represents every one of 49 products. It proves neither that
all products commute nor that a source classification holds at a replay cut.

`o19AttachedExternalKind` classifies ORIGINAL-source external/internal actions.
`o19AttachedRootHeadsSame` and `o19AttachedRootOrderObstruction` prove that two
distinct root actions cannot simply exchange their order under the frozen
`SameExternalOrchestration` relation. The native fixture is deliberately NOT a
sanctioned-block counterexample.

The A/O, O/A and O/O adapters are checked local theorems conditional on the
frozen diamonds' **existing primitive** premises. Early actions, alignment,
licensing, independence, owner separation and O/O safety are NOT derived from
the block interface. They do not close an unconditional pair/column producer.

## The 49-product obligation matrix

Forms: A=actor lifecycle, CI=yielded child Insert, CT=owned child Retire,
CM=owned child Remove, RI=forced root Insert, RT=same-bundle root Retire,
RM=same-bundle root Remove. Every matrix entry has an exact native-source
observation. The labels below name the additional replay obligations, not
assumptions already proved:

| left / right | A | CI | CT | CM | RI | RT | RM |
|---|---|---|---|---|---|---|---|
| A | AA | AO-I | AO-C | AO-C | AO-R | AO-RC | AO-RC |
| CI | OA-I | OO-II | OO-IC | OO-IC | OO-IR | OO-IRC | OO-IRC |
| CT | OA-C | OO-CI | OO-CC | OO-CC | OO-CR | OO-CRC | OO-CRC |
| CM | OA-C | OO-CI | OO-CC | OO-CC | OO-CR | OO-CRC | OO-CRC |
| RI | OA-R | OO-RI | OO-RC | OO-RC | ROOT | ROOT | ROOT |
| RT | OA-RC | OO-RCI | OO-RCC | OO-RCC | ROOT | ROOT | ROOT |
| RM | OA-RC | OO-RCI | OO-RCC | OO-RCC | ROOT | ROOT | ROOT |

- AA: recover `PaperActivationStep` (not arbitrary lifecycle) and an actual
  early aligned right activation; then apply frozen activation/activation.
- AO/OA: derive native owner separation and child/parent licensing. OA also
  needs an actual right-before-left transition with the same action AND tag;
  action equality by itself supplies no checked application.
- Every OO cell: derive `OrchestrationSwapSafety`, its actual `earlyRight`,
  alignment, generation/freshness/licensing, not merely tag classification.
- C/RC: transport the actual child's/root's lookup and parent equation to the
  precise replay-before state. Preserve generation identity across removal;
  do not infer it from a raw-name or action-word match.
- R/RC: preserve the exact native core and `AttachedReason`. For KeyReleased,
  transport its witnessed release occurrence and data; for EarlierForcedRoot,
  preserve actual prior-root membership in the same ordered bundle.
- ROOT: the nine root/root products additionally require a **reached
  impossibility/separation theorem compatible with fixed root-input order**.
  A local state diamond cannot supply `pairExternalOrder`. Two nonempty bundles
  may expose a genuine counterexample to the unconditional interface; test a
  complete sanctioned two-block fixture before assuming the missing theorem
  is true. R207 does not establish that counterexample.

`NoGeneratedChild` forbids Insert of a selected child only. It does NOT ban
controls of existing children, forced roots, or same-bundle root controls.
`AdjacentActorSwapSafety` provides the real right opening at the pre-left cut,
not arbitrary early applications of every interior root/control action.

## Exact production chain that remains open

1. Expand `o19OriginalPairAtReplayOrigins` / `o19ReplayRowSourceClasses` using
   native source occurrence evidence and genuine source-to-replay transport.
2. Re-derive `o19ObservedPairReplay`, `o19WordRowStep`, `o19BubbleWordRow`, then
   `o19CartesianColumns` / `o19CartesianSourceSpines`, with the count, literal
   word, site and origin laws on the SAME expanded run.
3. Produce the unconditional replacement of `o19CartesianActualBlocks`.
   Its R207 proof is valid only with explicit selected-body LegacyActorOnly.
4. Inhabit `O19WholeBlockUnconditionalObligation`: the same expanded cursor,
   exact finite-product count, nonempty chain, and whole-block derivation.
5. Inhabit `O19ReachedUnconditionalObligation` and
   `O19ReachedDecompositionUnconditionalObligation`: actual reached blocks,
   native BlockBefore order, full lifecycle coverage and disjointness. Word
   equality cannot reconstruct the state-indexed attached grammar. The R207
   proofs require every decomposed source body to be legacy actor-only.
6. Produce an inhabitant of `O19OperationalUnconditionalObligation`, whose
   literal original function type is retained. Assemble its whole derivation,
   reached decomposition, endpoint, entire replay bundle, uniqueness and full
   external input relation from that SAME expanded cursor.
7. Only then can byte-identical
   `CrossTrace:operationalAdjacentBlockSwapSpike` typecheck. Its current own
   native failure is the legacy-premise function/result mismatch. Do not add a
   premise there, modify its body, or accept an earlier TTC.

All 36 exact pre-migration statements are in `O6-R207-CROSSTRACE-NEEDS.json`.
The new obligation types have NO inhabitant, postulate, hole or escape hatch;
they are not claimed as proven expanded theorems.

## Newly exposed resolver-frame frontier

`BlockResolverFrame` had five old-arity grammar consumers. U010 re-proves their
legacy-restricted versions, matching the earlier RightOpeningTransport premise.
`O20ExpandedBlockResolverObligation` preserves all five expanded conclusions as
TYPE-only fields with explicit nameEq and no inhabitant. Their pre-migration
signatures are archived in `O6-R207-BLOCK-RESOLVER-NEEDS.json`. Expanded ownership
and resolver transport remain OPEN; downstream callers cannot use source block
`ActorLifecycleOnly` evidence as if it were LegacyActorOnly.

## Frozen surfaces fed by those obligations

- `CP5O19SurfaceSpike:AdjacentActorSwapSafety`, `WholeBlockAdjacentSwap`, and
  the exact occurrence/grid requirements remain unchanged.
- LocalDiamond's four orientation-specific local diamond functions and
  `adjacentSwapSuffixSpike` remain unchanged. Suffix replay still needs real
  external-order evidence and its full sealed replay/invariant boundary.
- `CrossTrace:operationalAdjacentBlockSwapSpike` needs the full unconditional
  operational producer. Its O19 body and its TWO O20 holes remain unchanged.
- `CanonicalSort`, `DeletionChain`, `RenamingComposition`, and
  `ReplayedCanonicalEndpointBridge` are unchanged; census is 4=1/2/0/0/1.

## Downstream V / B / A / C gating

- A15: use the one authorized different-premise two-path literal append
  restatement only in the rechecked `ChronologicalOccurrenceHistory` context.
  Its whole-path projections are public, but NativeInsertEnvironment and
  InsertOccurrenceHistory genuinely use CrossTrace exports. Opacity repair
  is not dependency repair. R207 does not retry either exhausted old statement.
- B: the three canonical agreements for `o20RebaseAllNameCutConditional` are
  still unproduced: current-present names, actual parent references, actual
  provider references. Recheck CurrentNameRebase/ChainCurrentDisappearance
  and the R204 capital; then build D5 on the actual deletion-chain result, not
  on a caller-supplied endpoint/cut or a supported-only map.
- A: E8 currently gives literal native chronology lists and a permutation
  pairing. Ordered canonical zip needs support-order induction and real
  canonical-prefix transport. Predecessor A6 ALL-name cut, supported and
  unsupported runtime skips, R198 whole histories and conditional-first
  synchronization remain open; ordinary/deleted scanner edges are not epsilon.
- C remains INELIGIBLE while A/B and the genuine bridge are open.
- The retired R172 fixture remains retired. Its two retirement-only and 26
  shared R206 dependents need their own disposition, not resurrected TTCs.


## Final fresh BROKEN roots (after U010; no slot extension)

1. `CP5ConfluenceCrossTraceSpike:operationalAdjacentBlockSwapSpike` — its
   unchanged body calls the legacy-conditional operational function but must
   return the original unconditional value. Native VFINAL-17 reproduces the
   exact function/result mismatch; this is NOT an expected-negative PASS.
2. `R193InstalledBlockResolverPositive:r193FrameActorOnly` — stale old
   `ActorLifecycleOnly 2 body` arity and removed ActorYieldedRegistrationStep
   constructor. Native VFINAL-27 has its own Building and exact diagnostics.
   Source is retained unchanged after Unit0 reaches10/10. R208 should migrate
   this concrete legacy fixture explicitly, then refresh its dependents.

The previous117 now partition as13 fresh positive PASS,2 fresh FAIL,102 blocked.
The blocked count is74 Cartesian-only +26 shared +2 retirement-only. No new
failure/blocked source is represented as a checked theorem.
