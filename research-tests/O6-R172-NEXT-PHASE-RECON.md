# O6 R172 — post-DeletionChain next-phase reconnaissance

## Boundary, method, and current facts

Unit A is **analysis only**: no research source contents, declaration, hole body, fixture, production module or package changed. Start **0e58c54**, branch `cp5-thm73-scoping`, verified at **2026-09-06 11:49:38 UTC**; only allowed untracked `paper/` and byte-frozen `review-o6-body-adversarial.md`. Idris **0.8.0**. Governing R146, R171, full THM73 plan, R143–R145/R147 and R134–R136 stop audits were read before recommendation; all 3883 extracted-paper lines (3882 newline characters) were read. Later R161–R171 audit/source seams were inspected for current capital/debt, not treated as authority to change interfaces.

Deadline: **15:49:38 UTC**, no new attempt after **15:09:38**, safe final gate by **15:34:38**. One compiler at a time; process inspection before invocations. Direct CLI spelling was verified from `idris2 --help`. No build tree or TTC deletion, unseeded build, production change, subagent, new `with`, or invocation of frozen `deletionTheoremProof`.

DeletionChain is CLOSED: O9 `89435dc`, O10 `dd3ed2d`, O11 `8356a64`; the complete surviving discipline and full TARGET bundle remain retained. Final Core history and Step accounting/origin seals are negative-tested, not assumed. Hole count is **7 = CanonicalSort 2 / CrossTrace 4 / DeletionChain 0 / LocalDiamond 0 / RenamingComposition 1**. This is **not** Theorem-73 closure: non-hole dischargeability debts below are material.

The types below are exact source declarations at the start coordinate, including all written premises and indices. Inherited `name`, `key`, `world`, `error`, and `value : key -> Type` implicit binders are left as the source states them; this memo adds no Idris declaration. Line coordinates distinguish signature start and RHS hole.

## O14 — finite support ordering

- Hole: `?supportOrderingSpike_rhs`
- File: `research/DGamma/CP5ConfluenceCanonicalSortSpike.idr`
- Signature line **781**; hole line **788**.

```idris
0 supportOrderingSpike :
  (nameEq : DecEq name) -> (keyEq : DecEq key) ->
  (protocol : RegistrationProtocol key value world error) ->
  {initial, finalState : SystemState name key value world error} ->
  (trace : Transitions initial finalState) ->
  ReplayInvariantBundle name key world error value protocol nameEq keyEq trace ->
  SupportOrderingCapital name key world error value nameEq keyEq finalState
```

**Last attempt:** R135 helper 3/3, body still 0/3. R134 helper 3/3 was fully removed; only `canonicalRankLTETransitive` survived R135 (`75021cc`). Exact R135 audit, attempt 3:

> “Idris still could not bind the dependent `value` family inside the manually restated lookup/lifecycle case, and the downstream `replace` retained a state-family constraint.”

The same audit reports a residual as-pattern, also disqualifying that candidate. R136 explicitly kept O14 parked. **R146 status: probe-first, missing-capital chain; not banned.** The memo's stop-map accidentally calls `sortClosingFreeTraceSpike` O14. Source and R134–R145 audits resolve the labels: O14 is **this** hole, O17 is the next. The ranking's two distinct producer slots are preserved.

**Applicable capital / precise feed:** `deleteAllClosingEpisodesSpike` now supplies an actual `ClosingFreeReduction`; `chainReplayCapital (reducedPremises reduction)` supplies O14's exact trace bundle. Its `replayProtocolRanked` and `replayParentRanksIncrease` discharge the two premises of `CP4Support.supportPathRankIncreases`, whose `pathRankIncreases` must become `LinearizesSupport.supportPathsOrdered`. `replaySupportMatchesActive` turns supported truth into final active truth. Already-public `CP4SupportQuiescence.activePredicateAtFoundQ` states the actual lookup/active equation without independently reifying a Bool case tree; it is the concrete probe entry for the R135 wall. These foundational lemmas predate R161: the genuinely new capital is the inhabited post-deletion bundle, and the R161–R171 explicit-index/producer-equation and simultaneous-construction recipe. No RAR export is needed for a pure list sort.

**Route:** probe-first; first micro-unit is a disposable single-declaration, fully explicit `supportedActiveAt`/found-fiber equation bridge using `activePredicateAtFoundQ`. Commit its exact successful or failed transcript before any retained O14 helper/body. If successful, construct an erased exact lookup/rank view, then a named stable insertion/sort invariant carrying output, membership both ways, uniqueness and nondecreasing ranks together. Convert strict path rank into `BeforeIn` and assemble the unchanged O14 type. No new semantic premise or surface revision is proposed.

**Risk:** medium mechanics / low identified semantic risk. `supportSet` discovery order is not a topological order. Do not confuse an arbitrary reduced order with the shared original/reduced order owed by O15; rank sorting is a promising producer-specific answer, not a proof of universal transfer.

## O17 — operational closing-free trace sorting

- Hole: `?sortClosingFreeTraceSpike_rhs`
- File: `research/DGamma/CP5ConfluenceCanonicalSortSpike.idr`
- Signature line **867**; hole line **878**.

```idris
0 sortClosingFreeTraceSpike :
  (nameEq : DecEq name) -> (keyEq : DecEq key) ->
  (protocol : RegistrationProtocol key value world error) ->
  {initial, finalState : SystemState name key value world error} ->
  (trace : Transitions initial finalState) ->
  ReplayInvariantBundle name key world error value protocol nameEq keyEq trace ->
  ClosingFreeTraceShape name key world error value nameEq keyEq trace ->
  (ordering : SupportOrderingCapital name key world error value nameEq keyEq
    finalState) ->
  SortedClosingFreeTrace name key world error value protocol nameEq keyEq trace
    ordering
```

**Last attempt:** R144 body 3/3, restored. Exact O17 stop audit, attempt 3:

> “No definitional coercion turns the input interleaved episode into the output block package; finite adjacent swaps must be produced first.”

R143's O15/O16/O18 closures did not prove this algorithm. **R146 status: probe-first, missing-capital chain, parked body; identity-as-complete-sort approach closed.**

**Applicable capital / precise feed:** the O11 result gives `reducedClosingFree` plus full `reducedPremises`, so `closingFreeTraceShapeSpike` produces actual `ClosingFreeTraceShape`, including `supportedOpenEpisode` and `unsupportedTakesNoLifecycle`. O14 supplies the ordering premise. A reached current sort state must retain the actual `ReplayInvariantBundle`; each `adjacentSwapSuffixSpike` result supplies `swappedPremises`, `swappedOccurrenceCorrespondence`, `swappedEndpoint` and the exact external witness for the next state. R167 exports `identityRelationalReplayCorrespondence`, `consRelationalReplayCorrespondence`, `r97AppendRelationalReplayCorrespondence` and the generator/stage lift equation pairs for a simultaneous worklist; R168 `singletonAdvanceRAR` and its stage-family/locator exports avoid reconstructing the L-Advance RAR. None manufactures `earlyRight`, constant targets, or contiguous block ranges. R171's Core/Step seals illustrate retaining exact history/origin at construction rather than proving it for an arbitrary record afterwards.

**Route:** probe-first producer-owned root-hoist/one-minimal-actor state; first operational probe must build one genuine hoist from source bundle, exact checked early transition, local diamond, pair external-order witness and suffix splice, retaining every new index together. Then a decreasing finite worklist simultaneously constructs blocks, ranges and derivation; `canonicalSortedIdentity` is only the terminal case. Three-attempt budgets belong to individual declarations, not the whole algorithm. No public surface change indicated.

**Risk:** high/XL. Activation applicability, stable generated yields, child orchestration placement, exact Cartesian positions and termination all remain real proof work. O19 is not a legitimate shortcut while banned.

## CrossTrace — canonical support matching

- Hole: `?canonicalSupportOrdersMatchSpike_rhs`
- File: `research/DGamma/CP5ConfluenceCrossTraceSpike.idr`
- Signature line **970**; hole line **986**.

```idris
0 canonicalSupportOrdersMatchSpike :
  (nameEq : DecEq name) -> (keyEq : DecEq key) ->
  (protocol : RegistrationProtocol key value world error) ->
  {initial, leftFinal, rightFinal : SystemState name key value world error} ->
  (leftTrace : Transitions initial leftFinal) ->
  (rightTrace : Transitions initial rightFinal) ->
  (sameInputs : SameOrchestrationModuloGenerated nameEq keyEq leftTrace
    rightTrace) ->
  (leftCapital : IndependentCanonicalSchedule name key world error value protocol
    nameEq keyEq leftTrace) ->
  (rightCapital : IndependentCanonicalSchedule name key world error value protocol
    nameEq keyEq rightTrace) ->
  MappedCanonicalSupportOrders name key world error value protocol nameEq keyEq
    leftTrace rightTrace
    (currentNameBijection (endpointRenaming sameInputs))
    (canonicalSchedule leftCapital) (canonicalSchedule rightCapital)
```

**Last attempt:** R144 body 3/3, restored, with checked `canonicalSupportOrderForwardFromTruth` (`02c3baf`), backward (`cd1ee19`) and `canonicalSupportOrdersFromTruth` (`7d0fac1`). Exact serial stop audit:

> “No existing field directly connects those endpoint support computations, and no circular appeal to O20 was introduced.”

Attempt 2 met `rightFinal` versus `leftFinal`; attempt 3 used a nonexistent inverse-name helper, a charged spelling error that would not have solved endpoint equality. **R146 status: open producer lane / prior body budget parked, missing-capital chain.**

**Applicable capital / precise feed:** the actual deletion pipeline now supplies both canonicalization reductions and their original-origin registration accounting; R171 `foldedOriginExact` authenticates the actual derivation's accounting before the public reduction wrapper. Together with accepted `generatedRegistrationTree sameInputs` and `endpointRenaming sameInputs`, this is the prospective source of exact current birth/component/parent/retirement correspondence. `leftCurrentGenerationMapped` / `rightCurrentGenerationMapped` split mapped from vestigial, but require a current-generation lookup and do not themselves equate components or support. Derive current endpoint lookup coherence from the accepted scan, then dependency/provision/parent and retirement transport; apply support fixed-point uniqueness to obtain the two truth implications consumed by `canonicalSupportOrdersFromTruth`. The global bijection inverse laws prevent collisions across different component/parent shapes. No existing one-trace endpoint composer is already a cross-trace theorem.

**Route:** small exact-current-generation/endpoint-domain probe, then producer-side support preservation in both directions, then direct fill using the retained assembler. Do not demand equality of the two list enumerations. Do not consume O20 to prove its own prerequisite.

**Risk:** high. Generation reuse, potentially different discarded sets, registry-wide fixed points, and shared-order debt remain. New deletion capital removes the upstream vacuity, not the endpoint-structure proof.

## O19 — local operational adjacent block swap

- Hole: `?operationalAdjacentBlockSwapSpike_rhs`
- File: `research/DGamma/CP5ConfluenceCrossTraceSpike.idr`
- Signature line **699**; hole line **714**.

```idris
0 operationalAdjacentBlockSwapSpike :
  (nameEq : DecEq name) -> (keyEq : DecEq key) ->
  (protocol : RegistrationProtocol key value world error) ->
  {sourceOrder, targetOrder : List name} ->
  (orderSwap : AdjacentActorOrderSwap name sourceOrder targetOrder) ->
  {initial, sourceFinal : SystemState name key value world error} ->
  (sourceTrace : Transitions initial sourceFinal) ->
  (sourceBlocks : ActorBlockDecomposition name key world error value nameEq keyEq
    sourceOrder sourceTrace) ->
  (sourcePremises : ReplayInvariantBundle name key world error value protocol
    nameEq keyEq sourceTrace) ->
  (safety : AdjacentActorSwapSafety name key world error value protocol nameEq
    keyEq orderSwap sourceTrace sourceBlocks sourcePremises) ->
  OperationalAdjacentBlockSwap name key world error value protocol nameEq keyEq
    orderSwap sourceTrace sourceBlocks sourcePremises safety
```

**Last attempt:** R144 disposable dependency fixture 3/3, mechanically inconclusive; **body 0/3**. Exact dependency-probe audit, attempt 3:

> “full checked packaging stopped at `fiberViewInvariant`; the post-finish consumer resolution also required a producer-owned exact `targetFiber` equation rather than bare reduction.”

The early consumer `LBegin = Nothing` and provider raw finish had checked, but the complete theorem telescope had not been refuted. **R146 status: BODY BANNED, suspected false surface.**

**Applicable capital / precise feed:** R161–R171 producer-owned equations can separate source well-formedness, actual target resolution, raw finish and checked finish for the same public provider/consumer fixture, rather than asking the evaluator to normalize an uncorrelated restatement. Full TARGET bundle and discipline constructors now available are a construction model for the required `sourcePremises`; the 12+4 RAR exports can package a genuine local replay after applicability is established. They cannot supply an early consumer activation where the provider is not active. `AdjacentActorSwapSafety` still has coordinate/disjointness and licensing exclusions, not support incomparability/both-direction applicability. Deletion closure does not repair that predicate.

**Route:** finish a committed negative fixture/countershape or an exclusion theorem first. Any surface proposal must follow that refutation or an exact-wall committed probe transcript; the revised surface must itself have a committed negative fixture **before any body attempt**. Candidate strengthening is support incomparability plus derivable early applicability, or an exact two-sided applicability certificate from the real selector. No such revision is made or authorized by this memo.

**Risk:** critical semantic, then XL Cartesian operational fold. A failed evaluator fixture is not a countermodel. This remains below support matching despite downstream fan-out.

## O19 — certified operational canonical permutation selection

- Hole: `?selectOperationalCanonicalPermutationSpike_rhs`
- File: `research/DGamma/CP5ConfluenceCrossTraceSpike.idr`
- Signature line **1048**; hole line **1067**.

```idris
0 selectOperationalCanonicalPermutationSpike :
  (nameEq : DecEq name) -> (keyEq : DecEq key) ->
  (protocol : RegistrationProtocol key value world error) ->
  {initial, leftFinal, rightFinal : SystemState name key value world error} ->
  (leftTrace : Transitions initial leftFinal) ->
  (rightTrace : Transitions initial rightFinal) ->
  (sameInputs : SameOrchestrationModuloGenerated nameEq keyEq leftTrace
    rightTrace) ->
  (leftCapital : IndependentCanonicalSchedule name key world error value protocol
    nameEq keyEq leftTrace) ->
  (rightCapital : IndependentCanonicalSchedule name key world error value protocol
    nameEq keyEq rightTrace) ->
  (matching : MappedCanonicalSupportOrders name key world error value protocol
    nameEq keyEq leftTrace rightTrace
    (currentNameBijection (endpointRenaming sameInputs))
    (canonicalSchedule leftCapital) (canonicalSchedule rightCapital)) ->
  CertifiedOperationalCanonicalPermutation name key world error value protocol
    nameEq keyEq leftTrace rightTrace sameInputs leftCapital rightCapital matching
```

**Last attempt:** R144 body 3/3, restored. Exact serial stop audit:

> “A `decEq` split did not refine the hidden computed order inside `CertifiedOperationalCanonicalPermutation` under the inferred nondependent case motive; more importantly, the unequal branch still requires a finite adjacent-transposition selector and a safety-producing operational fold.”

The identity branch required exact list equality; mutual mapped membership did not supply it. **R146 status: parked/missing-capital, blocked by O19 revision.**

**Applicable capital / precise feed:** `matching` already supplies the two membership directions; order uniqueness comes from each schedule's linearization. The actual reduced/sorted bundle now has a real deletion-side producer, and R167/R168 RAR combinators can compose operational steps once they exist. The simultaneous-construction cure suggests one worklist whose constructor fixes both `selectedActorPermutation` and `selectedPermutationRealized`, avoiding a later equality between independently computed lists/traces. Every nonempty step still needs the corrected O19 `OperationalAdjacentBlockSwap`, plus a proof that the selected pair is incomparable in a shared order. Neither cumulative generation accounting nor yield-map commutation alone implies that safety.

**Route:** after matching and the O19 surface/negative gate, prove a finite duplicate-free linear-extension transposition selector with decreasing measure, constructing each operational step and its exact intermediate bundle together. Probe the unequal-list constructor before a whole-body attempt.

**Risk:** very high; do not equate lists from equal sets or feed a public pure permutation to O20. No new budget is spent here.

## O20 — canonical endpoint convergence

- Hole: `?canonicalSchedulesConvergeSpike_rhs`
- File: `research/DGamma/CP5ConfluenceCrossTraceSpike.idr`
- Signature line **1304**; hole line **1325**.

```idris
0 canonicalSchedulesConvergeSpike :
  (nameEq : DecEq name) -> (keyEq : DecEq key) ->
  (protocol : RegistrationProtocol key value world error) ->
  {initial, leftFinal, rightFinal : SystemState name key value world error} ->
  (leftTrace : Transitions initial leftFinal) ->
  (rightTrace : Transitions initial rightFinal) ->
  (sameInputs : SameOrchestrationModuloGenerated nameEq keyEq leftTrace
    rightTrace) ->
  (leftCapital : IndependentCanonicalSchedule name key world error value protocol
    nameEq keyEq leftTrace) ->
  (rightCapital : IndependentCanonicalSchedule name key world error value protocol
    nameEq keyEq rightTrace) ->
  {matching : MappedCanonicalSupportOrders name key world error value protocol
    nameEq keyEq leftTrace rightTrace
    (currentNameBijection (endpointRenaming sameInputs))
    (canonicalSchedule leftCapital) (canonicalSchedule rightCapital)} ->
  (operational : CertifiedOperationalCanonicalPermutation name key world error
    value protocol nameEq keyEq leftTrace rightTrace sameInputs leftCapital
      rightCapital matching) ->
  CanonicalConvergenceResult name key world error value protocol nameEq keyEq
    leftTrace rightTrace sameInputs leftCapital rightCapital operational
```

**Last attempt:** R144 body 3/3, restored. Exact serial stop audit, attempt 3:

> “exposed the four remaining semantic fields: ambient equality, renamed table equality, renamed control correspondence, and exact generated-birth matching.”

Retained `operationalPermutationEndpoint`, `operationalPermutationSameExternalInputs`, `permutedCanonicalExecutionFromOperational`, and `canonicalConvergenceFromBridge` account for replay composition already. **R146 status: parked body / open missing-capital producer lane.**

**Applicable capital / precise feed:** O11 supplies authenticated one-trace deletion history and bidirectional coverage/removal; R169 global generation bijection + frozen-origin equation prevent incorrectly matching different generated occurrences. R167/R168 RAR exports retain exact iterator outcomes when transporting each activation. These are inputs to an induction comparing two canonical executions at matching activation stages: Definition-60 outcome stability yields equal registered components; accepted scanner generation equations then feed `replayedGeneratedBirthMatched`. Effect/RAR composition supplies the left replay endpoint, not equality to the right trace on its own. R147's `expectedBridgeBijection sameInputs` now definitionally fixes every bridge clause; the old free-map rewrite wall is gone, so the resulting four clauses can be passed directly to its sealed constructor and then `canonicalConvergenceFromBridge`.

**Route:** after operational permutation capital, producer-owned stage-by-stage canonical comparison and four-clause sealed bridge assembly; probe exact generated-birth transport before large table/control folds. No signature change or shortcut from same raw action names indicated.

**Risk:** highest downstream payload; common ambient inputs do not prove endpoint equality without matched stable executions. O21's seal does not manufacture any of these clauses.

## O21 — original endpoint composition modulo vestigial generations

- Hole: `?replayedCanonicalToOriginalEndpointSpike_rhs`
- File: `research/DGamma/CP5ConfluenceRenamingCompositionSpike.idr`
- Signature line **2693**; hole line **2723**.

```idris
0 replayedCanonicalToOriginalEndpointSpike :
  (nameEq : DecEq name) -> (keyEq : DecEq key) ->
  (protocol : RegistrationProtocol key value world error) ->
  {initial, leftFinal, rightFinal, replayedLeftFinal :
    SystemState name key value world error} ->
  (leftTrace : Transitions initial leftFinal) ->
  (rightTrace : Transitions initial rightFinal) ->
  (sameInputs : SameOrchestrationModuloGenerated nameEq keyEq leftTrace
    rightTrace) ->
  (leftCapital : IndependentCanonicalSchedule name key world error value protocol
    nameEq keyEq leftTrace) ->
  (rightCapital : IndependentCanonicalSchedule name key world error value protocol
    nameEq keyEq rightTrace) ->
  AcceptedDeletionScannerCapital name key world error value protocol nameEq keyEq
    leftTrace rightTrace sameInputs leftCapital rightCapital ->
  (replayedLeftTrace : Transitions initial replayedLeftFinal) ->
  RelationalReplayCorrespondence name key world error value
    (canonicalTrace (canonicalSchedule leftCapital)) replayedLeftTrace ->
  RelationalReplayEndpoint name key world error value nameEq keyEq
    (canonicalFinal (canonicalSchedule leftCapital)) replayedLeftFinal ->
  (replayedOccurrences : ActionRegistrationReplayCorrespondence name key world
    error value (canonicalTrace (canonicalSchedule leftCapital))
      replayedLeftTrace) ->
  ReplayedCanonicalEndpointBridge name key world error value protocol nameEq keyEq
    leftTrace rightTrace sameInputs leftCapital replayedLeftTrace
      replayedOccurrences rightCapital ->
  SystemEquivalentByRenamingModuloVestigial name key world error value nameEq
    keyEq (generatedRegistrationTree sameInputs)
    (currentNameBijection (endpointRenaming sameInputs))
```

**Last body attempt:** R145 3/3, fully restored. Exact final diagnostic quoted in that audit:

```text
Can't solve constraint between:
  bridge .replayBridgeBijection
and:
  (endpointRenaming sameInputs) .currentNameBijection
```

**Later surface work:** R147 `e6314b1`, four-clause constructor-owned bridge, positive rebuild plus wrong-birth/wrong-trace/wrong-occurrence/public-schedule negatives. No subsequent O21 body/withdrawal attempt. **R146 status: bounded seal cure COMPLETE; full hole parked, all withdrawal branches remain PARKED by standing ruling.**

**Applicable capital / precise feed:** R147 `replayBridgeBijectionFixed = Refl` and directly canonical-indexed `replayBridgeTables`/controls remove exactly the quoted wall. R145 ambient/table/outside-control and unilateral-absence helpers can now be assembled at the expected map without reindexing. R171 cumulative endpoint composition preserves literal empty ordered tables and actual absent names, separately from historical raw withdrawals. `leftWithdrawnInAcceptedScanner` / `rightWithdrawnInAcceptedScanner` supply discarded-generation membership; `leftCurrentGenerationMapped` / right counterpart can supply a `VestigialEndpointGeneration` only after an authentic current-generation lookup. Missing endpoint-domain/current coherence must come from the accepted scan, not from name membership, and the mapped branch must be excluded using canonical absence at that exact birth. None of these observations opens the parked branches.

**Route:** if separately selected, first check only an outside-both assembly adapter using the R147 seal. The complete theorem cannot close under the standing branch restriction. Any future withdrawal campaign needs explicit authorization and a producer-side exact-current/absence induction, not another bridge surface revision or a chosen birth from callers.

**Risk:** bounded low risk for outside-both; high semantic risk for the parked current-generation/vestigial branches. R146 ranked this high for bounded risk retirement, which R147 already performed; it is not the best remaining serial unlock.

## Ranked next three shifts and the first requested micro-unit

1. **R172 Unit B: O14 `supportOrderingSpike`, PROBE-FIRST.** Its old R135 wall is a precise lookup/active observation boundary; the new proof discipline is tailored to it, the actual deletion output now inhabits its input, and pure ordering is cheaper and earlier than any operational swap. First disposable declaration: explicit `{name,key,value,world,error}` `lookupFiber` equation to `supportedActiveAt = isActive (fiberLifecycle fiber)`, consuming public `activePredicateAtFoundQ` rather than mirroring the Bool evaluator. One declaration, up to three checks; remove the probe and commit source/transcript in the audit. Only after that committed result, continue with an erased exact lookup/rank producer and simultaneous stable-rank-sort invariant, not a monolithic O14 body. If the probe fails 3/3, stop-audit and gate; if structurally impossible, park. No surface change proposed.
2. **Next shift: O17 operational sort, PROBE-FIRST**, only when O14 capital is real (otherwise continue O14). Check one root-hoist/minimal-actor operational state constructor before a worklist or body, retaining exact intermediate trace/bundle, pair external order, origin and rank/block-range data. The full deletion bundle removes the old upstream blocker but not early applicability or yield stability.
3. **Following shift: canonical support matching**, once the two canonical capitals can be produced (otherwise continue O17). Start with exact accepted-scan current-generation/endpoint lookup coherence; then map retirement/parent/component/dependency/provision observations into two support-truth implications for `canonicalSupportOrdersFromTruth`. O19 countershape/surface work follows, before selector/O20. An O19 body is still banned.

This honors the surviving R146 ordering slots **8, 9, 10**. Slots 1–4, 6–7 are closed; slot 5's bounded seal cure is already complete at R147 and its remaining withdrawal lane is explicitly parked. Thus promoting O14 is removing completed/restricted work from the list, not silently reversing the triage. No R161–R171 theorem supplies the missing O19 early transition, so its high fan-out does not justify promotion. The three shifts are priority windows, **not closure promises or fresh authority for later bodies**.

## Debt register (not included in the seven-hole census)

| Debt | Current status and exact discharge frontier |
|---|---|
| `CanonicalReplayAccountingLaws` producer (R143 O16) | Still missing over exact `deletionSortingOccurrenceCorrespondence`. R171 `ScopedDerivationAccounting.foldedOriginExact` proves the deletion origin equation at the actual structural producer; **`scopedClosingReductionFromAccounting` does not retain that field in arbitrary `ClosingFreeReduction`**, whose stored accounting remains unsealed to `reductionOccurrenceCorrespondence`. Likewise `SortedClosingFreeTrace.sortedRegistrationTree` is not sealed to `sortingOccurrenceCorrespondence`. Producer-indexed extraction/composition is the next honest route; neither arbitrary-record coherence nor public visibility is newly authorized. The endpoint plus exact cumulative withdrawal equality are also still owed at the combined deletion/sorting boundary. |
| Shared original/reduced support order (R143 O15) | Still a genuine producer obligation. `R6FourFiberStatic.r143CanonicalSupportTransportRefuted` pins the retired universal-transfer claim. `R8FullPipeline.FullPipelineLateCanonicalPremises.sideOriginalLinearization` still quantifies over arbitrary reduction/ordering, so it is supplied capital, not a constructive discharge. Rank sorting the actual chosen order could respect original paths across withdrawn unsupported intermediates, but that requires its own proof; do not restore universal transfer. |
| `R16ConfluenceTheoremAssemblyPositive` drift | R152 recorded that line 85's `fullPipelineFromBundles` application omits both `FullPipelineLateCanonicalPremises` arguments; current source still does. Its unchanged immutable-theorem result cannot be repaired by calling a function with a longer telescope unless those actual premises are produced. No full R11/assembly-green claim from the seeded package. Not fixed in Unit A. |
| CP3 raw-premise defect | R137 checked reuse countershape refutes the raw-name-global exclusion needed for the intended selection route. Production CP3 `deletionTheorem` and CP4 `deletionTheoremProof` remain frozen; no scoped-to-raw coercion. The research chain bypasses them. Inherited `checkedDeletionSubroutine = deletionTheoremProof` still exists but is not called by the new chain or this shift; it is not a solution to this debt. Future production unfreeze requires a separate campaign. |
| Deferred R129 end-to-end fixture | Still absent. R129 exact returned-trace/final-to-R27/R29 comparisons hit opaque `AdjacentSwapResult` and independent producer indices. The 12+4 RAR exports do **not** expose its constructor or a result-to-R27 exact equation. Keep the precise integration demand parked pending owner-selected exact producer theorem versus explicitly relational-only fixture. |
| Remaining copied `with` cleanup | Current DeletionChain has **9** code occurrences of `with` followed by `(` across whitespace: three remaining R161 copied clauses at 15823 (`beginSuccessTagScopedPostFold`), 15937/15966 (`scopedPostCloseSuffixFold`); one spent R165 LAdvance exemption at 18171 (`scopedAdvanceResolvedUnretired`, `e43636f`); five older scanner occurrences at 29282/29286/29302/29306/29355. R162 removed lifecycle case (`8f2725f`), later target case removed (`f78cf2a`); its historical “four copied remain” is no longer the current count. No cleanup is performed now, no exemption renewed, and no new `with` allowed. Older CanonicalSort/LocalDiamond lets/withs are inherited, not a recipe for new code. |
| R161–R171 semantic retention/coherence debts | The previous tag/readiness, surviving discipline, full TARGET totality/independence, global generation inverse, frozen-origin accounting, SameExternal, history recurrence, endpoint omission composition, and bidirectional registration fold debts are CLOSED by R169–R171. Do not relabel source discipline as target discipline or resurrect the removed eager intermediate (R171 `465ce36`). At the public reduction/sorting consumer, the producer-correlation boundary in the first row remains. |
| Concrete/authenticity regression debt | Concrete two-birth/one-withdrawal O16 and reachable repeated-Iter 2×2 remain separate obligations; abstract repackagers are not fixtures. The new Core-history/Step-accounting clone negatives test real seals but not the entire canonical/confluence pipeline. No wider R11 runner, runtime suite or clean rebuild is claimed here. |
| Paper risks still scoped | Raw-name reissue, constructive finite linear extensions, stable registered yields and the base-calculus/no-unload-guard claim require explicit accounting; deletion closure is for the guarded, generation-scoped research calculus only. Approximate effect equality and renamed control equivalence are distinct clauses, never silently identified. |

## Fresh-check and freeze evidence

All checks use the seeded package cache and the verified CLI:

```sh
idris2 --source-dir src --source-dir research --check research/DGamma/CP5Confluence<Part>Spike.idr
idris2 --build dgamma.ipkg
```

| Module | Fresh evidence / elapsed wrapper window | Sampled max RSS KiB |
|---|---|---:|
| CanonicalSort | `3/3: Building`, exit 0, 11:52:28–11:52:33 (5.0 s) | Below first sample |
| RenamingComposition + CrossTrace | `4/5: Building` RenamingComposition then `5/5: Building` CrossTrace, exit 0, 11:52:33–11:52:49 (15.1 s combined) | 988608 |
| DeletionChain | `2/2: Building`, exit 0, 11:54:52–11:56:18 (85.6 s) | 4339104 |
| LocalDiamond | `1/1: Building`, exit 0, 11:56:18–12:04:42 (503.9 s, ~8m24s) | 50365840 (~48.0 GiB) |
| Seeded package | exit 0, 12:05:02–12:05:22; **207/207** module TTC files retained | 219936 |

Exactly one fresh elaboration per spike file. The first unchanged DeletionChain
and LocalDiamond invocations were cached (no Building marker); to get the
allowed fresh elaborations only their source mtimes were touched, never file
contents or TTCs. RenamingComposition elaborated as a CrossTrace prerequisite,
so its later direct invocation was also cached. Those three cached calls are
explicitly **not** fresh evidence. Eight compiler invocations total, sequential,
none failed; no fixture or proof attempt. Timings include five-second polling,
not precise declaration timings; RSS is sampled, not an OS peak. Complete
console logs and UTC windows reside in `/tmp/dgamma-r172/` during this session;
this committed table records their outcome without depending on their retention.

Asserting freeze validator: `python3 -I /tmp/dgamma-r172-validate.py` verifies
these exact values plus the live hole coordinates, branch, allowed untracked
paths, no staged files, whitespace and no prohibited additions:

- full adjacent definition: **1470 bytes**, SHA-256
  `2d01486bf953f11191b758ac3cfb5722d1d02b1a192b6e552adc8a3f58199ecf`;
- statement prefix through `adjacentSwapSuffixSpike =`: **1154 bytes**, SHA-256
  `3aae5a9fbc5b14e0411b4a91e557a6f3dc68c9a6282b9ec2b3fc658cec337adf`;
- review file still untracked, SHA-256
  `61fc23ae4cea4565b442c840be39c41746ecbac73b8c2f73d04f1e3b4f4681e8`;
- CP3 blob `2c697e532e83989de8591fa6a4378747c6a501c0`;
- production/package diff versus `34b21c9`: empty;
- LocalDiamond and **all research source contents** versus `0e58c54`: empty;
- live source hole split **2/4/0/0/1**, delta **0**; DeletionChain `with` count
  **9 → 9**. No source or fixture body is added by Unit A.

The gate requests only the ranked first route above. Independent reviewer
acceptance remains required; baseline typechecks with named holes do not prove
Theorem 73 or discharge the tracked non-hole debts.
