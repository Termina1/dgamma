# R184 grind-shift audit

## Authenticated start and boundaries

2026-09-07 23:01 UTC, HEAD `9ecf780`, branch `cp5-thm73-scoping`.
Tracked tree clean; only permitted paper/ and frozen untracked review.
Idris 2 0.8.0; `--source-dir` and `--check` verified; no orphan compiler.
No new attempt after 02:21 UTC Sep8; safe final gate by 02:46; timeout 03:01.
One new top-level declaration per serialized seeded compiler invocation;
three attempts maximum per micro-unit, fresh ordinary PASS before source commit.
Unchanged 48 GiB guard; no build deletion, cold build, broad R11, production
change, LocalDiamond declaration/visibility change, protected body edit, with,
local alias, computed-existential elimination, scalar replay-builder observer,
unsafe primitive or new hole. Original insertion uniqueness and generated
orchestration matching remain attached to the actual replay bundles.

## Ordered scope

A <=30 prerequisite units: (i) O/A, A/O, O/O orientation rows with derived
cut guards; (ii) simultaneous Cartesian iteration; (iii) source-origin plan
and product count; (iv) coverage; (v) reached target blocks/origin update;
(vi) same-chain endpoint/full bundle/external correspondence. O19 body only
after all six committed. B <=12 selector obligations after A stop/closure;
C <=8 actual paired-prefix synchronization prerequisites if time, no bodies.
No resurrection of R183 A25: actual-pair domain work must compose A26–A30.
O17 root phase, O21 withdrawal and both O20 bodies remain prohibited.

## Status

Start census 5 = 1/3/0/0/1. R183 and R182 audit/evidence inspected. No O19
body attempt or closure claim. Compiler snapshots/logs/intervals collected
under /tmp/dgamma-r184 and archived before gates.

## Unit A stop: A2 exhausted, fully removed

A1 `o19ActualPairEarlyPartialRun`: PASS1,1.041s, commit `50e764e`.
This is a DIRECT composition of the R183 A24 kernel, A29 actual-pair
commutation, A30 frame projection and existing public frame/respect producers.
No generator-map eta argument or reproved A25 body. It produces the actual
captured right partial-effect result; captured-map rebasing and checked
control/tag applicability are NOT established.

A2 `o19TargetAfterObservedInsert` failed3/3 and its entire new module
CP5O19InsertOpeningSpike.idr was removed. No TTC/TTM had been emitted.
- A2-1: targetFiber congruence under the conditional did not convert
  (2.079s); A2-2 explicitly instantiated type arguments, same failure (2.078s).
- A2-3 used the existing resolveViewInactiveInsert by rewrite; native exit0
  nonetheless emitted Undefined name Data.List.Elem.Elem (3.123s). The harness
  correctly records failed, not a compiler PASS. No fourth import-only retry.

This is an elaboration/import stop, NOT structural impossibility or a third
O19 specification gap. All attempted source snapshots and diagnostics retained
only in the compiler archive. O19 body NOT ATTEMPTED; prerequisite(i) lacks
O/A, A/O and O/O rows. Consequently(ii)-(vi) remain open, unchanged from R183.
Micro-unit ledger A2/30:1 retained fresh-PASS declaration,1 wholly reverted
3-attempt unit. Baseline1 + A1/1 + A2/3 =5 serialized checks,2 ordinary PASS,
3 rejected, no interruptions. Audit/stop gate precedes Unit B as authorized.

## Supervisor continuation ruling and second same-seam stop

Supervisor ratified A2 but explicitly ordered continuation of A (28 slots
remained), by DISTINCT observed-value/equation prerequisites; Unit B deferred
until cap or TWO exhausted units in the same seam. No A2 restatement authorized.

A3 `O19ResolutionObservation` PASS1,3.116s (`f4d691a`); A4
`o19ResolutionFromInsertPlan` PASS1,3.107s (`3de39dd`); A5
`o19ResolutionAfterCheckedInsert` PASS1,3.103s (`63c173f`). These own an
actual Maybe View and exact BEFORE/AFTER resolveView equations, produced from
the actual checked OInsert and its original absence witness. They do NOT
claim targetFiber or Begin preservation. Data.List.Elem now directly imported.

A6 `o19TargetFromResolution` FAILED3/3, entire declaration reverted to
committed A5 source. A6-1/2 rewrite could not see resolver under targetFiber
(2.067s/2.075s); A6-3 explicit replace under conditional failed conversion
between identically printed conditional resolver expressions (2.070s).
All are native rejections, no interruptions, no fourth retry. This is the
SECOND 3/3 in the insertion-target transport seam, so the supervisor's genuine
unit-level stop rule applies. No mathematical impossibility or new O19
specification defect claimed. Observed resolver capital survives; target/Begin
and the orientation rows remain open. No O19 body attempt. Six A slots used:
four retained declarations (A1,A3,A4,A5), two exhausted/reverted (A2,A6).
