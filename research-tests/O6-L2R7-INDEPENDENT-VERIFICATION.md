# L2R7 independent evidence verification

**PASS** at `b94ad80f839afec78eb2f985239ed69c6c9b6e1a`, 2026-09-09T06:58:58.214062+00:00, BEFORE publication of this report. This is compiler-free independent reconstruction, not an independent human mathematical review. The parent-owned reviewer gate is still required. Overall research milestone is explicitly **PARTIAL**, NOT a complete cure.

## Reproduce

From `/Users/vyacheslavshebanov/Work/dgamma-lane2`:

```sh
set -e
python3 -I research-tests/run-l2r7-evidence-tests.py
python3 -I research-tests/run-l2r7-independent-verify.py
git diff --check
git diff --cached --name-only
git status --short
```

Do not rerun the immutable final compiler plan or reopen capped proof units. Its17 V10–V26 invocations are already fresh-PASS. The tests/verifier are compiler-free and perform no shared lock/window or main-worktree operation. Full raw receipts are under `/tmp/dgamma-l2r7`; all proof/final-check evidence is also in the committed archive.

## Independently reconstructed

-85 invocation records,76 PASS /9 FAIL;17/17 final own-target Building lines and exact final source hashes. Every invocation serialized; source-mutation/interruption flags false.
-57 retained declarations in17 new modules;57 one-declaration guarded source commits. A22/B16/C12/D8 caps consumed, no extension. A8 exhausted3/3 and exact revert authenticated. No fourth attempt/restatement. B2 PASS then rstrip-only V1 final validation authenticated.
-All commits have exact source-hash/fresh-PASS/no-intervening-invocation receipts. The final plan was committed in630c0bb5 BEFORE checks and orders direct new-module dependencies before dependents.
-Maximum250ms sampled RSS2,397,632KiB (2.286560059GiB), not a continuous peak guarantee. No own compiler, no staged files, clean working tree at verification.
-All source snapshots/logs/individual JSON records match raw ledger and immutable archive. Archive SHA256: `30d204f665426b31e2867566921fd88c00480dc1f38aa51006173b0eaf3b560c`.
-Exact3 authorized L2R6 runner HEADER-ONLY repairs preserve executable ASTs. No predecessor Idris body/comment/statement delta. No companion authority this shift.
-Production src, frozen research, package/root docs untouched. Only predecessor non-Idris edits are the authorized draft/manifest and3 headers. All original Tier-1 fragments, predecessor source hashes, CP3 relocation and30 applied renamings verified; new SAME-BUNDLE C candidate's source/fragment hashes verified separately.
-25/25 evidence regression tests pass on actual helper/assertion ASTs. No compiler or process signalling used by tests or verifier.

## Exact semantic boundary

General classifier correspondence and computed-catalog→actual placed-occurrence producers close. Same-bundle controls grammar, old→new inclusion, ten-edge fixture with both controls INSIDE parent [0,8) and literal gap0, and conditional C zero-gap close. General phases/move existence/normalizer/D8 reproduction, cross-bundle authenticated generation history, general anchor transport/distance invariance and scoped NF/all-premises zero-gap remain OPEN. GeneralAttachedNormalForm is a TYPE only. The phase-contiguity obstruction is a candidate, not a checked counterexample; coreContiguityRestored remains OPEN.

The archive boundary is630c0bb5 (all source receipts and final validations); source boundary435bd364. E2/E3 publication receipts cannot be recursively included in their own archive. The verifier checks archived receipts as an exact prefix of the live append-only receipt log and authenticates every later artifact commit. This report stores the E2 checkpoint; rerunning the verifier after E3 authenticates its publication too, writing the latest report only to `/tmp/dgamma-l2r7/independent-verification.json`.
