# R206 semantic scope gate

Requested after source inspection, before any Idris edit or native invocation.

## Supervisor ruling

APPROVED — proceed with the honest checked-partial plan, no fabricated casts: (1) keep o19OwnedSafeWord's old two-case observation as an EXPLICITLY RESTRICTED input (a named predicate "legacy actor-only block": ActorWithoutForcedRoots whose core has no CoreChildRetire/Remove steps) to a CONDITIONAL assembler of the existing O19SourcePairObservation; (2) add the expanded TOTAL observation/producer over the production grammar (legacy cases OR concrete child-control / attached-root cases) as a new declaration; (3) migrate unconditional consumers (o19OriginalClassesConditional / OriginalClasses, ReachedBlocks, PaperBranchCompleteness) only to the exact weaker results that actually hold, each with a NOTES entry; (4) record every newly broken root and the blocked closure in the disposition — CrossTrace's fresh PASS is NOT promised this shift. In parallel write research-tests/O6-R206-O19-EXPANSION-DESIGN.md: the gated redesign for R207 — the expanded O19 source-pair/column classes for the attached grammar (child Retire/Remove inside the block; forced-root bundle inserts/controls), which adjacent-swap statements (frozen O19 body, LocalDiamond) it must re-derive, and which A10/A12 paper-gap cases it introduces. Then, as ordered: V19 restatement/retirement, R172 retirement, the visibility companion + A15, and Units B/A. This is exactly the consumer re-proof obligation the CP3 draft predicted; say so in the audit.

## Consequence

No frozen or production edit is authorized. Expanded observation is not a cast to the old four source-pair cases; action-word equality is not transport of source-state lookup/parent/AttachedReason evidence. Fresh validation and blocked dispositions remain separate.
