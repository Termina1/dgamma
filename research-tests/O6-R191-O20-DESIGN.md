# R191 O20 ownership map / exact R192 frontier

This is checked **partial capital for paper Theorem 73**, not closure of either
protected O20 theorem. The protected selector and convergence statements/bodies
are byte-unchanged. O19 remains closed. Production and CP3 are unchanged.

## Retained producers

| Obligation | Idris location / declaration | Precise status |
| --- | --- | --- |
| Common accepted supported reference | `CP5O20SupportedReferenceSpike.o20AcceptedSupportedReference` | Produced from accepted inputs, not a supplied reference oracle. |
| Same actual search carries that reference | `CP5O20ReferenceDescentSpike.o20SearchAcceptedReference` | Initializes the actual total operational descent and carries the fixed reference. |
| Finite inversion at actual stop | `CP5O20FiniteInversionSpike.o20StoppedInversionAvailable` | Equality **or** an adjacent goal inversion; the non-goal branch remains explicit. |
| Physical two-sided child exclusion | `CP5O20InversionChildSafetySpike.o20ReachedInversionChildSafety` | Derived at the actual reached inversion from sealed birth/original metadata correspondence and supported incomparability. |
| Checker completeness | `CP5O20SafeBlockSelectionSpike.o20CheckSafetyAtMembersComplete` | Complete at its exact member-selected blocks, **given** the four physical clauses. Early Begin and zero gap are not supplied by this theorem. |
| Finite filtering completeness | `CP5O20SafeBlockSelectionSpike.o20MapMaybeSelectionComplete` | A real positive anywhere in the enumerated list is found. Exact candidate/orientation membership is still required. |
| General actual paired Begin | `CP5O20SharedBeginAdapterSpike.o20PairedActualBeginCut` | Both actual Begin observations, shared component and successor are produced from the actual steps and pre-cut. |
| Actual observed Advance / last-step Finish | `CP5O20PairedAdvanceSpike.o20PairedObservedAdvanceCut` | Complete all-name successor from actual checked edges and authenticated shared-program/capability/callback observations. Not yet their general canonical extraction. |
| Actual empty-program Finish | `CP5O20PairedAdvanceSpike.o20PairedObservedEmptyFinishCut` | Complete all-name successor, including ordered tables and pointwise undo. |
| Actual insertion/retirement gaps | `CP5O20PairedAdvanceSpike.o20PairedObservedInsertCut`, `o20PairedObservedRetireCut` | Cover roots or children at actual checked edges; no zero-gap/block-placement claim. |
| Actual paired execution induction | `CP5O20PairedExecutionSpike.o20PairedExecutionCut` | Total structural induction through the native-edge stage family. No post-cut or preservation callback is stored in a constructor. |
| Explicit LTS projection | `CP5O20PairedExecutionSpike.o20PairedExecutionTraces` | Produces both actual indexed traces at the same physical cuts. |
| All-canonical original-birth disposition | `CP5O20CanonicalBirthDispositionSpike.o20CanonicalOriginMatchOrClosing` | Exact source stamp plus authentic closing rejection **or** matched right-ORIGINAL birth and historical generation equation. Not the full right-canonical/current-name triangle. |
| Operational integration | `R191PairedExecutionPositive.r191PairedRunProducesEndpoint` | Eleven actual paired edges, all five stage constructors, and all-name endpoint from empty origin. Identity renaming only. |

Proofs are total and erased. Runtime states, reference/order data and the
successful Advance control branch remain concrete data/functions. No new unsafe
escape, axiom, partial definition or hole was introduced. No plugin runtime or
runtime performance result is claimed.

## Restricted reference and the approved internal migration

`O20SupportedPath` annotates **every vertex**, not just its endpoints, as
supported. Its raw projection is sound; the reverse coercion is not available.
The fresh R6 fixture demonstrates the unsupported-intermediate seam, but it is
not an accepted-selector countermodel.

The separately ratified four-module migration removed unused `goalState` and
full-path goal linearization plumbing, retaining the exact `UniqueKeys goalOrder`
actually consumed by rank descent. Enumeration, actual O19 safety calls,
operational/result folds and fixed-goal measure decrease were unchanged. The
obsolete conditional full-path helper was removed without callers; it was not
silently proved at the stronger type. See the audit and the special multi-source
fresh-check receipt for commit `c9d77cac8fea9f300a6fc182817b1edbc55f8cf6`.

## A10: real candidate, not a proved accepted counterexample

`R191CanonicalChildRetirementGap` owns:

- eleven actual checked edges at explicit physical cuts;
- actual alignment and parent-yield/child-retirement discipline;
- all fifteen `ReplayInvariantBundle` clauses;
- all three frozen `LocatedOpenEpisodeBlock` values.

The parent block occupies `[3,6)`, actor 1 occupies `[6,8)`, and actor 2 occupies
`[9,11)`. The real `ORetire 3` at ordinal 8 is a **child** retirement outside every
selected block. The frozen `ActorLifecycleOnly` grammar permits child insertion,
not child retirement/removal. Root-first constrains root orchestration; by itself
it does not eliminate these child actions.

However, F7's full decomposition failed native total coverage of finite
`BeforeIn` cases on the three-block order and exhausted three attempts. Full
decomposition, `CanonicalInputPlacement` and `IndependentCanonicalSchedule`
were not inhabited. F8 was never invoked. The retained fixture is therefore NOT
a refutation of the protected selector. The later paired-run fixture proves a
different operational property and does not retry or replace F7.

**Owner decision:** settle A10 before any grammar/placement/safety surface edit.
Possible research-side cures still need an exact clause map and accepted
positive/negative fixtures; no cure was implemented in R191. For the next
accepted-candidate construction, the supervisor directs the **R188 target-order
selector pattern**, not another flat finite-order case split.

**A8 correction:** initial all-root placement is already owned by accepted O20
capital. A8 is an **O17-only initial-placement construction dependency**, not an
extra O20 premise. What remains in O20 is valid operational preservation and
physical gap ownership, subject to A10.

## Convergence: what the new induction does not assume or prove

`O20PairedStage` constructors store two actual native checked edges (or two
`BeginStep` values), source observations and input alignment metadata. They do
not store an endpoint relation, a successor all-name cut or a preservation
function. Thus the all-name induction is genuine, not a restatement of its
conclusion as a constructor field.

Nevertheless, arbitrary accepted canonical traces have not been shown to
inhabit this family. The Advance constructor still requires shared-program
observations. Remove, failure and diversion are not constructors. No theorem
says that all accepted traces exclude the omitted cases. These limitations are
not hidden by the successful eleven-edge fixture.

The general Begin adapter uses the expressly authorized observed-VALUE boundary:
component equality is derived and eliminated before the component-indexed
payloads. This does not reattempt R190 B7's exhausted suspended-record equality
proof.

## Unsupported-birth analysis

The historical `RegistrationGenerationBijection` and the fixed current
`NameBijection` are different objects. `CurrentEndpointRenaming` admits a
vestigial alternative. `originalRegistrationAccounted` admits withdrawal or
retention. Neither alternative may be discarded by fiat.

The new disposition producer authenticates every actual canonical origin,
including children absent/unsupported at the original endpoint. Uniqueness pins
the scanner stamp to that exact occurrence. A retained scanner event then owns
its actual opposite ORIGINAL event/birth and historical generation equation;
authentic closing remains the other branch. No endpoint-support or lookup
oracle is required, and no O21 withdrawal theorem is consumed.

This result does not provide fixed current child/parent names or a retained
right-canonical occurrence. It is a proved weaker statement, not convergence
closure and not proof that the protected theorem is false.

## Exact R192 work list

1. **Owner A10 decision / accepted candidate:** resolve the child-orchestration
   grammar/placement seam; follow the ratified next-shift construction pattern.
2. **Selector physical zero gap:** produce it from the actual accepted and
   operationally preserved clauses, subject to A10. Do not add it as an oracle.
3. **Selector right-first Begin:** transport actual birth/target-domain/guard
   ownership to the pre-left cut. A `Maybe` checker is not an inhabited
   `CheckedEarlyApplication`.
4. **Selector wiring:** connect the finite actual inversion to the exact
   candidate enumeration and orientation checker.
5. **Selector terminal equation:** prove `stoppedOrder = goal` only after the
   physical safety/selection prerequisites; then a fresh gated body attempt.
6. **Convergence canonical paired extraction:** derive every actual paired cut
   and shared Advance observation from accepted canonical executions.
7. **Convergence missing-case completeness:** handle or honestly exclude
   Remove/failure/diversion and the remaining asynchronous gap cases.
8. **Convergence unsupported triangle:** resolve the explicit closing/current
   name/right-canonical retention branches and produce the exact fourth clause.
9. Attempt convergence's body only when all four clauses are producer-owned.

No O17 work, O21 withdrawal consumption, frozen O19/adjacent-swap edits, ungated
LocalDiamond changes, or disguised retries of R190 A24/B7 or R191 CF1/F7 belong
in this list without the corresponding new-shift authorization.

## Status

94 retained new declarations plus one explicitly approved internal plumbing
migration. Accepted reference/search, finite inversion, physical child safety,
general actual Begin, actual observed paired operational induction/LTS
projections and the weaker exact origin disposition are checked. Selector
completeness and canonical convergence remain partial. Both protected O20 bodies
remain untouched. The full theorem census remains four holes, split
`1/2/0/0/1`. Final regression evidence is recorded separately in the verification,
compiler ledger/archive and append-only audit; author review is not independent
acceptance.
