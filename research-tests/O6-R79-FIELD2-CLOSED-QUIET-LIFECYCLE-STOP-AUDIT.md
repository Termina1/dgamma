# O6 revision 79: field 2 and fields 3–5 close; quiet lifecycle stop

## Scope

Grind shift #87 (overall #141) resumed from accepted revision-78 boundary
`dcbaa0f`. It executed the authorized mechanically reshaped whole-prefix
consumer and then advanced the private target bundle strictly in field order.
All frozen revision-19/20/21 capital, the revision-76 retirement package, the
revision-77 moved-pair proofs, revision-78 prefix view, and the public adjacent
interface remain unchanged.

## Retained checked capital

Every retained unit received TTC/TTM removal, a visible direct CP5 source
rebuild, a lemma-sized commit, and an additional R16 assembly check.

1. `reindexPrefixRegistrationStepExplicit` (`95aed52`) reindexes the stored
   dependent prefix head and rest with fully explicit statement-level types.
2. `reindexPrefixRegistrationTailExplicit` (`17e5d91`) reindexes the tail
   discipline with fully explicit state/trace types.
3. `adjacentRegistrationDiscipline` (`648bc5b`) closes on its first fresh retry.
   It has no dependent `let` block: it eliminates the frozen
   `PrefixRegistrationDisciplineView` once, calls both helpers before the
   registration callback, and passes exact append-indexed retirement provenance
   to `adjacentChildRetirementProvenance`.
4. `AdjacentAlignedPointwiseReplay.alignedReplayDiscipline` and its producer
   population (`1c4fbf1`) complete bundle field 2.
5. Field 3, initial well-formedness (`ed94546`), is retained directly from the
   authenticated source bundle.
6. Field 4, initial emptiness (`b03858b`), is retained directly from the same
   source initial state.
7. Field 5, final well-formedness (`ef71a05`), is retained from the sealed suffix
   endpoint.

All proof-only fields and locals are quantity 0.

## Exhausted field-6 helper

The first field-6 helper, `pointwiseQuietFiberTrue`, exhausted its independent
three-attempt budget. It was reconstructed from the historical revision-22
pointwise-domain argument, but checked directly rather than trusting the stale
revision-74 chain.

1. Direct rewriting by equality of concrete target queries failed to rewrite the
   reduced `quietFiber` branch.
2. Replacing rewrite with `cong isNothing` closed the inactive-success case.
   The active case then exposed independently bound source and target view names.
3. Matching the target lifecycle instead of the source made both views print as
   `rightView`, but Idris still reported them as distinct dependent binders:

```text
Mismatch between: rightView ... and rightView.
```

The failure occurs while composing the active branch's `targetMatches` equation;
it is a correlated dependent lifecycle/view wall in the known revision-74
class. It is not evidence against quietness preservation, but it is semantic
elaboration rather than layout, so work stopped immediately. The complete
unverified helper was removed. Fields 2–5 remain at safe HEAD `ef71a05`.

## Recommended cure

Separate lifecycle quietness from concrete fibers:

1. define a tiny executable/private `lifecycleQuietAt lifecycle target` with the
   four `quietFiber` mode equations;
2. prove `lifecycleControlQuietRelated` directly by eliminating one
   `LifecycleControlRelated`, taking target-query equality as an argument;
3. only then connect `quietFiber` to `lifecycleQuietAt` and use
   `pointwiseConcreteTargetFiberSame`.

This keeps source/target views under the single lifecycle-relation elimination
and avoids reopening independently named `MkFiber` lifecycles. The lifecycle
lemma and the fiber bridge must receive separate fresh three-attempt budgets.
After field 6, rebuild the finite target-registry fold with explicit target
membership, then continue field 7 onward.

## Status

- whole-prefix consumer: **closed**;
- bundle field 2: **complete**;
- fields 3–5: **complete**;
- field 6 (`replayQuiet`): **open at correlated active-view transport**;
- fields 7–15: **unopened in this shift**;
- result assembly/body: **unopened**;
- holes: **20**, split **6/4/8/1/1**.

This shift closed field 2 and four subsequent record positions. The remaining
estimate narrows from **1–13** to an honest **1–12 shifts**.

## Isolation

The 1183-byte `adjacentSwapSuffixSpike` signature retains SHA-256
`e6d0c2d669355e5b7bef9efea1707f5853c708deb888bafe5770ba40dbd15a41`.
Production `src/`, `dgamma.ipkg`, and CP3 blob
`2c697e532e83989de8591fa6a4378747c6a501c0` remain isolated. No hole, postulate,
escape hatch, detached caller premise, or public surface was added.
