# O6 revision 38: O-Remove closure and L-Unload exact-map stop

## Scope

Shift #46 remains private to the O6 research body. The frozen
`adjacentSwapSuffixSpike` declaration/body hole, `SealedSuffixReplaySpine`,
opaque adjacent result, manifest, production `src/`, package, and immutable CP3
theorem are unchanged. No caller-selected checked target, endpoint, guard, map,
occurrence, ordinal, bundle, or new output-shaped premise was introduced.

## O-Remove closes

`replayPointwiseRemoveHead` resumes the fully reverted revision-37 producer with
both Boolean equality declarations parenthesized and the source observation and
owner explicitly bound at quantity zero. The documented correction elaborates
without a new non-syntax wall.

The complete producer consumes `PointwiseRemoveSourceObservation`, pointwise
owner lookup, `pointwiseNoChildPreserved`, `pointwiseRemovalGuardRelated`, and
`pointwiseControlAfterDelete`. It owns:

- the exact target composite guard and concrete delete endpoint;
- target preservation and checked transition;
- source/target O-Remove effect frames and the empty-table relation;
- the next effect/control endpoint;
- exact singleton action map;
- singleton non-advance RAR;
- occurrence, ordinal, tag, action, and alignment evidence.

Commit: `3aefe30` — `replay pointwise remove suffix heads`.

O-Remove joins O-Insert, O-Retire, L-Begin, L-Divert, and L-Leave. Six of the
eight generic action families are now closed.

## L-Unload stops at frozen exact-map capital

The first L-Unload prerequisite tested the exact field required twice by the
sealed replay design:

```idris
(state : EffectState name key value world) ->
partialEffectMap sourceStep state = partialEffectMap replayedStep state
```

It appears in both `RelationalReplayCorrespondence.replayGeneratorMapPreserved`
and `PointwiseRelationalHeadReplay.headReplayMapPreserved` (and is copied into
`SealedSuffixReplaySpine.headMapPreserved`). For L-Unload, Table-1 maps execute
the owner accumulator and install its output table.

`FiberControlRelated` deliberately supplies only `AccumulatorRelated`. That
relation proves equal output ambient values and equal complete ordered binding
lists; it does not equate the erased `UniqueKeys` functions stored in the output
`CoeffectContext`. This is the accepted CP4 Finding-12 boundary, not missing
lifecycle information.

The generic exact-map attempt reduced both accumulators at the same canonical
input, eliminated `LocalStateRuntimeRelated` ambient and binding equalities, and
then failed when exact output equality still required equality of independently
stored proof functions. Because this is a new output-shaped-capital demand, the
critical O6 rule triggers an immediate STOP-AUDIT rather than spending two more
attempts on proof irrelevance.

The self-contained expected failure
`DGamma.R38UnloadExactMapFromRuntimeAccumulatorNegative` makes the obstruction
concrete. `ProbeLeftAccumulator` and `ProbeRightAccumulator` ignore their input
and return tables with identical runtime bindings but distinct valid uniqueness
functions. They constructively satisfy `AccumulatorRelated`. Asking for equality
of their unload runtime maps fails exactly at:

```text
Can't solve constraint between: probeFreshRight and probeFreshLeft.
```

Commit: `a1578fb` — `pin unload exact map scoping failure`.

The runner now tracks 43 exact negatives and 88 Idris test modules. This is not
a claim that the two runtime maps are observationally different: their outputs
are `EffectStateRelated`. It proves only that the frozen exact equality cannot be
derived from the intentionally weaker programming interface.

## Rejected continuations

The following continuations are not authorized:

1. accepting exact map equality as a caller premise — output-shaped capital;
2. strengthening `AccumulatorRelated` to proof-bearing output equality — breaks
   CP4 Finding-12 and the executable proof-distinct accumulator witness;
3. identifying `UniqueKeys`, `No`, or arbitrary function closures — unavailable
   proof irrelevance/function extensionality;
4. silently canonicalizing production L-Unload map outputs — changes executable
   `src/` semantics and production isolation; or
5. weakening only the sealed-head field while leaving
   `RelationalReplayCorrespondence.replayGeneratorMapPreserved` exact — the same
   obstruction immediately recurs in singleton RAR packaging.

A genuine repair would require a separately scoped frozen-interface review of
map preservation throughout `RelationalReplayCorrespondence`, singleton RAR,
sealed heads, and the independence consumers, replacing proof-bearing equality
with the existing `PartialRelated ... EffectStateRelated` notion where sound.
No such repair is proposed or made in this shift.

## L-Advance and suffix composition

L-Advance was not opened after the L-Unload STOP-AUDIT. It is expected to meet a
related yielded-map boundary and must not be used to bypass the frozen map issue.
Whole-suffix composition was not started, as required.

## Safe boundary and estimate

Six generic semantic families are closed. L-Unload and L-Advance remain open,
with L-Unload blocked specifically by frozen exact map capital. Whole-suffix
RAR/ordinal composition, target bundle construction, and final opaque adjacent
result remain open.

Holes remain **20**, split **6/4/8/1/1**. Despite reaching 6/8 families, the O6
estimate is held at **18–34 shifts** rather than narrowed: the newly pinned exact
map obstruction requires a frozen-interface scoping/review decision before
L-Unload, likely L-Advance, or whole-suffix capital can honestly close.
